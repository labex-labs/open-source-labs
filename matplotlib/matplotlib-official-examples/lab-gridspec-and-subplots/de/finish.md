# Zusammenfassung

In diesem Lab haben wir gelernt, wie man zwei Subplots in Matplotlib mit `subplots` und `GridSpec` kombinieren kann. Wir haben eine Figur mit Subplots erstellt, das `GridSpec` von den Achsen erhalten, die zugrunde liegenden Achsen entfernt, eine größere Achse hinzugefügt, die größere Achse beschrieben und das Layout der Subplots angepasst. Schließlich haben wir das Diagramm mit Matplotlib angezeigt.
