# Создаем образцовые данные

Далее мы создадим образцовые данные для нашего графика с использованием NumPy. Мы сгенерируем 100 точек данных в диапазоне от 0 до 2π и вычислим соответствующие значения синуса.

```python
x = np.linspace(0, 2 * np.pi, 100)
y = 2 * np.sin(x)
```