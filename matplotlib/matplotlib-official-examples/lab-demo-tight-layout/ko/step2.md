# 간단한 플롯 생성

Matplotlib 을 임포트했으므로 시각화를 생성할 수 있습니다. 간단한 플롯을 생성하는 것으로 시작해 보겠습니다.

```python
x = [1, 2, 3, 4, 5]
y = [2, 4, 6, 8, 10]

plt.plot(x, y)
plt.show()
```

여기서는 플롯의 x 및 y 값을 포함하는 두 개의 리스트 `x`와 `y`를 생성합니다. 그런 다음 `plot` 함수를 사용하여 `x`와 `y`의 선 그래프를 생성합니다. 마지막으로, `show` 함수를 사용하여 플롯을 표시합니다.
