# ライブラリのインポート

最初のステップは、必要なライブラリをインポートすることです。グラフを作成するために Matplotlib と、データを生成するために NumPy を使用します。

```python
import matplotlib.pyplot as plt
import numpy as np
import matplotlib.animation as animation
```
