# Configurando o Ambiente

Antes de começarmos a criar nosso gráfico, precisamos configurar o ambiente. Abra seu terminal e crie um novo arquivo Python chamado `mpl_with_glade3.py`. Certifique-se de ter instalado as bibliotecas necessárias: `matplotlib`, `numpy`, `gi` e `Gtk`.
