# 3番目のセットのハッチパターンを作成する

2つのパターンを組み合わせて新しいパターンを作成することで、3番目のセットのハッチパターンを作成します。以下のリストを使用します。

```python
hatches = ['/o', '\\|', '|*', '-\\', '+o', 'x*', 'o-', 'O|', 'O.', '*-']
```

四角形を作成するために、以前と同じループを使用します。

```python
for ax, h in zip(axs.flat, hatches):
    hatches_plot(ax, h)
```
