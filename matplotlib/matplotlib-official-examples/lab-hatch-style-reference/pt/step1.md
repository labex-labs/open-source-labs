# Importar as bibliotecas necessárias

Para usar o Matplotlib, devemos importar a biblioteca e os módulos necessários. Usaremos o módulo `pyplot` para criar os subplots e o módulo `patches` para criar o objeto `Rectangle`.

```python
import matplotlib.pyplot as plt
from matplotlib.patches import Rectangle
```
