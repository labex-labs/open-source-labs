# 필요한 라이브러리 임포트

Matplotlib 을 사용하려면 라이브러리와 필요한 모듈을 임포트해야 합니다. 서브플롯 (subplots) 을 생성하기 위해 pyplot 모듈을 사용하고, Rectangle 객체를 생성하기 위해 patches 모듈을 사용합니다.

```python
import matplotlib.pyplot as plt
from matplotlib.patches import Rectangle
```
