# 세 번째 해칭 패턴 세트 생성

두 패턴을 결합하여 새로운 패턴을 생성하여 세 번째 해칭 패턴 세트를 생성합니다. 다음 목록을 사용합니다.

```python
hatches = ['/o', '\\|', '|*', '-\\', '+o', 'x*', 'o-', 'O|', 'O.', '*-']
```

이전과 동일한 루프를 사용하여 사각형을 생성합니다.

```python
for ax, h in zip(axs.flat, hatches):
    hatches_plot(ax, h)
```
