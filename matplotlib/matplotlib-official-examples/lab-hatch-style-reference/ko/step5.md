# 두 번째 해칭 패턴 세트 생성

밀도를 높이기 위해 각 패턴을 두 번 반복하여 두 번째 해칭 패턴 세트를 생성합니다. 다음 목록을 사용합니다.

```python
hatches = ['//', '\\\\', '||', '--', '++', 'xx', 'oo', 'OO', '..', '**']
```

이전과 동일한 루프를 사용하여 사각형을 생성합니다.

```python
for ax, h in zip(axs.flat, hatches):
    hatches_plot(ax, h)
```
