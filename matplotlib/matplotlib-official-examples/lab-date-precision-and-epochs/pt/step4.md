# Conversão de ida e volta da data

Podemos então fazer a conversão de ida e volta da data usando a função `mdates.num2date` para garantir que a conversão seja precisa.

```python
date2 = mdates.num2date(mdate1)
```
