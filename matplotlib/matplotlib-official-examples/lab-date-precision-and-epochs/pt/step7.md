# Converter a data de volta com a nova época

Podemos então converter a data de volta usando a função `mdates.num2date` para garantir que a conversão seja precisa.

```python
date2 = mdates.num2date(mdate1)
```
