# Definir a época para o padrão antigo

O próximo passo é definir a época para o padrão antigo, que são os dias desde "0000-12-31T00:00:00". Isso é feito usando o método `mdates.set_epoch`.

```python
mdates.set_epoch('0000-12-31T00:00:00')
```
