# Importar pacotes necessários

O primeiro passo é importar os pacotes necessários, incluindo `datetime`, `matplotlib.pyplot` e `numpy`.

```python
import datetime
import matplotlib.pyplot as plt
import numpy as np
import matplotlib.dates as mdates
```
