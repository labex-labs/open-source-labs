# 필요한 패키지 가져오기

첫 번째 단계는 `datetime`, `matplotlib.pyplot`, `numpy`를 포함한 필요한 패키지를 가져오는 것입니다.

```python
import datetime
import matplotlib.pyplot as plt
import numpy as np
import matplotlib.dates as mdates
```
