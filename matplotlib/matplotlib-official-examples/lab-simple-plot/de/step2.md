# Daten generieren

Wir müssen Daten für das Diagramm generieren. In diesem Beispiel werden wir zwei Arrays, `t` und `s`, generieren.

```python
t = np.arange(0.0, 2.0, 0.01)
s = 1 + np.sin(2 * np.pi * t)
```
