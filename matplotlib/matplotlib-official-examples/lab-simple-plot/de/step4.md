# Beschriftungen und Titel hinzufügen

Wir können Beschriftungen für die x- und y-Achse sowie einen Titel für das Diagramm hinzufügen, indem wir `ax.set()` verwenden.

```python
ax.set(xlabel='time (s)', ylabel='voltage (mV)', title='About as simple as it gets, folks')
```
