# Agregar etiquetas y título

Podemos agregar etiquetas al eje x y al eje y, así como un título al gráfico utilizando `ax.set()`.

```python
ax.set(xlabel='tiempo (s)', ylabel='voltaje (mV)', title='Lo más simple posible, amigos')
```
