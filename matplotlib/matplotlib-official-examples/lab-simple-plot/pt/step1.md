# Importar as bibliotecas necessárias

Antes de começarmos a criar o gráfico, precisamos importar as bibliotecas necessárias. Neste caso, precisamos importar `numpy` e `matplotlib.pyplot`.

```python
import numpy as np
import matplotlib.pyplot as plt
```
