# Adicionar rótulos e título

Podemos adicionar rótulos aos eixos x e y, bem como um título ao gráfico, usando `ax.set()`.

```python
ax.set(xlabel='time (s)', ylabel='voltage (mV)', title='About as simple as it gets, folks')
```
