# Acessar Toolbar e VBox

Acessaremos os atributos toolbar e vbox do gerenciador de canvas da figura usando os métodos `manager.toolbar` e `manager.vbox`, respectivamente.

```python
manager = fig.canvas.manager
toolbar = manager.toolbar
vbox = manager.vbox
```
