# 툴바 (Toolbar) 및 VBox 접근

`manager.toolbar` 및 `manager.vbox` 메서드를 사용하여 figure canvas manager 의 툴바 (toolbar) 및 vbox 속성에 각각 접근합니다.

```python
manager = fig.canvas.manager
toolbar = manager.toolbar
vbox = manager.vbox
```
