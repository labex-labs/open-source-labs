# グリッドを作成する

次に、ベクトル場を表示するための点のグリッドを作成します。この例では、NumPy の`meshgrid`関数を使って点のメッシュグリッドを作成します。`arange`関数は、指定された区間内で等間隔に配置された点の配列を作成するために使用されます。

```python
x, y, z = np.meshgrid(np.arange(-0.8, 1, 0.2),
                      np.arange(-0.8, 1, 0.2),
                      np.arange(-0.8, 1, 0.8))
```
