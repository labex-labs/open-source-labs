# フォント辞書を定義する

次に、テキストとラベルのスタイルオプションを含むフォント辞書を定義します。この例では、フォントファミリーを「serif」、色を「darkred」、太さを「normal」、サイズを16に設定します。

```python
font = {'family': 'serif',
        'color':  'darkred',
        'weight': 'normal',
        'size': 16,
        }
```
