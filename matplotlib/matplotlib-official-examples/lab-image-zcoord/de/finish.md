# Zusammenfassung

In diesem Lab haben wir gelernt, wie man den Koordinatenformatter in Matplotlib so modifiziert, dass der Bild-„z“-Wert des nächsten Pixels bei gegebenen x- und y-Werten ausgegeben wird. Wir haben auch gelernt, wie man mit der `imshow`-Methode ein Diagramm einer Zufallsmatrix erstellt und die `~.axes.Axes.format_coord`-Funktion anpasst, um den Koordinatenformatter zu modifizieren.
