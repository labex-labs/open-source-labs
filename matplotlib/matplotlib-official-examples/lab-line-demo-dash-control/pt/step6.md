# Definir o estilo de traço usando um `property_cycle`

Podemos configurar o estilo de traço usando um `property_cycle`. Isso pode ser feito passando uma lista de sequências de traços usando a palavra-chave _dashes_ para o cycler. Neste exemplo, não usaremos este método.
