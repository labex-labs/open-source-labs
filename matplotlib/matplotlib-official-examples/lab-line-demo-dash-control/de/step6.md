# Die Strichstil mit einem `property_cycle` einstellen

Wir können den Strichstil mit einem `property_cycle` konfigurieren. Dies kann dadurch erreicht werden, dass eine Liste von Strichfolgen mit dem Schlüsselwort _dashes_ an den Cycler übergeben wird. In diesem Beispiel werden wir diese Methode nicht verwenden.
