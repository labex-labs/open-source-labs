# 요약

이 Lab 에서는 Python 의 Matplotlib 을 사용하여 축 (axes) 없이 Figure 에 선을 추가하는 방법을 배웠습니다. `matplotlib.pyplot` 및 `matplotlib.lines` 라이브러리를 사용하여 `Figure` 객체를 생성하고 `fig.add_artist()` 메서드를 사용하여 선을 추가했습니다. 그런 다음 `plt.show()` 메서드를 사용하여 Figure 를 표시했습니다.
