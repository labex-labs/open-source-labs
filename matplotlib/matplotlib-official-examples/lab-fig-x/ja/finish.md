# まとめ

この実験では、Python の Matplotlib を使って、軸なしのグラフに線を追加する方法を学びました。`matplotlib.pyplot` と `matplotlib.lines` ライブラリを使って `Figure` オブジェクトを作成し、`fig.add_artist()` メソッドを使ってそれに線を追加しました。その後、`plt.show()` メソッドを使ってグラフを表示しました。
