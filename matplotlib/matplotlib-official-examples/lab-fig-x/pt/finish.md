# Resumo

Neste laboratório, aprendemos como adicionar linhas a uma figura sem eixos usando Matplotlib em Python. Usamos as bibliotecas `matplotlib.pyplot` e `matplotlib.lines` para criar um objeto `Figure` e adicionar linhas a ele usando o método `fig.add_artist()`. Em seguida, exibimos a figura usando o método `plt.show()`.
