# Importar as bibliotecas necessárias

Primeiramente, precisamos importar as bibliotecas necessárias. Usaremos `matplotlib.pyplot` e `matplotlib.lines` para este laboratório.

```python
import matplotlib.pyplot as plt
import matplotlib.lines as lines
```
