# 요약

이 랩에서는 Python Matplotlib 에서 아티스트가 다른 아티스트를 포함하도록 기본 메서드를 재정의하는 방법을 보여주었습니다. `lines.Line2D`에서 상속받고 레이블을 지정하기 위해 `mtext.Text` 인스턴스를 포함하는 `MyLine`이라는 사용자 정의 클래스를 만들었습니다. 그런 다음 선을 축 객체에 추가하고 플롯을 표시했습니다.
