# 라이브러리 임포트

시작하려면 필요한 라이브러리를 임포트해야 합니다. 이 경우, `matplotlib.pyplot`, `matplotlib.artist`, `matplotlib.patches`, 그리고 `matplotlib.transforms`에서 `IdentityTransform`을 임포트해야 합니다.

```python
import matplotlib.pyplot as plt
import matplotlib.artist as artist
import matplotlib.patches as patches
from matplotlib.transforms import IdentityTransform
```
