# Importar Bibliotecas

Para começar, precisamos importar as bibliotecas necessárias. Neste caso, precisamos importar `matplotlib.pyplot`, `matplotlib.artist`, `matplotlib.patches` e `IdentityTransform` de `matplotlib.transforms`.

```python
import matplotlib.pyplot as plt
import matplotlib.artist as artist
import matplotlib.patches as patches
from matplotlib.transforms import IdentityTransform
```
