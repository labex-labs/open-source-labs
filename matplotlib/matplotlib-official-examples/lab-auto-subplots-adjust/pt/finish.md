# Resumo

Neste laboratório, aprendemos como ajustar programaticamente os parâmetros dos subplots com base no tamanho dos rótulos. Usamos o módulo `matplotlib.transforms` para calcular as bounding boxes (caixas delimitadoras) dos rótulos e o `draw_event` para chamar nossa função `on_draw`.
