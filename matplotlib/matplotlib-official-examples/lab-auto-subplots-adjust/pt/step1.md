# Importar as bibliotecas necessárias

Precisaremos de `matplotlib.pyplot` e `matplotlib.transforms` para criar o gráfico e manipular os parâmetros dos subplots.

```python
import matplotlib.pyplot as plt
import matplotlib.transforms as mtransforms
```
