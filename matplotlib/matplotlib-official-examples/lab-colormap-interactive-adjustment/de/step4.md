# Füge die Farbskala hinzu

Um die Farbskala interaktiv anzupassen, musst du der Grafik eine Farbskala mit der Funktion `colorbar()` hinzufügen.

```python
fig.colorbar(im, ax=ax, label='Interactive colorbar')
```
