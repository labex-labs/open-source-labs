# まとめ

この実験では、Matplotlib の`floating_axes`モジュールを使って矩形でない形状のカスタムプロットを作成する方法を学びました。異なる座標系を使って散布図、棒グラフ、セクタープロットを作成しました。必要な変換を作成するために`GridHelperCurveLinear`を使い、グリッド線を設定するために`FixedLocator`と`MaxNLocator`を使い、目盛りのラベルをフォーマットするために`DictFormatter`と`FormatterHMS`を使いました。
