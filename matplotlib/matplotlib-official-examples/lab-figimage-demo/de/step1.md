# Importieren der erforderlichen Bibliotheken

Zunächst müssen wir die erforderlichen Bibliotheken importieren, nämlich `matplotlib.pyplot` und `numpy`. Wir werden `numpy` verwenden, um ein Array von Zufallswerten zu erstellen, das wir als unser Bild verwenden werden.

```python
import matplotlib.pyplot as plt
import numpy as np
```
