# 必要なライブラリをインポートする

このステップでは、この実験に必要なライブラリをインポートします。Matplotlibを使ってスライダーを作成し、NumPyを使って描画するデータを生成します。

```python
import matplotlib.pyplot as plt
import numpy as np

from matplotlib.widgets import Button, Slider
```
