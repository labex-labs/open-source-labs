# Генерировать данные

В этом шаге вы будете генерировать данные для построения графика. Вы создадите синусоидальную волну с частотой 3 Гц и амплитудой 5.

```python
t = np.arange(0.0, 1.0, 0.001)
a0 = 5
f0 = 3
s = a0 * np.sin(2 * np.pi * f0 * t)
```