# Rechter Titel

Fügen Sie dem Diagramm einen Titel hinzu, der rechtsbündig ausgerichtet ist, indem Sie die Funktion `title()` und den Parameter `loc` verwenden.

```python
plt.title('Right Title', loc='right')
```
