# Linker Titel

Fügen Sie dem Diagramm einen Titel hinzu, der linksbündig ausgerichtet ist, indem Sie die Funktion `title()` und den Parameter `loc` verwenden.

```python
plt.title('Left Title', loc='left')
```
