# Erstellen eines einfachen Diagramms

Zuerst importieren Sie die Matplotlib-Bibliothek und erstellen Sie ein einfaches Diagramm mit der Funktion `plot()`.

```python
import matplotlib.pyplot as plt
plt.plot(range(10))
```
