# Créer un graphique de base

Tout d'abord, importez la bibliothèque Matplotlib et créez un graphique de base en utilisant la fonction `plot()`.

```python
import matplotlib.pyplot as plt
plt.plot(range(10))
```
