# Importar as bibliotecas necessárias

Primeiramente, precisamos importar as bibliotecas necessárias. Neste laboratório, usaremos `matplotlib.pyplot` para plotagem, `mpl_toolkits.axes_grid1.parasite_axes.HostAxes` e `mpl_toolkits.axes_grid1.parasite_axes.ParasiteAxes` para criar eixos parasitas.

```python
import matplotlib.pyplot as plt
from mpl_toolkits.axisartist.parasite_axes import HostAxes
```
