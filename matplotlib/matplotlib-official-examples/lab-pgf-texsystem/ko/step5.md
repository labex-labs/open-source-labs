# 레이아웃 조정 및 플롯 저장

마지막으로, `fig.tight_layout()` 및 `fig.savefig()` 함수를 사용하여 플롯의 레이아웃을 조정하고 파일을 저장할 수 있습니다.

```python
fig.tight_layout(pad=.5)

fig.savefig("pgf_texsystem.pdf")
fig.savefig("pgf_texsystem.png")
```
