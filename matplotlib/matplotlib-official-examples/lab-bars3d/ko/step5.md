# 축 사용자 정의

이제 3D 플롯의 축을 사용자 정의합니다. `set_xlabel()`, `set_ylabel()`, 및 `set_zlabel()` 메서드를 사용하여 각각 x, y, z 축의 레이블을 설정합니다. 또한 `set_yticks()` 메서드를 사용하여 y 축의 눈금을 설정합니다.

```python
ax.set_xlabel('X')
ax.set_ylabel('Y')
ax.set_zlabel('Z')
ax.set_yticks(yticks)
```
