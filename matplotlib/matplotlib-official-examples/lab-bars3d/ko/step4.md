# 막대 그래프 사용자 정의

이제 막대 그래프를 사용자 정의합니다. 색상 배열을 생성하고 `bar()` 메서드를 사용하여 막대 그래프를 플롯합니다. `zdir` 매개변수를 'y'로 설정하여 막대 그래프를 y 축 평면에 투영합니다. 또한 `alpha` 매개변수를 0.8 로 설정하여 막대의 투명도를 조정합니다.

```python
    cs = [c] * len(xs)
    cs[0] = 'c'
    ax.bar(xs, ys, zs=k, zdir='y', color=cs, alpha=0.8)
```
