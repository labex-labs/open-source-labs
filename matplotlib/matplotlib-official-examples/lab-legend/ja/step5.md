# 凡例のスタイリング

最後に、凡例をより視覚的に魅力的にするためにスタイリングします。凡例の枠を取得するために`get_frame`関数を使用し、その後、枠の背景色を設定するために`set_facecolor`関数を使用します。

```python
# Put a nicer background color on the legend.
legend.get_frame().set_facecolor('C0')
```
