# データの生成

次に、グラフに使用するための疑似データを生成します。NumPyの`arange`関数を使って、2つの配列`a`と`b`を作成します。その後、`a`の指数関数を計算するために`exp`関数を使って、さらに2つの配列`c`と`d`を計算し、`d`を`c`の逆順にします。

```python
# Make some fake data.
a = b = np.arange(0, 3,.02)
c = np.exp(a)
d = c[::-1]
```
