# 라이브러리 가져오기

필요한 라이브러리를 가져오는 것으로 시작합니다. Matplotlib 과 `mpl_toolkits.axisartist.axislines` 라이브러리를 사용할 것입니다.

```python
import matplotlib.pyplot as plt
from mpl_toolkits.axisartist.axislines import AxesZero
```
