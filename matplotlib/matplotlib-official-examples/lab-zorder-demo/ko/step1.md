# Zorder 이해하기

Matplotlib 의 `zorder` 속성은 아티스트의 그리기 순서를 결정하는 부동 소수점 숫자입니다. `zorder`가 높은 아티스트는 `zorder`가 낮은 아티스트 위에 그려집니다. `zorder`의 기본값은 아티스트의 유형에 따라 다릅니다. 예를 들어, 이미지는 기본 `zorder`가 0 이고, 패치는 기본 `zorder`가 1 입니다.
