# ラベルの整列

ここでは、`align_xlabels` 関数と `align_ylabels` 関数を使って、ラベルを適切に整列させます。または、`align_labels` 関数を使って、両方を一度に行うこともできます。

```python
fig.align_labels()  # same as fig.align_xlabels(); fig.align_ylabels()
```
