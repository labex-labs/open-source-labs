# 레이블 정렬

이제 `align_xlabels` 및 `align_ylabels` 함수를 사용하여 레이블을 적절하게 정렬합니다. 또는 `align_labels` 함수를 사용하여 두 작업을 한 번에 수행할 수 있습니다.

```python
fig.align_labels()  # same as fig.align_xlabels(); fig.align_ylabels()
```
