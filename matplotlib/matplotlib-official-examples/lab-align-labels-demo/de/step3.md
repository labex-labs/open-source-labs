# Beschriftungen ausrichten

Wir werden jetzt die Funktionen `align_xlabels` und `align_ylabels` verwenden, um die Beschriftungen richtig auszurichten. Alternativ können wir die Funktion `align_labels` verwenden, um beides auf einmal zu tun.

```python
fig.align_labels()  # gleichbedeutend mit fig.align_xlabels(); fig.align_ylabels()
```
