# 라이브러리 임포트

Matplotlib 를 사용하여 베지어 곡선 (Bezier Curve) 을 만들기 위해 필요한 라이브러리를 임포트해야 합니다. 플롯을 생성하기 위해 `matplotlib.pyplot`을, `PathPatch` 객체를 생성하기 위해 `matplotlib.patches`를 임포트합니다.

```python
import matplotlib.pyplot as plt
import matplotlib.patches as mpatches
import matplotlib.path as mpath
```
