# Импорт необходимых библиотек

Перед тем как мы сможем начать добавлять текстовые поля к нашим графикам, нам нужно импортировать необходимые библиотеки. В этом примере мы будем использовать `matplotlib.pyplot` и `numpy`.

```python
import matplotlib.pyplot as plt
import numpy as np
```
