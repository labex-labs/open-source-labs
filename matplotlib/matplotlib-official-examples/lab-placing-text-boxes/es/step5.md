# Crear las propiedades del cuadro de texto

Crearemos un diccionario que contenga las propiedades para el cuadro de texto. En este ejemplo, utilizaremos un cuadro redondeado con un fondo de color trigo y un valor de opacidad (alpha) de 0.5.

```python
props = dict(boxstyle='round', facecolor='wheat', alpha=0.5)
```
