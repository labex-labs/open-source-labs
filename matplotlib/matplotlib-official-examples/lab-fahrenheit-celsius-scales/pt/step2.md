# Definir uma função para converter Fahrenheit para Celsius

Em seguida, definimos uma função para converter a temperatura em Fahrenheit para Celsius.

```python
def fahrenheit2celsius(temp):
    """
    Retorna a temperatura em Celsius dada a temperatura em Fahrenheit.
    """
    return (5. / 9.) * (temp - 32)
```
