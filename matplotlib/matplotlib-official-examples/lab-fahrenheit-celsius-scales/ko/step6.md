# 축 제한 및 레이블 설정

x 축 제한을 (0, 100) 으로 설정하고, y 축 레이블과 제목을 설정합니다.

```python
ax_f.set_xlim(0, 100)
ax_f.set_title('Two scales: Fahrenheit and Celsius')
ax_f.set_ylabel('Fahrenheit')
ax_c.set_ylabel('Celsius')
```
