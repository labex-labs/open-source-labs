# Personalizar o gráfico

Podemos personalizar o gráfico adicionando um título, rótulos para o eixo x e o eixo y, e uma grade. O código para isso é mostrado abaixo:

```python
plt.title("My Plot")
plt.xlabel("X-axis Label")
plt.ylabel("Y-axis Label")
plt.grid(True)
```
