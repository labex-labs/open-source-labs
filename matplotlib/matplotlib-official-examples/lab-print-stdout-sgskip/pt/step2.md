# Criar um conjunto de dados

Em seguida, precisamos criar um conjunto de dados para plotar. Neste exemplo, usaremos uma lista simples de números. O código para isso é mostrado abaixo:

```python
data = [1, 2, 3, 4, 5]
```
