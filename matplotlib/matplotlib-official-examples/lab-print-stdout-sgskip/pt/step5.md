# Salvar o gráfico

Finalmente, podemos salvar o gráfico em um arquivo usando a função `savefig()`. Neste exemplo, salvaremos o gráfico como um arquivo PNG. O código para isso é mostrado abaixo:

```python
plt.savefig("my_plot.png")
```
