# Criar um gráfico

Agora que temos nossos dados, podemos criar um gráfico. Neste exemplo, criaremos um gráfico de linha simples usando a função `plot()`. O código para isso é mostrado abaixo:

```python
plt.plot(data)
```
