# Speichern des Plots

Schließlich können wir den Plot mit der `savefig()`-Funktion in eine Datei speichern. In diesem Beispiel speichern wir den Plot als PNG-Datei. Der Code dazu ist unten dargestellt:

```python
plt.savefig("my_plot.png")
```
