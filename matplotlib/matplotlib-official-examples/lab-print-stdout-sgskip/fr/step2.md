# Création d'un ensemble de données

Ensuite, nous devons créer un ensemble de données à tracer. Dans cet exemple, nous utiliserons une simple liste de nombres. Le code correspondant est montré ci-dessous :

```python
data = [1, 2, 3, 4, 5]
```
