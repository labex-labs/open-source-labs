# Enregistrer le graphique

Enfin, nous pouvons enregistrer le graphique dans un fichier à l'aide de la fonction `savefig()`. Dans cet exemple, nous allons enregistrer le graphique au format PNG. Le code correspondant est montré ci-dessous :

```python
plt.savefig("my_plot.png")
```
