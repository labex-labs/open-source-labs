# Personalizar el gráfico

Podemos personalizar el gráfico agregando un título, etiquetas para el eje x y el eje y, y una cuadrícula. El código para esto se muestra a continuación:

```python
plt.title("My Plot")
plt.xlabel("X-axis Label")
plt.ylabel("Y-axis Label")
plt.grid(True)
```
