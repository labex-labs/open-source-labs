# Guardar el gráfico

Finalmente, podemos guardar el gráfico en un archivo usando la función `savefig()`. En este ejemplo, guardaremos el gráfico como un archivo PNG. El código para esto se muestra a continuación:

```python
plt.savefig("my_plot.png")
```
