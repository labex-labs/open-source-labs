# Crear un conjunto de datos

A continuación, necesitamos crear un conjunto de datos para graficar. En este ejemplo, usaremos una lista simple de números. El código para esto se muestra a continuación:

```python
data = [1, 2, 3, 4, 5]
```
