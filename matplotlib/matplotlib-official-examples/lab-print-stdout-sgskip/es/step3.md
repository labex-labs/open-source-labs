# Crear un gráfico

Ahora que tenemos nuestros datos, podemos crear un gráfico. En este ejemplo, crearemos un gráfico de líneas simple usando la función `plot()`. El código para esto se muestra a continuación:

```python
plt.plot(data)
```
