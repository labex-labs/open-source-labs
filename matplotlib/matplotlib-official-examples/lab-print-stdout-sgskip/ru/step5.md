# Сохраняем график

Наконец, мы можем сохранить график в файл с использованием функции `savefig()`. В этом примере мы сохраним график в файл в формате PNG. Код для этого показан ниже:

```python
plt.savefig("my_plot.png")
```
