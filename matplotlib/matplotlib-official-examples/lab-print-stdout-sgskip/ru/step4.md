# Настраиваем график

Мы можем настроить график, добавив заголовок, метки для оси x и оси y, а также сетку. Код для этого показан ниже:

```python
plt.title("My Plot")
plt.xlabel("X-axis Label")
plt.ylabel("Y-axis Label")
plt.grid(True)
```
