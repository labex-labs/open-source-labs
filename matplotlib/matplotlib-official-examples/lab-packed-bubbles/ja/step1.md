# 必要なライブラリをインポートする

パックドバブルチャートを作成するには、`matplotlib.pyplot` と `numpy` ライブラリをインポートする必要があります。`numpy` ライブラリは、配列に対して数学的演算を行うために使用され、バブルサイズの計算に便利です。

```python
import matplotlib.pyplot as plt
import numpy as np
```
