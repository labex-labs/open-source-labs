# Criar o Gráfico

Finalmente, criamos o gráfico usando os dados e o objeto eixos que acabamos de criar.

```python
ax.plot_surface(X, Y, Z)
ax.set_xlabel('X Label')
ax.set_ylabel('Y Label')
ax.set_zlabel('Z Label')
```
