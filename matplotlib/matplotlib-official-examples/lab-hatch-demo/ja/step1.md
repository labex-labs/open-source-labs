# ライブラリのインポート

始めるには、必要なライブラリをインポートする必要があります。この場合、Matplotlib と NumPy を使用します。NumPy は、Python プログラミング言語用のライブラリで、大きな多次元配列や行列に対するサポートとともに、これらの配列を操作するための多数の高度な数学関数が用意されています。

```python
import matplotlib.pyplot as plt
import numpy as np
```
