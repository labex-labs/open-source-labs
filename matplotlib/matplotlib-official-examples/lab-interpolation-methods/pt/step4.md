# Criar Subplots

Crie subplots para exibir as imagens usando métodos de interpolação.

```python
fig, axs = plt.subplots(nrows=3, ncols=6, figsize=(9, 6),
                        subplot_kw={'xticks': [], 'yticks': []})
```
