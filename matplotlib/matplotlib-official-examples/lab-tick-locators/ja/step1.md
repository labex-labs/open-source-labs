# ライブラリのインポート

最初のステップは、必要なライブラリをインポートすることです。ここでは、`matplotlib.pyplot` と `matplotlib.ticker` を使用します。

```python
import matplotlib.pyplot as plt
import matplotlib.ticker as ticker
import numpy as np
```
