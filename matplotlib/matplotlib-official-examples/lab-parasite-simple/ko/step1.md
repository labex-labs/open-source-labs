# 라이브러리 임포트

필요한 라이브러리를 임포트하는 것으로 시작합니다. 그래프를 생성하기 위해 `matplotlib.pyplot`을, 이중 축 (dual axes) 을 생성하기 위해 `mpl_toolkits.axes_grid1`을 사용합니다.

```python
import matplotlib.pyplot as plt
from mpl_toolkits.axes_grid1 import host_subplot
```
