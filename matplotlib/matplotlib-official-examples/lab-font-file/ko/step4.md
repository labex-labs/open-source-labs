# 제목 폰트 설정

`Axes` 클래스의 `set_title()` 메서드를 사용하여 플롯의 제목에 대한 폰트를 설정합니다. 폰트 경로를 `font` 매개변수로 전달하고, 폰트 파일의 이름을 플롯의 제목으로 사용합니다.

```python
ax.set_title(f'This is a special font: {fpath.name}', font=fpath)
```
