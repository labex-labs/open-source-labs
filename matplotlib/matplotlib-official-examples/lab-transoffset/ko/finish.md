# 요약

이 랩에서는 `transforms.offset_copy`를 사용하여 임의의 좌표계에서 주어진 위치를 기준으로 화면 좌표에서 지정된 오프셋에 그림 요소를 배치하는 방법을 배웠습니다. 이 함수를 사용하여 Python 의 `matplotlib.pyplot` 라이브러리를 사용하여 생성된 산점도 (scatter plot) 와 극좌표 플롯에 텍스트를 추가했습니다.
