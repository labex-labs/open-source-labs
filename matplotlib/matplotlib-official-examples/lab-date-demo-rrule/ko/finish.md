# 요약

이 랩에서는 Matplotlib 에서 반복 규칙 (recurrence rules) 을 사용하여 플롯에 사용자 정의 날짜 눈금을 배치하는 방법을 배웠습니다. 먼저 `rrulewrapper` 함수를 사용하여 반복 규칙을 설정한 다음, `RRuleLocator` 및 `DateFormatter` 함수를 사용하여 눈금 로케이터 (tick locator) 및 포맷터 (formatter) 를 설정했습니다. 마지막으로 데이터를 플롯하고 눈금 로케이터 및 포맷터 함수를 사용하여 x 축 눈금을 설정했습니다.
