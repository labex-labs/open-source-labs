# 필요한 라이브러리 가져오기

먼저, 필요한 라이브러리를 가져와야 합니다. `numpy`를 사용하여 임의의 데이터를 생성하고, `matplotlib.pyplot` 및 `matplotlib.patches`를 사용하여 타원을 그립니다.

```python
import matplotlib.pyplot as plt
import numpy as np
from matplotlib.patches import Ellipse
```
