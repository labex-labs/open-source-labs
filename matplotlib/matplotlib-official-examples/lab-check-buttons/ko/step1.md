# 라이브러리 임포트

필요한 라이브러리를 임포트하는 것으로 시작합니다. 데이터를 생성하기 위해 `numpy`가 필요하고, 플롯을 생성하기 위해 `matplotlib`가 필요합니다.

```python
import matplotlib.pyplot as plt
import numpy as np
from matplotlib.widgets import CheckButtons
```
