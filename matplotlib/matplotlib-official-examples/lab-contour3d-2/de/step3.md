# 3D Achsen erstellen

Wir werden die `add_subplot()`-Funktion verwenden, um ein 3D-Unterplot für unsere Darstellung zu erstellen. Wir werden auch die Projektion auf `'3d'` festlegen.

```python
ax = plt.figure().add_subplot(projection='3d')
```
