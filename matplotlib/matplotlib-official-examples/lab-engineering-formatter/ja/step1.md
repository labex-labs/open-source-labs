# 必要なライブラリをインポートする

最初のステップは、必要なライブラリをインポートすることです。この実験では、`Matplotlib`、`NumPy`、および `EngFormatter` を使用します。

```python
import matplotlib.pyplot as plt
import numpy as np
from matplotlib.ticker import EngFormatter
```
