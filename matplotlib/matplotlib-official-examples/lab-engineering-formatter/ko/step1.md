# 필요한 라이브러리 임포트

첫 번째 단계는 필요한 라이브러리를 임포트하는 것입니다. 이 랩에서는 `Matplotlib`, `NumPy`, 그리고 `EngFormatter`를 사용합니다.

```python
import matplotlib.pyplot as plt
import numpy as np
from matplotlib.ticker import EngFormatter
```
