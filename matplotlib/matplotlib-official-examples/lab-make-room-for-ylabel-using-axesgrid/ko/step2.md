# Y-Ticks 및 레이블 설정

이 단계에서는 y 축에 대한 y-ticks 와 레이블을 설정합니다. `set_yticks` 메서드를 사용하여 y-tick 의 위치를 설정하고, `labels` 매개변수를 사용하여 y-tick 에 대한 레이블을 설정합니다.

```python
ax.set_yticks([0.5], labels=["very long label"])
```
