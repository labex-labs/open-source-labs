# 필요한 라이브러리 가져오기

랩에서 사용할 라이브러리를 가져오는 것으로 시작합니다. 구체적으로, 시각화를 위해 `matplotlib.pyplot`, 수치 계산을 위해 `numpy`, 수학 함수를 위해 `math`를 사용할 것입니다.

```python
import math

import matplotlib.pyplot as plt
import numpy as np
```
