# Vorbereitung der Daten

Als nächstes werden wir einige Beispiel-Daten erstellen, um sie mit der Bibliothek `numpy` zu plotten. Wir werden einen linear gleichmäßig verteilten Array von 100 Werten zwischen 0 und 10 erstellen.

```python
x = np.linspace(0, 10, 100)
```
