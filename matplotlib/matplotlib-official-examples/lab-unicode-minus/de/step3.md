# Plotten der Daten

Jetzt werden wir die Daten mit der `plot`-Funktion von Matplotlib plotten. Wir werden eine Sinuswelle mit einer Frequenz von 1 und einer Amplitude von 1 plotten.

```python
y = np.sin(x)
plt.plot(x, y)
```
