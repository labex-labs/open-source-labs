# Erstellen des Diagramms

Als Nächstes erstellen wir das Diagramm, indem wir die Funktion `plot()` aufrufen und ihr einen Wertebereich übergeben.

```python
fig, ax = plt.subplots()
ax.plot(range(10))
```
