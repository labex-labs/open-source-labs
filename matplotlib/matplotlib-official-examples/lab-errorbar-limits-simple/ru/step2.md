# Создаем данные

В этом шаге мы создаем данные, которые будем использовать для создания графика с отрезками ошибок.

```python
x = np.arange(10)
y = 2.5 * np.sin(x / 20 * np.pi)
yerr = np.linspace(0.05, 0.2, 10)
```
