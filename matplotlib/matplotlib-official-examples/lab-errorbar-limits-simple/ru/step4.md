# Создаем график с отрезками ошибок только с верхними пределами

В этом шаге мы создаем график с отрезками ошибок только с верхними пределами.

```python
plt.errorbar(x, y + 2, yerr=yerr, uplims=True, label='uplims=True')
```
