# Создаем график с отрезками ошибок с обоими пределами (стандартный вариант)

В этом шаге мы создаем график с отрезками ошибок с обоими верхними и нижними пределами, что является стандартным поведением.

```python
plt.errorbar(x, y + 3, yerr=yerr, label='both limits (default)')
```
