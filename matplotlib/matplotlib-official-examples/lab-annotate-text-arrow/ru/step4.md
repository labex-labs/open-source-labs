# Задаем пределы графика и показываем график

Наконец, мы зададим пределы по осям x и y графика и покажем график с использованием функций `ax.set_xlim()`, `ax.set_ylim()` и `plt.show()`.

```python
ax.set_xlim(-4, 4)
ax.set_ylim(-4, 4)

plt.show()
```
