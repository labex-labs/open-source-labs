# Diagrammgrenzen festlegen und das Diagramm anzeigen

Schließlich legen wir die x- und y-Grenzen des Diagramms fest und zeigen das Diagramm mit den Funktionen `ax.set_xlim()`, `ax.set_ylim()` und `plt.show()` an.

```python
ax.set_xlim(-4, 4)
ax.set_ylim(-4, 4)

plt.show()
```
