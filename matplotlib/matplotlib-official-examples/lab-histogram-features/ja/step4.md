# 最適フィット線を追加する

このステップでは、ヒストグラムに最適フィット線を追加します。線のy値を計算し、ヒストグラムの上にプロットします。

```python
y = ((1 / (np.sqrt(2 * np.pi) * sigma)) *
     np.exp(-0.5 * (1 / sigma * (bins - mu))**2))
ax.plot(bins, y, '--')
```
