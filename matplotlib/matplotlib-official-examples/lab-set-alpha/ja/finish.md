# まとめ

この実験では、Matplotlib で色のアルファ値を設定する 2 つの方法を学びました。具体的には、`alpha` キーワード引数を使用する方法と、`(matplotlib_color, alpha)` という色の書式を使用する方法です。`alpha` キーワード引数を使用すると、グラフ内のすべての棒に同じアルファ値を設定できます。一方、`(matplotlib_color, alpha)` という色の書式を使用すると、各棒の y 値に基づいて異なるアルファ値を設定することができます。
