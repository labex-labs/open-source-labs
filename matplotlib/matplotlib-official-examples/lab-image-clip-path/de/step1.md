# Bibliotheken importieren

Um loszulegen, müssen wir die erforderlichen Bibliotheken importieren. Wir werden Matplotlib verwenden, um das Bild anzuzeigen und den Patch zu erstellen, und die `cbook`-Bibliothek, um das Beispielbild zu laden.

```python
import matplotlib.pyplot as plt
import matplotlib.cbook as cbook
import matplotlib.patches as patches
```
