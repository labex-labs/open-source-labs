# ライブラリのインポート

始めるには、必要なライブラリをインポートする必要があります。画像を表示してパッチを作成するためにMatplotlibを、サンプル画像を読み込むために`cbook`ライブラリを使用します。

```python
import matplotlib.pyplot as plt
import matplotlib.cbook as cbook
import matplotlib.patches as patches
```
