# まとめ

このチュートリアルでは、Python の Matplotlib を使用して破線軸プロット（broken axis plot）を作成する方法を学びました。まず、必要なライブラリをインポートし、外れ値を含むランダムなデータを作成しました。次に、2 つのサブプロットを作成し、両方のサブプロットにデータをプロットし、y 軸の範囲を設定しました。サブプロット間の軸の枠線（spine）を非表示にし、x 軸の目盛り（tick）を調整しました。最後に、切り抜き斜線を作成し、プロットを表示しました。
