# Ocultar las Fronteras (Spines)

Ahora ocultaremos las fronteras (spines) entre las dos subfiguras (subplots) utilizando `ax1.spines.bottom.set_visible` y `ax2.spines.top.set_visible`.

```python
ax1.spines.bottom.set_visible(False)
ax2.spines.top.set_visible(False)
```
