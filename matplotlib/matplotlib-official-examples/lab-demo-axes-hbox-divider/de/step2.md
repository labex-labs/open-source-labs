# Daten erstellen

Wir erstellen zwei numpy-Arrays, die als Daten für unsere Subplots verwendet werden sollen.

```python
arr1 = np.arange(20).reshape((4, 5))
arr2 = np.arange(20).reshape((5, 4))
```
