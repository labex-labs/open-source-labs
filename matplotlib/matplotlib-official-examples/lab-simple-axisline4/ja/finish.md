# まとめ

このチュートリアルでは、Matplotlib を使って 2 つの y 軸を持つ単純なグラフを作成する方法を学びました。グラフを作成するために`host_subplot`モジュールを使い、2 番目の y 軸を作成するために`mpl_toolkits.axes_grid1`モジュールを使いました。データは`numpy`を使って生成し、`matplotlib.pyplot`を使ってグラフを表示しました。
