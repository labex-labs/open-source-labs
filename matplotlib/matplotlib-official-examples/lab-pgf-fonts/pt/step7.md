# Salvar o gráfico

Finalmente, salvaremos o gráfico como um arquivo PDF e PNG usando a função `fig.savefig()`.

```python
fig.tight_layout(pad=.5)

fig.savefig("pgf_fonts.pdf")
fig.savefig("pgf_fonts.png")
```
