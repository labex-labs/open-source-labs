# Definir as fontes cursivas

Definiremos as fontes cursivas usando o parâmetro `font.cursive`. Neste exemplo, usaremos "Comic Neue" e "Comic Sans MS".

```python
plt.rcParams.update({
    "font.cursive": ["Comic Neue", "Comic Sans MS"],
})
```
