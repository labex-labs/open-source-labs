# Сохраняем график

Наконец, мы сохраним график в файлы PDF и PNG с использованием функции `fig.savefig()`.

```python
fig.tight_layout(pad=.5)

fig.savefig("pgf_fonts.pdf")
fig.savefig("pgf_fonts.png")
```
