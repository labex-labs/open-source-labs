# 필요한 라이브러리 가져오기

Matplotlib 를 사용하기 전에 필요한 라이브러리를 가져와야 합니다. 이 랩에서는 `matplotlib.pyplot`와 `matplotlib.widgets`를 사용할 것입니다.

```python
import matplotlib.pyplot as plt
from matplotlib.widgets import PolygonSelector
```
