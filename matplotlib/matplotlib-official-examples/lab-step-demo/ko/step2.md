# 플롯을 위한 데이터 생성

다음으로, 플롯에 사용할 데이터를 생성해 보겠습니다. `numpy.arange()` 함수를 사용하여 0 에서 14 까지의 값 배열을 생성하고 이를 변수 `x`에 저장합니다. 또한 `numpy.sin()` 함수를 사용하여 `x`의 각 값을 2 로 나눈 값의 사인 값을 갖는 값 배열을 생성하고 이를 변수 `y`에 저장합니다.

```python
x = np.arange(14)
y = np.sin(x / 2)
```
