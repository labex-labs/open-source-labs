# グラフ用のデータを作成する

次に、グラフに使うデータを作成しましょう。0 から 14 までの値の配列を作成し、変数`x`に格納するために`numpy.arange()`関数を使います。また、`x`の各値の 2 で割った値のサインの値の配列を作成し、変数`y`に格納するために`numpy.sin()`関数を使います。

```python
x = np.arange(14)
y = np.sin(x / 2)
```
