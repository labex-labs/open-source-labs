# 准备数据

在这一步中，我们将初始化标签和一个NumPy数组。我们要确保数组中有N个值对应的N个标签。

```python
# 初始化标签和一个NumPy数组，确保数组中有
# N个值对应的N个标签
xlabels = ['I', 'II', 'III', 'IV', 'V']
playerA = np.array([5, 15, 22, 20, 25])
playerB = np.array([25, 32, 34, 30, 27])
```
