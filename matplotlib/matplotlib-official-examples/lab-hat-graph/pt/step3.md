# Preparar Dados

Nesta etapa, inicializaremos rótulos e um array numpy. Certificaremos que temos N rótulos com N números de valores no array.

```python
# inicializar rótulos e um array numpy, certifique-se de ter
# N rótulos com N números de valores no array
xlabels = ['I', 'II', 'III', 'IV', 'V']
playerA = np.array([5, 15, 22, 20, 25])
playerB = np.array([25, 32, 34, 30, 27])
```
