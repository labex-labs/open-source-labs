# 사이드 체인 및 코너 링크 추가

이제 `side()` 및 `corner()` 함수를 사용하여 사이드 체인과 코너 링크를 추가합니다.

```python
side(sankey, n=links_per_side)
corner(sankey)
side(sankey, n=links_per_side)
corner(sankey)
side(sankey, n=links_per_side)
corner(sankey)
side(sankey, n=links_per_side)
```
