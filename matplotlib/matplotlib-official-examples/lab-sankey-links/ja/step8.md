# サイドチェーンとコーナーリンクを追加する

次に、`side()` と `corner()` 関数を使ってサイドチェーンとコーナーリンクを追加します。

```python
side(sankey, n=links_per_side)
corner(sankey)
side(sankey, n=links_per_side)
corner(sankey)
side(sankey, n=links_per_side)
corner(sankey)
side(sankey, n=links_per_side)
```
