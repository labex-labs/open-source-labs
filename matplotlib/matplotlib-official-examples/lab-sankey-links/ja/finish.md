# まとめ

この実験では、PythonのMatplotlibを使って長い接続チェーンを作成するために、`matplotlib.sankey`モジュールの`Sankey`クラスをどのように使用するかを学びました。それぞれサイドチェーンとコーナーリンクを生成するために、`side`と`corner`関数を定義しました。また、`Sankey`オブジェクトを使って最初の図、サイドチェーン、コーナーリンクを追加し、`plt.show()`を使って図を表示しました。
