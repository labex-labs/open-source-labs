# Add side chains and corner links

Now, we add the side chains and corner links using the `side()` and `corner()` functions.

```python
side(sankey, n=links_per_side)
corner(sankey)
side(sankey, n=links_per_side)
corner(sankey)
side(sankey, n=links_per_side)
corner(sankey)
side(sankey, n=links_per_side)
```
