# 2番目のプロットを作成する

次に、2番目のプロットを作成します。再び `subplot` を使用しますが、今回は `sharex` 属性を最初のプロット (`ax1`) に設定します。これにより、2番目のプロットが最初のプロットと同じx軸を共有するようになります。

```python
ax2 = plt.subplot(212, sharex=ax1)
ax2.plot(t, np.sin(4*np.pi*t))
```
