# ライブラリのインポート

始める前に、必要なライブラリをインポートする必要があります。この実験では、`matplotlib.pyplot` と `mpl_toolkits.axisartist` を使用します。

```python
import matplotlib.pyplot as plt
import mpl_toolkits.axisartist as axisartist
```
