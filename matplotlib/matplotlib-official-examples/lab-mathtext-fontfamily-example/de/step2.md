# Erstellen des Diagramms

Jetzt werden wir ein einfaches Diagramm für den Hintergrund mit der `plot()`-Funktion erstellen.

```python
fig, ax = plt.subplots(figsize=(6, 5))
ax.plot(range(11), color="0.9")
```
