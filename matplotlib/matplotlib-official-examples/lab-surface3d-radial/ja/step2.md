# メッシュを作成する

次に、極座標でメッシュを作成し、対応するZを計算します。半径値の配列 `r` と角度値の配列 `p` を作成し、その後NumPyの `meshgrid()` 関数を使って `R` と `P` の値のグリッドを作成します。最後に、`Z` の式を使って曲面の各点の高さを計算します。

```python
r = np.linspace(0, 1.25, 50)
p = np.linspace(0, 2*np.pi, 50)
R, P = np.meshgrid(r, p)
Z = ((R**2 - 1)**2)
```
