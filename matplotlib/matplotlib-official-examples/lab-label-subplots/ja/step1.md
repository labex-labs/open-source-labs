# ライブラリのインポート

最初のステップは、必要なライブラリをインポートすることです。サブプロットを作成および変換するために、`matplotlib.pyplot` と `matplotlib.transforms` を使用します。

```python
import matplotlib.pyplot as plt
import matplotlib.transforms as mtransforms
```
