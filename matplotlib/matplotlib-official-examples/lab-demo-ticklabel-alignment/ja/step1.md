# Matplotlib と AxisArtist のインポート

まず、Matplotlib と AxisArtist をインポートする必要があります。AxisArtist は、カスタム グリッドを作成するための追加ツールを提供します。

```python
import matplotlib.pyplot as plt
import mpl_toolkits.axisartist as axisartist
```
