# 필요한 라이브러리 가져오기

필요한 라이브러리인 Matplotlib 와 NumPy 를 가져오는 것으로 시작합니다. Matplotlib 는 데이터 시각화 라이브러리이고, NumPy 는 Python 에서 수치 계산을 위한 라이브러리입니다.

```python
import matplotlib.pyplot as plt
import numpy as np
```
