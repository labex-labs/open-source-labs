# 눈금 색상 설정

각 y-axis 의 눈금 색상을 레이블의 색상과 일치하도록 설정합니다.

```python
ax.tick_params(axis='y', colors=p1.get_color())
twin1.tick_params(axis='y', colors=p2.get_color())
twin2.tick_params(axis='y', colors=p3.get_color())
```
