# 레이블 및 제목 추가

`xlabel()`, `ylabel()`, 그리고 `title()` 함수를 사용하여 선 그래프에 레이블과 제목을 추가할 수 있습니다.

```python
plt.title('8 Random Lines - Line')
plt.xlabel('x label', fontsize=14)
plt.ylabel('y label', fontsize=14)
```
