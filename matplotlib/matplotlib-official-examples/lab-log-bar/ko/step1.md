# 라이브러리 임포트

먼저, 필요한 라이브러리를 임포트해야 합니다. 이 경우, `matplotlib.pyplot` 및 `numpy` 라이브러리를 사용합니다. `pyplot` 라이브러리는 막대 차트를 생성하는 데 사용되며, `numpy` 라이브러리는 데이터를 조작하는 데 도움이 됩니다.

```python
import matplotlib.pyplot as plt
import numpy as np
```
