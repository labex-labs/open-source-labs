# 데이터 정의

다음으로, 막대 차트에 사용할 데이터를 정의해야 합니다. 이 예제에서는 튜플의 튜플을 사용하며, 각 내부 튜플은 두 개의 값을 포함합니다. 첫 번째 값은 x 축 값을 나타내고, 두 번째 값은 y 축 값을 나타냅니다.

```python
data = ((3, 1000), (10, 3), (100, 30), (500, 800), (50, 1))
```
