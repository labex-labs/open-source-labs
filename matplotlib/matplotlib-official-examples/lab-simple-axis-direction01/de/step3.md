# Das Teilbild anpassen

Wir passen das Teilbild mit der Funktion `fig.subplots_adjust()` an, um Platz für die Beschriftungen auf der rechten Seite des Diagramms zu schaffen.

```python
fig.subplots_adjust(right=0.8)
```
