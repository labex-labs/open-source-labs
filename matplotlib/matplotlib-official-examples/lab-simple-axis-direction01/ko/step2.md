# Figure 및 Axes 생성

다음으로, `plt.subplots()` 함수를 사용하여 figure 와 axes 객체를 생성합니다. `figsize` 매개변수를 사용하여 figure 크기를 지정합니다.

```python
fig = plt.figure(figsize=(4, 2.5))
ax1 = fig.add_subplot(axes_class=axisartist.Axes)
```
