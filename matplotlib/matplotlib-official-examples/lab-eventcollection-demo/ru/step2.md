# Создаем случайные данные

Мы создадим случайные данные для двух кривых с использованием функции `numpy.random.random()`. Мы сгенерируем два набора по 10 случайных чисел в диапазоне от 0 до 1 и сохраним их в массиве.

```python
# create random data
xdata = np.random.random([2, 10])
```
