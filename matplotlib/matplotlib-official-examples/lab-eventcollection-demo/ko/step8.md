# 제한 설정 및 제목 추가

`matplotlib.pyplot.xlim()`, `matplotlib.pyplot.ylim()`, 및 `matplotlib.pyplot.title()` 함수를 사용하여 x 및 y 축의 제한을 설정하고 플롯에 제목을 추가합니다.

```python
# set the limits
ax.set_xlim([0, 1])
ax.set_ylim([0, 1])
ax.set_title('line plot with data points')
```
