# 플롯에 이벤트 추가

`matplotlib.pyplot.add_collection()` 함수를 사용하여 플롯에 이벤트를 추가합니다.

```python
# add the events to the axis
ax.add_collection(xevents1)
ax.add_collection(xevents2)
ax.add_collection(yevents1)
ax.add_collection(yevents2)
```
