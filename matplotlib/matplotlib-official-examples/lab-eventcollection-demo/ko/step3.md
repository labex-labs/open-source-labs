# 데이터 정렬

깔끔한 곡선을 만들기 위해 `sort()` 메서드를 사용하여 데이터를 정렬합니다.

```python
# split the data into two parts
xdata1 = xdata[0, :]
xdata2 = xdata[1, :]
# sort the data so it makes clean curves
xdata1.sort()
xdata2.sort()
```
