# 데이터 생성

0 과 3π 사이의 500 개의 균등하게 간격을 둔 값을 포함하는 numpy 배열 `x`를 생성합니다. 또한 `x`의 값의 사인 값을 포함하는 다른 numpy 배열 `y`를 생성합니다. 마지막으로, `y`의 첫 번째 미분값을 포함하는 numpy 배열 `dydx`를 생성합니다.

```python
x = np.linspace(0, 3 * np.pi, 500)
y = np.sin(x)
dydx = np.cos(0.5 * (x[:-1] + x[1:]))
```
