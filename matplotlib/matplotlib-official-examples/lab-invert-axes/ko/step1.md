# 필요한 라이브러리 가져오기

첫 번째 단계는 필요한 라이브러리를 가져오는 것입니다. 이 랩에서는 Matplotlib 와 NumPy 를 사용합니다. Matplotlib 는 Python 에서 널리 사용되는 플로팅 라이브러리이며, NumPy 는 Python 에서 과학적 계산을 위한 라이브러리입니다.

```python
import matplotlib.pyplot as plt
import numpy as np
```
