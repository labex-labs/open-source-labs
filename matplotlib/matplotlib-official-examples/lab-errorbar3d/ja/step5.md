# プロットをカスタマイズする

`set_xlabel`、`set_ylabel`、および`set_zlabel`メソッドを使って、x 軸、y 軸、および z 軸にラベルを追加することで、プロットをカスタマイズできます。

```python
ax.set_xlabel("X label")
ax.set_ylabel("Y label")
ax.set_zlabel("Z label")
```
