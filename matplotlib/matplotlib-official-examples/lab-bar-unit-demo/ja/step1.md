# 必要なライブラリをインポートする

最初のステップは、必要なライブラリをインポートすることです。バーチャートを作成するために、NumPyとMatplotlibを使用します。`cm`と`inch`の単位は、単位を変換するために使用される`basic_units.py`ファイルに定義されています。

```python
import matplotlib.pyplot as plt
import numpy as np
from basic_units import cm, inch
```
