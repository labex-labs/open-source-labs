# 차트에 레이블 및 제목 추가

마지막 단계는 차트에 레이블과 제목을 추가하는 것입니다. 차트 제목, x 축 레이블 및 차트 범례 (legend) 를 추가합니다.

```python
ax.set_title('Cup height by group and beverage choice')
ax.set_xlabel('Group')
ax.legend()
ax.autoscale_view()
```
