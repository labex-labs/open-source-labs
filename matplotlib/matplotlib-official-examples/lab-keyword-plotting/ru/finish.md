# Резюме

В этом практическом занятии вы узнали, как генерировать графики, используя строки, соответствующие переменным, с использованием формата данных `numpy.recarray` или `pandas.DataFrame` с аргументом ключевого слова `data` в Matplotlib. Вы также узнали, как интерпретировать точечный график, используя переменные `a`, `b`, `c` и `d`.
