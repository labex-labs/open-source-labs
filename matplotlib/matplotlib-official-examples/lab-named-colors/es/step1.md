# Instalando Matplotlib

Antes de poder usar Matplotlib, debemos instalarlo. Esto se puede hacer utilizando pip, que es un administrador de paquetes para Python. Abra el símbolo del sistema y ejecute el siguiente comando para instalar Matplotlib:

```python
pip install matplotlib
```
