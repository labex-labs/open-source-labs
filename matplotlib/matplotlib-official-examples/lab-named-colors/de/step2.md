# Matplotlib importieren

Sobald wir Matplotlib installiert haben, können wir es in unser Python-Programm importieren, indem wir den folgenden Befehl verwenden:

```python
import matplotlib.pyplot as plt
```
