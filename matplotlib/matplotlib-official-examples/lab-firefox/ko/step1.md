# 필요한 라이브러리 임포트

먼저 이 프로젝트에 필요한 모든 라이브러리를 임포트합니다. 여기에는 `re`, `matplotlib.pyplot`, `numpy`, `matplotlib.patches`, 그리고 `matplotlib.path`가 포함됩니다.

```python
import re
import matplotlib.pyplot as plt
import numpy as np
import matplotlib.patches as patches
from matplotlib.path import Path
```
