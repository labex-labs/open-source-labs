# Importar as Bibliotecas Necessárias

Primeiramente, importaremos todas as bibliotecas necessárias para este projeto, que incluem `re`, `matplotlib.pyplot`, `numpy`, `matplotlib.patches` e `matplotlib.path`.

```python
import re
import matplotlib.pyplot as plt
import numpy as np
import matplotlib.patches as patches
from matplotlib.path import Path
```
