# 제목 및 레이블 설정

이 단계에서는 axes 객체의 `set_title`, `set_xlabel`, 및 `set_ylabel` 메서드를 사용하여 플롯의 제목과 레이블을 설정합니다.

```python
ax.set_title("'fivethirtyeight' style sheet")
ax.set_xlabel("x")
ax.set_ylabel("y")
```
