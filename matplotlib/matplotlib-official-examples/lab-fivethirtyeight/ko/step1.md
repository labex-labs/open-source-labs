# Matplotlib 및 NumPy 라이브러리 가져오기

첫 번째 단계는 Matplotlib 및 NumPy 라이브러리를 가져오는 것입니다. NumPy 는 Python 에서 과학적 계산을 위한 기본 패키지로, 강력한 배열과 선형 대수 함수를 제공합니다.

```python
import matplotlib.pyplot as plt
import numpy as np
```
