# タイトルとラベルを設定する

このステップでは、軸オブジェクトの`set_title`、`set_xlabel`、および`set_ylabel`メソッドを使用して、プロットのタイトルとラベルを設定します。

```python
ax.set_title("'fivethirtyeight' style sheet")
ax.set_xlabel("x")
ax.set_ylabel("y")
```
