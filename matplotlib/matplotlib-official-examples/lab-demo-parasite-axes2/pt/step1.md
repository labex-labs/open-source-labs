# Importar Bibliotecas

Primeiramente, precisamos importar as bibliotecas necessárias. Usaremos `matplotlib.pyplot` para plotar e `mpl_toolkits` para criar eixos parasitas (parasite axis).

```python
import matplotlib.pyplot as plt
from mpl_toolkits import axisartist
from mpl_toolkits.axes_grid1 import host_subplot
```
