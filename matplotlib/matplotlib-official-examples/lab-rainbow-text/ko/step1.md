# 첫 번째 텍스트 객체 생성

첫 번째 단계는 `~.Axes.text`를 사용하여 첫 번째 텍스트 객체를 생성하는 것입니다. 이 텍스트 객체는 연결 프로세스의 시작점이 됩니다. 다음 코드는 플롯의 (0.1, 0.5) 위치에 "Matplotlib"라는 단어가 있는 빨간색 텍스트 객체를 생성합니다.

```python
text = ax.text(.1, .5, "Matplotlib", color="red")
```
