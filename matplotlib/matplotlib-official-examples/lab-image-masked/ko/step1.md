# 라이브러리 가져오기

첫 번째 단계는 필요한 라이브러리를 가져오는 것입니다. 이 튜토리얼에서는 NumPy 와 Matplotlib 을 사용합니다. NumPy 는 수치 계산을 위한 라이브러리이고 Matplotlib 은 데이터 시각화를 위한 라이브러리입니다.

```python
import numpy as np
import matplotlib.pyplot as plt
```
