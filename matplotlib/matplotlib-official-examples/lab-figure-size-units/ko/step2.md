# 인치 단위의 그림 크기 (기본값)

Matplotlib 에서 기본 그림 크기 단위는 인치입니다. subplots 함수의 figsize 매개변수를 사용하여 그림 크기를 지정할 수 있습니다. 아래 코드는 6 인치 x 2 인치 크기의 그림을 만드는 방법을 보여줍니다.

```python
plt.subplots(figsize=(6, 2))
plt.show()
```
