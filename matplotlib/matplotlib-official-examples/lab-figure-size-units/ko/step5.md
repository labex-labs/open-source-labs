# 빠른 대화형 작업

빠른 대화형 작업의 경우 픽셀이 일반적으로 적절한 크기 단위입니다. 픽셀 값을 인치로 변환하기 위해 기본 dpi 값인 100 을 사용할 수 있습니다. 그런 다음 이 값을 subplots 함수의 figsize 매개변수로 사용할 수 있습니다. 아래 코드는 픽셀 값을 사용하여 6 인치 x 2 인치 크기의 그림을 만드는 방법을 보여줍니다.

```python
plt.subplots(figsize=(600/100, 200/100))
plt.show()
```
