# Matplotlib importieren

Um Matplotlib zu verwenden, müssen wir es zuerst importieren. Wir importieren auch numpy, um einige Beispiel-Daten zur Visualisierung zu generieren.

```python
import matplotlib.pyplot as plt
import numpy as np
```
