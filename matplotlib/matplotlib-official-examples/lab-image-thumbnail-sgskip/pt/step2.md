# Importar Bibliotecas

Nesta etapa, você importará as bibliotecas que instalou na etapa anterior. Você precisa importar `ArgumentParser` e `Path` dos módulos `argparse` e `pathlib`, respectivamente. Além disso, importe os módulos `sys` e `image` dos módulos `sys` e `matplotlib.image`, respectivamente.

```python
from argparse import ArgumentParser
from pathlib import Path
import sys
import matplotlib.image as image
```
