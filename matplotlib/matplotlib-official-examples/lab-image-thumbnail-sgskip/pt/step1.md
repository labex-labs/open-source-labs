# Instalar as Bibliotecas Necessárias

Primeiramente, você precisa instalar as bibliotecas necessárias. Abra seu terminal e digite os seguintes comandos para instalar Matplotlib e Pillow:

```python
pip install matplotlib
pip install pillow
```
