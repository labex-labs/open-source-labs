# まとめ

この実験では、Matplotlib を使って Python で不規則な間隔のデータのコンター図を作成する方法を示しました。規則的なグリッド上で補間された不規則な間隔のデータのコンター図と、非構造化三角グリッドのトライコンター図を比較しました。コンター図を作成するために `axes.Axes.contour` と `axes.Axes.tricontour` メソッドを使用しました。
