# Mover as Marcas (Ticks) para Dentro dos Spines

Mova as marcas para dentro dos _spines_ para o segundo _subplot_ usando o método `major_ticks.set_tick_out()`.

```python
ax2.axis[:].major_ticks.set_tick_out(False)
```
