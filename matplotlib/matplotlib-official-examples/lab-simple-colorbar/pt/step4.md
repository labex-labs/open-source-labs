# Personalizar a barra de cores

Podemos personalizar a aparência da barra de cores usando o parâmetro `cmap`. Este parâmetro especifica o mapa de cores (color map) usado para criar o gráfico.

```python
# Customize the colorbar
plt.colorbar(cmap='viridis')
```
