# Adicionar um rótulo à barra de cores

Podemos adicionar um rótulo à barra de cores usando o parâmetro `label`.

```python
# Add a label to the colorbar
plt.colorbar(cmap='viridis', label='Random Data')
```
