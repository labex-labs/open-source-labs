# Erstellen einer 2D-Darstellung

Als nächstes werden wir mit der `imshow`-Funktion eine einfache 2D-Darstellung erstellen.

```python
# Create a 2D array
data = np.random.rand(10, 10)

# Plot the array
plt.imshow(data)
```
