# Fügen einer Bezeichnung zur Farbskala hinzu

Wir können der Farbskala eine Bezeichnung hinzufügen, indem wir den Parameter `label` verwenden.

```python
# Add a label to the colorbar
plt.colorbar(cmap='viridis', label='Zufällige Daten')
```
