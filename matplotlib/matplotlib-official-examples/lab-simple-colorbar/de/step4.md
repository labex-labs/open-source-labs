# Anpassen der Farbskala

Wir können das Aussehen der Farbskala anpassen, indem wir den Parameter `cmap` verwenden. Dieser Parameter gibt die Farbpalette an, die zur Erstellung der Darstellung verwendet wird.

```python
# Customize the colorbar
plt.colorbar(cmap='viridis')
```
