# Ajouter une barre de couleur

Maintenant, nous pouvons ajouter une barre de couleur au tracé en utilisant la fonction `colorbar`.

```python
# Ajouter une barre de couleur au tracé
plt.colorbar()
```
