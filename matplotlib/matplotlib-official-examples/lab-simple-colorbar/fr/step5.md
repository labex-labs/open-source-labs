# Ajouter une étiquette à la barre de couleur

Nous pouvons ajouter une étiquette à la barre de couleur en utilisant le paramètre `label`.

```python
# Ajouter une étiquette à la barre de couleur
plt.colorbar(cmap='viridis', label='Random Data')
```
