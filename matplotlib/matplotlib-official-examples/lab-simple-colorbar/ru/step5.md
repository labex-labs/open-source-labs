# Добавляем метку к цветовой шкале

Мы можем добавить метку к цветовой шкале, используя параметр `label`.

```python
# Add a label to the colorbar
plt.colorbar(cmap='viridis', label='Random Data')
```
