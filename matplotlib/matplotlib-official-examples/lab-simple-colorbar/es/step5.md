# Agregar una etiqueta a la barra de colores

Podemos agregar una etiqueta a la barra de colores usando el parámetro `label`.

```python
# Agrega una etiqueta a la barra de colores
plt.colorbar(cmap='viridis', label='Random Data')
```
