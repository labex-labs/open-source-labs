# Daten erstellen

Wir werden einige Daten erstellen, die wir für unseren Graphen verwenden. In diesem Beispiel werden wir eine Sinuswelle erstellen.

```python
t = np.arange(0.0, 2, 0.01)
s = np.sin(2*np.pi*t)
```
