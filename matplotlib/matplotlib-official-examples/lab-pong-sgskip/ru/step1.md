# Установка Matplotlib

Прежде чем мы начнем, необходимо убедиться, что Matplotlib установлен. Вы можете установить Matplotlib с использованием pip, выполнив следующую команду в терминале:

```python
pip install matplotlib
```
