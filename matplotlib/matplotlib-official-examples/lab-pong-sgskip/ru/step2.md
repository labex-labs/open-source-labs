# Импорт библиотек

Первое, что нам нужно сделать, — это импортировать необходимые библиотеки. Мы будем использовать `time`, `numpy` и `matplotlib`.

```python
import time
import matplotlib.pyplot as plt
import numpy as np
```
