# Importar Bibliotecas

A primeira coisa que precisamos fazer é importar as bibliotecas necessárias. Usaremos `time`, `numpy` e `matplotlib`.

```python
import time
import matplotlib.pyplot as plt
import numpy as np
```
