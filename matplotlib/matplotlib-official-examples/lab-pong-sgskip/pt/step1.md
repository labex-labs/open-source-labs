# Instalar Matplotlib

Antes de começarmos, precisamos garantir que o Matplotlib esteja instalado. Você pode instalar o Matplotlib usando pip, executando o seguinte comando no seu terminal:

```python
pip install matplotlib
```
