# Instalar Matplotlib

Antes de comenzar, debemos asegurarnos de que Matplotlib esté instalado. Puede instalar Matplotlib utilizando pip ejecutando el siguiente comando en su terminal:

```python
pip install matplotlib
```
