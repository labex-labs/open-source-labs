# 요약

이 랩에서는 Matplotlib 에서 축 레이블과 컬러바 레이블의 위치를 설정하는 방법을 배웠습니다. `set_xlabel`, `set_ylabel`, 그리고 `colorbar` 메서드를 사용하여 레이블의 위치를 설정했습니다. 또한 위치를 `'top'`, `'bottom'`, `'left'`, `'right'`, `'center'`, 또는 `'baseline'`으로 설정할 수 있다는 것을 배웠습니다.
