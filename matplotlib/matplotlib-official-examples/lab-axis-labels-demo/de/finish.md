# Zusammenfassung

In diesem Lab haben wir gelernt, wie man in Matplotlib die Position von Achsenbeschriftungen und Farbskalenbeschriftungen einstellt. Wir haben die `set_xlabel`-, `set_ylabel`- und `colorbar`-Methoden verwendet, um die Position der Beschriftungen zu setzen. Wir haben auch gelernt, dass wir die Position auf `'top'` (oben), `'bottom'` (unten), `'left'` (links), `'right'` (rechts), `'center'` (mittig) oder `'baseline'` (Baseline) setzen können.
