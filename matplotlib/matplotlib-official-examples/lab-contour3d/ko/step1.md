# 라이브러리 가져오기

3D 등고선 플롯을 생성하는 데 필요한 라이브러리를 가져오는 것으로 시작합니다. `matplotlib` 및 `mpl_toolkits` 라이브러리를 사용합니다.

```python
import matplotlib.pyplot as plt
from matplotlib import cm
from mpl_toolkits.mplot3d import axes3d
```
