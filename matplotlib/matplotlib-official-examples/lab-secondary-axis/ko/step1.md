# 필요한 라이브러리 가져오기

먼저 필요한 라이브러리인 `matplotlib`, `numpy`, 그리고 `datetime`을 가져오는 것으로 시작합니다.

```python
import datetime
import matplotlib.pyplot as plt
import numpy as np
import matplotlib.dates as mdates
from matplotlib.ticker import AutoMinorLocator
```
