# Erstellen von Teilabbildungen mit `subplots()`

Wir werden Teilabbildungen mit der Funktion `subplots()` erstellen. Wir werden zwei Teilabbildungen erstellen, eine über der anderen.

```python
# Create subplots
fig, (ax1, ax2) = plt.subplots(2, 1)
```
