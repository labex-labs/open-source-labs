# Bibliotheken importieren

Zunächst müssen wir die erforderlichen Bibliotheken importieren. Wir werden Matplotlib und NumPy verwenden. NumPy wird verwendet, um einige Beispiel-Daten zu generieren.

```python
import matplotlib.pyplot as plt
import numpy as np
```
