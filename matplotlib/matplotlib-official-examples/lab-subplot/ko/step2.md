# 샘플 데이터 생성 (Generate Some Sample Data)

그래프를 그리는 데 사용할 샘플 데이터를 생성하겠습니다.

```python
# Create some fake data.
x1 = np.linspace(0.0, 5.0)
y1 = np.cos(2 * np.pi * x1) * np.exp(-x1)
x2 = np.linspace(0.0, 2.0)
y2 = np.cos(2 * np.pi * x2)
```
