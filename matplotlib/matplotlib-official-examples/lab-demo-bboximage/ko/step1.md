# 필요한 라이브러리 임포트

이 튜토리얼에 필요한 라이브러리를 임포트하는 것으로 시작합니다. `matplotlib.pyplot`, `numpy`, `BboxImage`, `Bbox` 및 `TransformedBbox`가 필요합니다.

```python
import matplotlib.pyplot as plt
import numpy as np

from matplotlib.image import BboxImage
from matplotlib.transforms import Bbox, TransformedBbox
```
