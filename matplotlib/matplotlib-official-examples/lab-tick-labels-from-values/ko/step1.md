# 필요한 라이브러리 가져오기

이 튜토리얼에 필요한 라이브러리, 즉 `matplotlib.pyplot`와 `matplotlib.ticker`에서 `MaxNLocator`를 가져오는 것으로 시작합니다.

```python
import matplotlib.pyplot as plt
from matplotlib.ticker import MaxNLocator
```
