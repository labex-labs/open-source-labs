# 解説

コードを実行すると、異なる `CapStyle` オプションが表示されたグラフが表示されます。以下の `CapStyle` オプションが表示されます。

- `CapStyle.butt`
- `CapStyle.round`
- `CapStyle.projecting`

`butt` オプションはデフォルトのスタイルで、線分の終端まで単に直線を引きます。`round` オプションは線分の終端に半円を描画します。`projecting` オプションは線分の終端に半角の四角を描画します。
