# Bibliotheken importieren

Der erste Schritt besteht darin, die erforderlichen Bibliotheken zu importieren. Wir werden `Matplotlib` und `CapStyle` aus `Matplotlib._enums` verwenden.

```python
import matplotlib.pyplot as plt
from matplotlib._enums import CapStyle
```
