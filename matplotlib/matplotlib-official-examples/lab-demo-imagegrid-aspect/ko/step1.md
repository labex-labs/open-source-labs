# 필요한 라이브러리 가져오기

먼저, 필요한 라이브러리를 가져와야 합니다. 이 예제에서는 `matplotlib.pyplot` 및 `mpl_toolkits.axes_grid1.ImageGrid` 라이브러리가 필요합니다.

```python
import matplotlib.pyplot as plt
from mpl_toolkits.axes_grid1 import ImageGrid
```
