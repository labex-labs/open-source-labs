# 必要なライブラリをインポートする

まず、必要なライブラリをインポートする必要があります。この例では、`matplotlib.pyplot` と `mpl_toolkits.axes_grid1.ImageGrid` ライブラリが必要です。

```python
import matplotlib.pyplot as plt
from mpl_toolkits.axes_grid1 import ImageGrid
```
