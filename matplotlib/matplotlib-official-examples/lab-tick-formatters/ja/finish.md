# まとめ

この実験では、文字列を渡すか関数を `~.Axis.set_major_formatter` または `~.Axis.set_minor_formatter` に渡すことで、あるいはさまざまな `~.ticker.Formatter` クラスのインスタンスを作成して `~.Axis.set_major_formatter` または `~.Axis.set_minor_formatter` に渡すことで、Matplotlibにおける目盛りフォーマッタをどのように使用するかを学びました。また、目盛りの位置、目盛りの長さ、タイトルを設定してグラフを作成する方法も学びました。
