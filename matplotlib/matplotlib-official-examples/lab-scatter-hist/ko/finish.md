# 요약

이 랩에서는 Matplotlib 을 사용하여 히스토그램이 있는 산점도를 만드는 방법을 배웠습니다. `scatter_hist` 함수를 정의하고, 임의 데이터를 생성하고, `gridspec` 및 `inset_axes`를 사용하여 축 위치를 정의하고, 플롯을 표시했습니다. 히스토그램이 있는 산점도는 두 변수의 분포와 그 관계를 시각화하는 훌륭한 방법입니다.
