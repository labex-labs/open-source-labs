# Importieren der erforderlichen Bibliotheken

In diesem Schritt werden wir die erforderlichen Bibliotheken importieren, die `matplotlib.pyplot`, `numpy` und `LinearLocator` aus `matplotlib.ticker` umfassen.

```python
import matplotlib.pyplot as plt
import numpy as np
from matplotlib.ticker import LinearLocator
```
