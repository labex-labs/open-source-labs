# Crear figura y ejes

A continuación, crearemos un objeto de figura y ejes. Utilizaremos el método `add_subplot` para agregar el objeto de ejes.

```python
fig = plt.figure(figsize=(3, 3))
ax = fig.add_subplot(axes_class=Axes)
```
