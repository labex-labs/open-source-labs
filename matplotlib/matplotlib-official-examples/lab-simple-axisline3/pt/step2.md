# Criar Figura e Eixos

Em seguida, criaremos um objeto figura e eixos. Usaremos o método `add_subplot` para adicionar o objeto eixos.

```python
fig = plt.figure(figsize=(3, 3))
ax = fig.add_subplot(axes_class=Axes)
```
