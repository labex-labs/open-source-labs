# Figur und Achsen erstellen

Als nächstes werden wir ein Figuren- und Achsenobjekt erstellen. Wir werden die `add_subplot`-Methode verwenden, um das Achsenobjekt hinzuzufügen.

```python
fig = plt.figure(figsize=(3, 3))
ax = fig.add_subplot(axes_class=Axes)
```
