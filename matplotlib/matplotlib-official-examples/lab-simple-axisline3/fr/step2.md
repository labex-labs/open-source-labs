# Création de la figure et des axes

Ensuite, nous allons créer un objet figure et des axes. Nous utiliserons la méthode `add_subplot` pour ajouter l'objet axes.

```python
fig = plt.figure(figsize=(3, 3))
ax = fig.add_subplot(axes_class=Axes)
```
