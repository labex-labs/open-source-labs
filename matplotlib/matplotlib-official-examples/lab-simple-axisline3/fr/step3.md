# Personnaliser les axes

Nous allons maintenant personnaliser les axes en masquant les lignes d'axes droite et supérieure.

```python
ax.axis["right"].set_visible(False)
ax.axis["top"].set_visible(False)
```
