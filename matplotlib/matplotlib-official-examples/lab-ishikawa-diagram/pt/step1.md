# Instalar Matplotlib

Antes de começarmos, precisamos garantir que temos o Matplotlib instalado. Se você ainda não o fez, pode instalá-lo usando o seguinte comando:

```python
!pip install matplotlib
```
