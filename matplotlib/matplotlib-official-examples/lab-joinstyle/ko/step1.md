# 필요한 라이브러리 가져오기

`JoinStyle` 모듈을 사용하려면 `matplotlib._enums` 라이브러리에서 가져와야 합니다. 또한 그래프를 생성하기 위해 `matplotlib`에서 `pyplot` 모듈을 가져와야 합니다.

```python
import matplotlib.pyplot as plt
from matplotlib._enums import JoinStyle
```
