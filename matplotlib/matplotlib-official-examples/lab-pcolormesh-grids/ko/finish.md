# 요약

이 랩에서는 Matplotlib 에서 `pcolormesh` 및 `pcolor` 함수를 사용하여 2D 그리드를 시각화하는 방법을 배웠습니다. `flat`, `nearest`, `auto`, 그리고 `gouraud`를 포함한 다양한 쉐이딩 옵션에 대해 배웠습니다. 또한 NumPy 의 `meshgrid` 함수를 사용하여 2D 그리드를 생성하는 방법도 배웠습니다.
