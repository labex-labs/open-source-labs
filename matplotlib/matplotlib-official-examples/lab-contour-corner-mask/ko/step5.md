# 결과 해석

이 단계에서는 마스크된 등고선 플롯의 결과를 해석합니다. `corner_mask` 매개변수가 플롯의 모서리 점을 마스킹할지 여부를 제어하는 것을 관찰할 수 있습니다. `corner_mask`가 `True`로 설정되면 등고선 플롯의 모서리가 마스킹되고, `False`로 설정되면 마스킹되지 않습니다. 또한 마스크된 점은 빨간색 원으로 표시됩니다.
