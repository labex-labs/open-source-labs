# Создать данные

Далее нам нужно создать некоторые данные для построения графиков. Мы создадим две синусоидальные волны, которые будем отображать в отдельных графиках.

```python
t = np.arange(0.0, 2.0, 0.01)
s1 = np.sin(2*np.pi*t)
s2 = np.sin(4*np.pi*t)
```
