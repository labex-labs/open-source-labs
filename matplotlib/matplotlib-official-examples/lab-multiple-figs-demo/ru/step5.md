# Внести изменения в фигуру 1

Теперь мы вернемся к первой фигуре и внесем некоторые изменения. В верхнем подграфике мы построим вторую синусоидальную волну с использованием квадратных маркеров и удалим метки делений оси x из верхнего подграфика.

```python
plt.figure(1)

# Верхний подграфик
plt.subplot(211)
plt.plot(t, s2,'s')
ax = plt.gca()
ax.set_xticklabels([])
```
