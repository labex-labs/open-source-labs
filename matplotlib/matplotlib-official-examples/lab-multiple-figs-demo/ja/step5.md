# グラフ1を変更する

今度は、最初のグラフに戻り、いくつかの変更を加えます。上部のサブプロットに2番目のサイン波を四角形のマーカーを使って描画し、上部のサブプロットからx軸の目盛りラベルを削除します。

```python
plt.figure(1)

# 上部のサブプロット
plt.subplot(211)
plt.plot(t, s2, 's')
ax = plt.gca()
ax.set_xticklabels([])
```
