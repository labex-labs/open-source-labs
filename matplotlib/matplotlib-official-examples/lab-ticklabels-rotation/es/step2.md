# Crear datos para graficar

El segundo paso es crear los datos que quieres graficar. Para este laboratorio, usaremos los siguientes datos:

```python
x = [1, 2, 3, 4]
y = [1, 4, 9, 6]
labels = ['Frogs', 'Hogs', 'Bogs', 'Slogs']
```
