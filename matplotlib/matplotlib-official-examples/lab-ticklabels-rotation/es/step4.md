# Personalizar las etiquetas de los ejes

El cuarto paso es personalizar las etiquetas de los ejes. En este laboratorio, rotaremos las etiquetas de los ejes verticalmente. Puedes hacer esto ejecutando el siguiente código:

```python
plt.xticks(x, labels, rotation='vertical')
```
