# Настроить поля и расстояние между элементами

Пятым шагом является настройка полей и расстояния между элементами графика, чтобы убедиться, что метки делений не обрезаются. Вы можете это сделать, выполнив следующий код:

```python
plt.margins(0.2)
plt.subplots_adjust(bottom=0.15)
```
