# Création des données pour le tracé

La deuxième étape consiste à créer les données que vous souhaitez tracer. Pour ce laboratoire, nous allons utiliser les données suivantes :

```python
x = [1, 2, 3, 4]
y = [1, 4, 9, 6]
labels = ['Frogs', 'Hogs', 'Bogs', 'Slogs']
```
