# 필요한 라이브러리 가져오기

필요한 라이브러리인 `matplotlib.pyplot`와 `numpy`를 가져오는 것으로 시작합니다. 또한 삼각 메쉬를 생성하기 위해 `matplotlib.tri`를 가져옵니다.

```python
import matplotlib.pyplot as plt
import numpy as np
import matplotlib.tri as mtri
```
