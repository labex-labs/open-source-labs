# Resumo

Neste laboratório, aprendemos como criar e plotar grades triangulares não estruturadas em Python usando a biblioteca Matplotlib. Criamos uma triangulação de Delaunay e a plotamos usando a função `triplot`. Também criamos uma triangulação especificada pelo usuário e a plotamos usando a função `triplot`.
