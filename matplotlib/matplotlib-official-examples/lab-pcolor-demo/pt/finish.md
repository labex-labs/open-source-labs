# Resumo

Este tutorial mostrou como usar `Pcolor` em Matplotlib. Começamos com uma demonstração simples de `Pcolor`, depois comparamos `Pcolor` com funções semelhantes como `Pcolormesh` e `Imshow`. Finalmente, criamos um gráfico `Pcolor` com uma escala logarítmica.
