# Analizar el gráfico

El gráfico resultante mostrará los valores de `y` en función de los valores de `x`, con barras de error que indican la variabilidad en ambas direcciones. Las barras de error en `x` tendrán una longitud de 0,2 unidades, y las barras de error en `y` tendrán una longitud de 0,4 unidades.
