# 예제 데이터 생성

다음으로, 그래프에 사용할 예제 데이터를 생성합니다. 이 예제에서는 `numpy.arange()` 함수를 사용하여 0.1 과 4 사이의 값을 0.5 간격으로 배열을 생성합니다. 그런 다음 `numpy.exp()` 함수를 사용하여 배열의 각 값에 대한 지수 값을 계산합니다.

```python
# example data
x = np.arange(0.1, 4, 0.5)
y = np.exp(-x)
```
