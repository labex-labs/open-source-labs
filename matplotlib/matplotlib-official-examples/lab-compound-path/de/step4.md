# Erstellen des PathPatch

Wir werden ein `PathPatch` aus dem Pfad erstellen, den wir im vorherigen Schritt erstellt haben. Wir werden die `facecolor` auf `'none'` und die `edgecolor` auf `'green'` setzen.

```python
pathpatch = PathPatch(path, facecolor='none', edgecolor='green')
```
