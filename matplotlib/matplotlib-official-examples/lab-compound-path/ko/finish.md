# 요약

이 튜토리얼에서는 Python 의 Matplotlib 을 사용하여 복합 경로 (compound path) 를 만드는 방법을 배웠습니다. 사각형과 삼각형, 두 개의 간단한 다각형을 결합하여 복합 경로를 생성했습니다. 정점 (vertices) 과 코드 (codes) 로부터 경로를 생성하기 위해 `Path`를 사용했고, 경로로부터 패치 (patch) 를 생성하기 위해 `PathPatch`를 사용했습니다. 마지막으로, 복합 경로를 표시하기 위해 패치를 플롯에 추가했습니다.
