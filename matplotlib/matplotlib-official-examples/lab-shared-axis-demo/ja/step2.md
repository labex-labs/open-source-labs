# プロット用のデータを作成する

可視化するためのプロット用のデータを作成する必要があります。この例では、NumPy を使って 3 つの異なるデータセットを作成します。

```python
t = np.arange(0.01, 5.0, 0.01)
s1 = np.sin(2 * np.pi * t)
s2 = np.exp(-t)
s3 = np.sin(4 * np.pi * t)
```
