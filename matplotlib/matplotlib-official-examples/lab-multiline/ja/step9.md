# 2 番目のサブプロットに水平線とタイトルを追加する

2 番目のサブプロットに水平線を追加するには、`axhline`関数を使用できます。また、`set_title`関数を使用してサブプロットにタイトルを追加することもできます。

```python
ax1.axhline(0.4)
ax1.set_title("test line spacing for multiline text")
```
