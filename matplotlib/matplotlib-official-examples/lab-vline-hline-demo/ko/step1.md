# 라이브러리 임포트

첫 번째 단계는 필요한 라이브러리를 임포트하는 것입니다. 이 랩에서는 Matplotlib 및 NumPy 라이브러리를 사용합니다. Matplotlib 는 데이터 시각화 라이브러리이고 NumPy 는 Python 을 사용한 과학적 계산에 사용됩니다.

```python
import matplotlib.pyplot as plt
import numpy as np
```
