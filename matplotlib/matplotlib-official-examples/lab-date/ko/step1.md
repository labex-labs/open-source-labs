# 필요한 라이브러리 가져오기

`matplotlib.pyplot`, `matplotlib.cbook`, 그리고 `matplotlib.dates`를 포함한 필요한 라이브러리를 가져오는 것으로 시작합니다.

```python
import matplotlib.pyplot as plt
import matplotlib.cbook as cbook
import matplotlib.dates as mdates
```
