# Настройка графика

В этом шаге мы настроим график, добавив подписи осей и удалив линии осей.

```python
# Customize the plot
ax.set_axis_off()
ax.set_xlabel('X Label')
ax.set_ylabel('Y Label')
ax.set_title('Text Alignment in Matplotlib')
plt.show()
```
