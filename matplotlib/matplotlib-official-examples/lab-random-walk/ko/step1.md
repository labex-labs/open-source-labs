# 필요한 라이브러리 가져오기

필요한 라이브러리를 가져오는 것으로 시작합니다. 랜덤 숫자를 생성하기 위해 `numpy`를 사용하고, 플롯을 생성하기 위해 `matplotlib`를 사용합니다.

```python
import matplotlib.pyplot as plt
import numpy as np
import matplotlib.animation as animation
```
