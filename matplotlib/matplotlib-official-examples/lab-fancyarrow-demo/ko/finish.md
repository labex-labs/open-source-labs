# 요약

이 튜토리얼에서는 Python 의 Matplotlib 을 사용하여 화살표 스타일 참조 차트를 만드는 방법을 배웠습니다. `mpatches.ArrowStyle.get_styles()`를 사용하여 `~.Axes.annotate`에서 사용할 수 있는 모든 화살표 스타일을 가져오고, `plt.figure()` 및 `add_gridspec()`을 사용하여 figure 를 설정하고, 그리드의 각 셀에 각 화살표 스타일을 플롯하고, `plt.show()`를 사용하여 차트를 표시했습니다.
