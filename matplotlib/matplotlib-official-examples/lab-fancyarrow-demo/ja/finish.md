# まとめ

このチュートリアルでは、PythonのMatplotlibを使って矢印スタイルの参照チャートを作成する方法を学びました。`mpatches.ArrowStyle.get_styles()` を使って、`~.Axes.annotate` に利用可能なすべての矢印スタイルを取得し、`plt.figure()` と `add_gridspec()` を使ってグラフを設定し、グリッドの各セルに各矢印スタイルを描画し、`plt.show()` を使ってチャートを表示しました。
