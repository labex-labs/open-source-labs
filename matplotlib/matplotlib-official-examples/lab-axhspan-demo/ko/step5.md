# 수평선 추가

`axhline()` 함수를 사용하여 수평선을 추가합니다.

```python
# Thick red horizontal line at y=0 that spans the xrange.
ax.axhline(linewidth=8, color='#d62728')
# Horizontal line at y=1 that spans the xrange.
ax.axhline(y=1)
```
