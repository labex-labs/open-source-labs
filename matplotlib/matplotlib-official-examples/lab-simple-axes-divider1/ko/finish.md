# 요약

이 랩에서는 `mpl_toolkits.axes_grid1` 모듈을 사용하여 Matplotlib 으로 사용자 정의 축 그리드를 만드는 방법을 배웠습니다. 고정된 축 크기와 패딩을 사용하는 예제와 크기 조절 가능한 축 크기와 고정된 패딩을 사용하는 예제, 두 가지를 만들었습니다. `Divider` 클래스를 사용하여 축 사각형을 `horiz * vert`로 지정된 크기를 가진 그리드로 나누고, `add_axes()` 메서드와 `Divider` 클래스의 `new_locator()` 메서드를 사용하여 그림에 축을 추가했습니다.
