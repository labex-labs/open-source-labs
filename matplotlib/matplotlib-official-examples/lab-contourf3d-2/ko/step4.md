# 3D 표면 플롯

`plot_surface` 메서드를 사용하여 3D 표면을 플롯합니다. 또한 `edgecolor`, `linewidth`, `alpha`와 같은 몇 가지 매개변수를 설정합니다.

```python
ax.plot_surface(X, Y, Z, edgecolor='royalblue', lw=0.5, rstride=8, cstride=8, alpha=0.3)
```
