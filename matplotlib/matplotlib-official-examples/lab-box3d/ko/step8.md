# Colorbar 추가

`colorbar` 메서드를 사용하여 colorbar 를 추가합니다. colorbar 의 위치를 조정하기 위해 `fraction` 및 `pad` 매개변수를 설정하고, 데이터의 이름과 단위를 표시하도록 레이블을 설정합니다.

```python
# Colorbar
fig.colorbar(C, ax=ax, fraction=0.02, pad=0.1, label='Name [units]')
```
