# 라이브러리 가져오기

첫 번째 단계는 필요한 라이브러리를 가져오는 것입니다. 이 랩에서는 Matplotlib 와 NumPy 를 사용합니다. NumPy 는 Python 프로그래밍 언어를 위한 라이브러리로, 대규모 다차원 배열과 행렬을 지원하며, 이러한 배열에서 작동하는 다양한 고급 수학 함수를 제공합니다.

```python
import matplotlib.pyplot as plt
import numpy as np
```
