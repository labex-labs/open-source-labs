# Importieren der erforderlichen Bibliotheken

Zunächst müssen wir die Matplotlib-Bibliothek sowie alle anderen Bibliotheken importieren, die wir verwenden werden. In diesem Beispiel importieren wir auch die NumPy-Bibliothek, um einige Beispiel-Daten für unseren Linienplot zu generieren.

```python
import matplotlib.pyplot as plt
import numpy as np
```
