# Zeige das Diagramm an

Schließlich können wir das Diagramm anzeigen, indem wir die `show`-Funktion aufrufen. Wenn Sie ein Jupyter-Notebook verwenden, müssen Sie die `show`-Funktion nicht aufrufen. Das Diagramm wird automatisch angezeigt.

```python
plt.show()
```
