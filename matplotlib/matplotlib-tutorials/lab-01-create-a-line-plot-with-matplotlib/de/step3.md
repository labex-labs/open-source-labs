# Den Linienplot erstellen

Jetzt, wo wir unsere Beispiel-Daten haben, können wir den Linienplot mit der `plot`-Funktion aus der Matplotlib-Bibliothek erstellen. Wir werden die `x`- und `y`-Arrays als Argumente an die `plot`-Funktion übergeben.

```python
plt.plot(x, y)
```
