# Generar datos de muestra

A continuación, generaremos algunos datos de muestra para graficar. En este ejemplo, crearemos dos arrays, `x` e `y`, donde `x` representa las coordenadas x de los puntos de datos y `y` representa las coordenadas y.

```python
x = np.linspace(0, 10, 100)
y = np.sin(x)
```
