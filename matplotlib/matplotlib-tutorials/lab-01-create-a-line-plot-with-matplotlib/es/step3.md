# Crear el gráfico de líneas

Ahora que tenemos nuestros datos de muestra, podemos crear el gráfico de líneas utilizando la función `plot` de la biblioteca Matplotlib. Pasaremos los arrays `x` e `y` como argumentos a la función `plot`.

```python
plt.plot(x, y)
```
