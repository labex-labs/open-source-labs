# Mostrar el gráfico

Finalmente, podemos mostrar el gráfico llamando a la función `show`. Si estás usando un cuaderno de Jupyter, no es necesario llamar a la función `show`. El gráfico se mostrará automáticamente.

```python
plt.show()
```
