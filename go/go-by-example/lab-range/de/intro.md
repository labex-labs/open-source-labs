# Einführung

Das Schlüsselwort `range` wird verwendet, um über Elemente in verschiedenen Datenstrukturen in Golang zu iterieren. In diesem Lab werden wir untersuchen, wie `range` mit verschiedenen Datenstrukturen verwendet wird.
