# Résumé

Ce laboratoire porte sur l'implémentation d'une interface en Go. Nous avons implémenté l'interface `geometry` sur des `rect` et des `circle` et utilisé une fonction générique `measure` pour travailler sur n'importe quelle `geometry`.
