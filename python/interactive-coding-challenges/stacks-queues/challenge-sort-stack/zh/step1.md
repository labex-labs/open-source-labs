# 对栈进行排序

## 问题

给定一个栈，编写一个函数将其按升序排序。你可以使用另一个栈作为缓冲区。排序后的栈应将最大元素置于顶部。

## 要求

为了解决这个问题，我们需要考虑以下要求：

- 最大元素应位于排序后栈的顶部。
- 栈中可以有重复值。
- 我们可以假设已经有一个可用于此问题的栈类。
- 我们可以假设栈能装入内存。

## 示例用法

以下是该函数应有的一些行为示例：

- 空栈 -> 无
- 单元素栈 -> 相同元素
- 两个或更多元素的栈（一般情况） -> 按升序排序的栈
- 已排序的栈 -> 相同的栈
