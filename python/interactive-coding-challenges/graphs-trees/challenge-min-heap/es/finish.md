# Resumen

En resumen, un min heap es una estructura de datos de árbol binario que mantiene la propiedad de heap, donde el nodo padre siempre es menor o igual a sus hijos. La implementación de un min heap debe incluir los métodos `extract_min()` e `insert(value)`, y cumplir con los requisitos de entradas enteras y caber en memoria.
