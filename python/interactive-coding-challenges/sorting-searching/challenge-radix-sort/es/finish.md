# Resumen

El método de clasificación Radix es un algoritmo eficiente para ordenar enteros. Ordena los enteros comparando sus dígitos, comenzando por el dígito menos significativo hasta el más significativo. El algoritmo tiene una complejidad de tiempo lineal y a menudo se utiliza como subrutina en otros algoritmos de clasificación.
