# 概要

マージソートは、配列をソートするために分割統治法を使用する人気のあるソートアルゴリズムです。Python でマージソートを実装するには、アルゴリズムは分割統治法を使用し、任意の長さの配列を処理し、整数と文字列の両方に対して機能し、配列を昇順にソートし、O(n log n) の時間計算量を持ち、Python で実装する必要があります。入力が None の場合、アルゴリズムは例外を発生させる必要があり、入力が空の配列の場合、空の配列を返す必要があり、入力が 1 つの要素のみ含む場合、同じ配列を返す必要があり、左と右のサブ配列の長さが異なる場合、正しく配列をソートする必要があります。
