# Introducción

El merge sort es un algoritmo de clasificación popular que utiliza el enfoque divide y vencerás para ordenar una matriz. Es un algoritmo estable basado en comparaciones que tiene una complejidad temporal de O(n log n).
