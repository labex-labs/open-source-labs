# 两数之和

## 问题

给定一个整数数组，找出其中两个索引，使得这两个索引对应的元素之和等于一个特定值。函数应以列表形式返回这两个索引。

## 要求

为了解决两数之和问题，必须满足以下要求：

- 只有一个解决方案。
- 总是存在一个解决方案。
- 数组是整数数组。
- 数组未排序。
- 可能存在负值。
- 输入不一定有效。
- 解决方案必须能放入内存。

## 示例用法

两数之和问题可以通过以下示例解决：

- 无输入 -> 类型错误
- 空数组 -> 值错误
- [1, 3, 2, -7, 5]，7 -> [2, 4]
