# Resumen

En este desafío, implementamos una cola de prioridad respaldada por una matriz. La cola de prioridad soporta los métodos `insert`, `extract_min` y `decrease_key`. Nos aseguramos de que no hay claves duplicadas en la cola de prioridad y que no es necesario validar las entradas. La cola de prioridad cabe en memoria.
