# Introducción

En informática, las operaciones bit a bit se utilizan para manipular los bits individuales de un número binario. Un problema común es intercambiar los bits impares y pares de un número entero positivo. Esto se puede lograr con el menor número posible de operaciones utilizando operadores bit a bit.
