# Résumé

Dans ce défi, nous avons implémenté une méthode qui trace une ligne sur un écran en fonction de sa largeur et de deux positions absolues de pixels. L'implémentation a répondu aux exigences de gestion des entrées invalides, de définition des bits correspondants dans `screen` et de rentrée en mémoire.
