# 删除重复项

## 问题

给定一个非循环单链表，从中删除重复项。目标是就地修改原始链表并返回修改后链表的头节点。

## 要求

为了解决这个问题，我们需要考虑以下要求：

- 链表是非循环单链表。
- 列表中不能插入空值。
- 我们已经有一个可用于此问题的链表类。
- 需要实现两种解决方案：一种使用额外的数据结构，另一种不使用。
- 该问题在内存中可处理。

## 示例用法

以下是该函数应有的一些行为示例：

- 空链表 -> []
- 单元素链表 -> [元素]
- 无重复项的一般情况 -> [1, 2, 3, 4]
- 有重复项的一般情况 -> [1, 2, 3]
