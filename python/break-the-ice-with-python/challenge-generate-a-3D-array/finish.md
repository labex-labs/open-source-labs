# Summary

To generate a 3D array of size 3*5*8, we used list comprehension in our program. The output of the program is a 3D array where each element is 0. We can use this approach to generate arrays of different sizes and with different values.