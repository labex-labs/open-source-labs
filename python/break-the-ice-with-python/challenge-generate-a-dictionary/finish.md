# Summary

The program takes an integral number n as input and generates a dictionary containing (i, i x i) for all integral numbers i between 1 and n (inclusive). The resulting dictionary is then printed. The program uses the `dict()` function to create the dictionary. Input is assumed to be from the console.