# Summary

To concatenate two strings in Python, we can use the `+` operator. In this challenge, we have defined a function that takes two strings as input, concatenates them using the `+` operator, and then prints the result to the console. The function is quite simple and straightforward, and can be used to concatenate any two strings.
