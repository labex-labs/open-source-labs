# Summary

The program uses the given lists of subjects, verbs, and objects to generate all possible sentence combinations. It then outputs the sentences in a list format. The program uses the `list[index]` notation to access elements from the lists.
