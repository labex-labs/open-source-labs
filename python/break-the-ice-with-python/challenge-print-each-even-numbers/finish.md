# Summary

The program searches for even numbers between 1000 and 2200 (inclusive) whose digits are all even. Once found, the program prints these numbers in a comma-separated sequence on a single line. The program assumes that the input data is supplied through the console.
