# Summary

The program uses a for loop to iterate through the given tuple and checks if each element is even. If it is even, the element is added to a new list. Finally, the list is converted to a tuple using the `tuple()` function and printed as the output.
