# Summary

To generate a list of 5 random numbers between 100 and 200 inclusive, you can use the range() function to generate an iterable object and the random.sample() function to generate a list of random values. The program should output a list of 5 random numbers within the given range.