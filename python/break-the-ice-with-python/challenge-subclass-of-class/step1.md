# Subclass of Class

Define a class named `American` and its subclass `NewYorker`.

## Example

The following is one such subclass:

```bash
class NewYorker(American):
    pass
```

## Hints

- Use class `Subclass(ParentClass)` to define a subclass.
