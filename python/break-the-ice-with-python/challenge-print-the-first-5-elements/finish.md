# Summary

The program makes use of the ** operator to calculate the square of a number, and the range() function to generate a sequence of numbers. The list.append() method is used to add the square of each number to the list. Finally, the first 5 elements of the list are printed using list slicing.