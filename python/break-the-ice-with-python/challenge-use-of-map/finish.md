# Summary

The `map()` function is used to generate a list of squares of numbers between 1 and 20 (both included). The input to the program is `range(1, 21)` and the output should be a list of squares of the numbers in the input range. The `map()` function is used to generate this list.