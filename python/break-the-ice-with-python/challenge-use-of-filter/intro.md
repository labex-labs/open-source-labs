# Use of Filter

In programming, the `filter()` function is used to filter elements of a list based on a certain condition. This can be useful in many scenarios where you need to extract specific elements from a list. In this challenge, we will try to use the `filter()` function to create a list of even numbers between 1 and 20.
