# Summary

The program reads in a transaction log from console input, where each line represents a deposit or withdrawal transaction. The program then computes the net amount of the account by adding up all the deposits and subtracting all the withdrawals. The final balance is outputted to the console. The program assumes that the input data is supplied through the console.