# Summary

The program uses a generator to print even numbers between 0 and n in comma separated form. The value of n is taken as input from the console and the program produces the next even number using the `yield` keyword. The output is displayed in the console.