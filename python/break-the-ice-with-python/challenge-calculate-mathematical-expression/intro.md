# Calculate Mathematical Expression

This program is designed to calculate basic mathematical expressions entered through the console and print the result. It uses the `eval()` function to evaluate the expression and return the answer.
