# Summary

The program is able to take in basic mathematical expressions as input from the console and evaluate them using the `eval()` function. The result is then printed to the console. The program assumes that the input data is being supplied through the console.