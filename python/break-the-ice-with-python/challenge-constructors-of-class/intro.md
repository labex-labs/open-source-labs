# Constructors of Class

In this challenge, we tasked with defining a class named `Circle` that can be constructed by a radius. The `Circle` class also has a method named `area` which can compute the area.
