# Summary

The program sorts tuples containing name, age, and score in ascending order based on the criteria of name > age > score. The user inputs the tuples via console and the program outputs the sorted tuples. The program uses itemgetter to enable multiple sort keys.
