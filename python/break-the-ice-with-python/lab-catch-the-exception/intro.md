# Catching the Exception

In programming, errors and exceptions are common occurrences. One way to handle them is by using the `try/except` statement. In this lab, we will explore how to use `try/except` to catch exceptions when dividing by zero.
