# Summary

The program takes input from the user in the form of comma-separated numbers and generates a list and a tuple containing all the numbers. The input is assumed to be from the console. The program uses the `input.tuple()` method to convert the list to a tuple.
