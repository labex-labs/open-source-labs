# Generate a Even Number List

In this lab, we required to generate a list of 5 even numbers between 100 and 200 inclusive. We will use the range() function to generate an iterable object and the random.sample() function to generate a list of random even numbers.
