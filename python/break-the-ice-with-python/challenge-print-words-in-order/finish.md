# Summary

The program reads in a sequence of words separated by commas and sorts them alphabetically. The sorted words are then printed out in a comma-separated sequence. The input is assumed to be from the console.