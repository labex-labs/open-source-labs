# Einführung

In Python können Sie mit dem Modul `datetime` leicht das Datum von heute aus in `n` Tagen berechnen. In dieser Herausforderung werden Sie aufgefordert, eine Funktion zu schreiben, die eine ganze Zahl `n` als Eingabe erhält und das Datum von heute aus in `n` Tagen zurückgibt.
