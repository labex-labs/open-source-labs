# まとめ

このチャレンジでは、文字列をスネークケース（snake case）に変換するPython関数の書き方を学びました。`re.sub()` を使用して文字列内の単語をマッチングして置き換え、`str.lower()` を使用して小文字に変換し、`str.join()` を使用してアンダースコアを区切り文字として単語を結合しました。この知識を使えば、Pythonプログラムで簡単に文字列をスネークケースに変換することができます。
