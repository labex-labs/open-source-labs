# Resumo

Neste desafio, você aprendeu como criar uma função que verifica se uma função fornecida retorna `True` para cada elemento em uma lista. Você usou a função `all()` em combinação com `map()` e a função fornecida para resolver o problema.
