# 二项式系数

## 问题

编写一个名为 `binomial_coefficient(n, k)` 的函数，该函数接受两个整数 `n` 和 `k`，并返回 `n` 和 `k` 的二项式系数。你的函数应使用 `math.comb()` 方法来计算二项式系数。

## 示例

```python
binomial_coefficient(8, 2) # 28
binomial_coefficient(5, 3) # 10
binomial_coefficient(10, 5) # 252
```
