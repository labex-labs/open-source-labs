# 二項係数

## 問題

2つの整数 `n` と `k` を引数に取り、`n` と `k` の二項係数を返す `binomial_coefficient(n, k)` という関数を書きなさい。この関数は、二項係数を計算するために `math.comb()` メソッドを使用する必要があります。

## 例

```python
binomial_coefficient(8, 2) # 28
binomial_coefficient(5, 3) # 10
binomial_coefficient(10, 5) # 252
```
