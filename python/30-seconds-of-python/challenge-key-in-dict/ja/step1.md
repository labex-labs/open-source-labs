# 辞書にキーが存在するかどうかを確認する

## 問題

辞書 `d` とキー `key` を引数として受け取り、辞書にキーが存在する場合は `True` を返し、そうでない場合は `False` を返す関数 `key_in_dict(d, key)` を書きます。

## 例

```python
d = {'one': 1, 'three': 3, 'five': 5, 'two': 2, 'four': 4}
key_in_dict(d, 'three') # True
key_in_dict(d,'six') # False
```
