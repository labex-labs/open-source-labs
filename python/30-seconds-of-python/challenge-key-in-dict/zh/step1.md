# 检查字典中是否存在键

## 问题

编写一个函数 `key_in_dict(d, key)`，它接受一个字典 `d` 和一个键 `key` 作为参数，如果键存在于字典中则返回 `True`，否则返回 `False`。

## 示例

```python
d = {'one': 1, 'three': 3, 'five': 5, 'two': 2, 'four': 4}
key_in_dict(d, 'three') # 真
key_in_dict(d,'six') # 假
```
