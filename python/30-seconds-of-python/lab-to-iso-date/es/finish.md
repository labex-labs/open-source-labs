# Resumen

En este desafío, escribiste una función que convierte una fecha a su representación ISO-8601. Aprendiste cómo utilizar el método `datetime.datetime.isoformat()` para convertir un objeto `datetime.datetime` a una fecha ISO-8601.
