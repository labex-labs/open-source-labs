# Sommaire

Dans ce défi, vous avez écrit une fonction qui convertit une date en sa représentation ISO-8601. Vous avez appris à utiliser la méthode `datetime.datetime.isoformat()` pour convertir un objet `datetime.datetime` en une date au format ISO-8601.
