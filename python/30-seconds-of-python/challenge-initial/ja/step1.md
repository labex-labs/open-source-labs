# リストの先頭部分

## 問題

引数としてリストを受け取り、そのリストの最後の要素を除くすべての要素を返す、`initial(lst)` という Python 関数を書きなさい。

## 例

```python
initial([1, 2, 3]) # [1, 2]
initial(['apple', 'banana', 'orange', 'grape']) # ['apple', 'banana', 'orange']
```
