# 最大値のキー

## 問題

辞書 `d` を引数として受け取り、辞書内の最大値のキーを返す関数 `key_of_max(d)` を書きなさい。最大値と同じ値を持つ複数のキーがある場合は、それらのいずれかを返します。

この問題を解くには、`key` パラメータを `dict.get()` に設定した `max()` 関数を使用できます。これにより、辞書内の最大値のキーが返されます。

## 例

```python
key_of_max({'a':4, 'b':0, 'c':13}) # 'c'
```
