# Resumen

En este desafío, has aprendido cómo construir una lista utilizando una función iteradora y un valor semilla inicial. Has implementado la función `unfold` que toma una función iteradora y un valor semilla inicial como argumentos y devuelve una lista que es producida por el generador, utilizando la función iteradora.
