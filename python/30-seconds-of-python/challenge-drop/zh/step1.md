# 从左侧删除列表元素

## 问题

编写一个函数 `drop(a, n=1)`，它接受一个列表 `a` 和一个可选整数 `n` 作为参数，并返回一个新列表，其中从原始列表的左侧删除了 `n` 个元素。如果未提供 `n`，则函数应仅删除列表的第一个元素。

## 示例

```python
drop([1, 2, 3]) # [2, 3]
drop([1, 2, 3], 2) # [3]
drop([1, 2, 3], 42) # []
```
