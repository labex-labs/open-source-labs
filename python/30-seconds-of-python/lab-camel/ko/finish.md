# 요약

이 챌린지에서는 공백, 하이픈 또는 밑줄을 제거하고 첫 번째 단어를 제외한 각 단어의 첫 글자를 대문자로 변환하여 문자열을 camelcase 로 변환하는 방법을 배웠습니다. 정규 표현식 `r"(_|-)+"`를 사용하여 `re.sub()`를 통해 `-` 또는 `_`를 공백으로 바꾸고, `str.title()`을 사용하여 각 단어의 첫 글자를 대문자로 변환하고 나머지를 소문자로 변환했으며, `str.replace()`를 사용하여 단어 사이의 공백을 제거했습니다.
