# 요약

이 챌린지에서는 딕셔너리를 키를 기준으로 정렬하는 함수를 작성하는 과제를 수행했습니다. `sorted()` 함수를 사용하여 딕셔너리에서 튜플 쌍의 목록을 정렬하고, `dict()` 함수를 사용하여 다시 딕셔너리로 변환하는 방법을 배웠습니다. 또한 `sorted()`의 `reverse` 매개변수를 사용하여 딕셔너리를 역순으로 정렬하는 방법도 배웠습니다.
