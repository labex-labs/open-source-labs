# 요약

이 챌린지에서는 한 리스트의 모든 값이 다른 리스트에 포함되어 있는지 확인하는 방법을 배웠습니다. `for` 루프를 사용하여 `values` 리스트의 각 값을 반복하고, `in` 연산자를 사용하여 해당 값이 `lst` 리스트에 포함되어 있는지 확인했습니다. 모든 값이 포함되어 있다면 함수는 `True`를 반환했습니다. 어떤 값이라도 포함되어 있지 않다면 함수는 `False`를 반환했습니다.
