# Резюме

В этом испытании вы узнали, как преобразовать угол из градусов в радианы с использованием формулы `radians = (degrees * pi) / 180.0`. Вы также написали функцию, которая реализует эту формулу и возвращает угол в радианах, округленный до 4 знаков после запятой.
