# 数が偶数かどうかを確認する

引数として数値を受け取り、その数値が偶数の場合は `True` を返し、奇数の場合は `False` を返す関数 `is_even(num)` を作成します。数が偶数か奇数かを確認するには、剰余 (`%`) 演算子を使用できます。数が偶数の場合、2 で割ったときの余りはありません。数が奇数の場合、2 で割ったときの余りは 1 になります。

```python
def is_even(num):
  return num % 2 == 0
```

```python
is_even(3) # False
```
