# 检查一个数字是否为偶数

编写一个函数 `is_even(num)`，它接受一个数字作为参数，如果该数字是偶数则返回 `True`，如果是奇数则返回 `False`。要检查一个数字是偶数还是奇数，可以使用取模（`%`）运算符。如果一个数字是偶数，那么它除以 2 时将没有余数。如果一个数字是奇数，那么它除以 2 时将有余数 1。

```python
def is_even(num):
  return num % 2 == 0
```

```python
is_even(3) # False
```
