# 숫자를 16 진수로 변환하기

십진수를 인수로 받아 16 진수 표현을 반환하는 함수 `to_hex(dec)`를 작성하십시오. 함수는 다음 단계를 수행해야 합니다.

1. `hex()`를 사용하여 십진수를 16 진수 표현으로 변환합니다.
2. 16 진수 표현을 반환합니다.

```python
def to_hex(dec):
  return hex(dec)
```

```python
to_hex(41) # 0x29
to_hex(332) # 0x14c
```
