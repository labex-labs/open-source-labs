# Último Elemento da Lista

Escreva uma função chamada `last(lst)` que recebe uma lista como argumento e retorna o último elemento da lista.

```python
def last(lst):
  return lst[-1]
```

```python
last([1, 2, 3]) # 3
```
