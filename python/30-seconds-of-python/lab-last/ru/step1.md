# Последний элемент списка

Напишите функцию с именем `last(lst)`, которая принимает список в качестве аргумента и возвращает последний элемент этого списка.

```python
def last(lst):
  return lst[-1]
```

```python
last([1, 2, 3]) # 3
```
