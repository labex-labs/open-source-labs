# Resumen

En este desafío, aprendiste cómo convertir un número decimal a su equivalente hexadecimal utilizando la función `hex()` en Python. También escribiste una función que realiza esta conversión.
