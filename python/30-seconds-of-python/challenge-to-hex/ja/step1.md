# 10 進数を 16 進数に変換する

## 問題

10 進数を引数として受け取り、その 16 進数表記を返す関数 `to_hex(dec)` を書きなさい。あなたの関数は次の手順を実行する必要があります。

1. `hex()` を使って 10 進数を 16 進数に変換します。
2. 16 進数表記を返します。

## 例

```python
to_hex(41) # "0x29"
to_hex(332) # "0x14c"
```
