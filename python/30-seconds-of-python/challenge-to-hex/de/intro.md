# Einführung

In Python können wir eine Dezimalzahl einfach in ihre hexadezimale Entsprechung umwandeln, indem wir die `hex()`-Funktion verwenden. In dieser Herausforderung müssen Sie eine Funktion schreiben, die eine Dezimalzahl als Argument nimmt und ihre hexadezimale Darstellung zurückgibt.
