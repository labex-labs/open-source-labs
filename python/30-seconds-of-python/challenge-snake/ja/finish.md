# まとめ

このチャレンジでは、文字列をスネークケースに変換する Python 関数を書く方法を学びました。文字列内の単語をマッチさせて置き換えるために `re.sub()` を使用し、それらを小文字にするために `str.lower()` を使用し、アンダースコアを区切り文字として結合するために `str.join()` を使用しました。この知識を使えば、Python プログラムで文字列を簡単にスネークケースに変換できるようになりました。
