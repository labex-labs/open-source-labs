# 요약

이 챌린지에서 주어진 iterable 의 멱집합을 반환하는 Python 함수를 만드는 방법을 배웠습니다. 이 함수는 `range()`와 `itertools.combinations()`를 사용하여 모든 부분집합을 반환하는 제너레이터를 생성하고, `itertools.chain.from_iterable()`과 `list()`를 사용하여 제너레이터를 소비하고 리스트를 반환합니다.
