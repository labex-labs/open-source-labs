# リスト要素の削除

## 問題

`take(itr, n=1)` という関数を書きなさい。この関数は、リスト `itr` と整数 `n` を引数として受け取り、リストの先頭から `n` 個の要素を削除した新しいリストを返します。`n` がリストの長さより大きい場合、元のリストを返します。

## 例

```python
take([1, 2, 3], 1) # [2, 3]
take([1, 2, 3], 2) # [3]
take([1, 2, 3], 3) # []
take([1, 2, 3], 4) # []
```
