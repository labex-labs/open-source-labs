# 英里转公里

编写一个名为 `miles_to_km` 的Python函数，它接受一个参数 `miles`（浮点数或整数），并返回等效的公里数。你应该使用以下转换公式：`km = mi * 1.609344`。

```python
def miles_to_km(miles):
  return miles * 1.609344
```

```python
miles_to_km(5.03) # 8.09500032
```
