# Introducción

En Python, puedes obtener fácilmente un elemento aleatorio de una lista utilizando el módulo `random`. Este módulo proporciona una función llamada `choice()` que devuelve un elemento aleatorio de una lista dada. En este desafío, se te pedirá escribir una función que tome una lista como argumento y devuelva un elemento aleatorio de esa lista.
