# 요약

이 챌린지에서는 원하는 인덱스를 포함하는 다른 리스트를 기반으로 리스트를 정렬하는 함수를 만드는 과제를 수행했습니다. `zip()` 및 `sorted()` 함수를 사용하여 두 리스트를 결합하고 `indexes`의 값을 기반으로 정렬할 수 있었습니다. 그런 다음 리스트 컴프리헨션 (list comprehension) 을 사용하여 결과에서 각 쌍의 첫 번째 요소를 가져와 정렬된 리스트를 반환했습니다.
