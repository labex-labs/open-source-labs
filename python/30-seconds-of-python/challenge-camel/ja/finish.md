# まとめ

このチャレンジでは、空白、ハイフン、またはアンダースコアを削除し、最初の単語以外の各単語の最初の文字を大文字にすることで、文字列をキャメルケースに変換する方法を学びました。`re.sub()` を使用して、正規表現 `r"(_|-)+"` を使って `-` または `_` を空白に置き換え、`str.title()` を使って各単語の最初の文字を大文字にして残りを小文字に変換し、`str.replace()` を使って単語間の空白を削除しました。
