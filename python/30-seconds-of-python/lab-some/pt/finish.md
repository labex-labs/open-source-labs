# Resumo

Neste desafio, você aprendeu como usar a função `any()` em combinação com `map()` para verificar se uma função retorna `True` para pelo menos um elemento em uma lista. Você também criou uma função que recebe uma lista e uma função como argumentos, e retorna `True` se a função retornar `True` para pelo menos um elemento na lista.
