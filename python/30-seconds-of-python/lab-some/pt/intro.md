# Introdução

Em Python, podemos usar a função `any()` para verificar se pelo menos um elemento em uma lista é `True`. Neste desafio, você precisará criar uma função que recebe uma lista e uma função como argumentos, e retorna `True` se a função retornar `True` para pelo menos um elemento na lista.
