# Dernier élément d'une liste

## Problème

Écrivez une fonction appelée `last(lst)` qui prend une liste en argument et renvoie le dernier élément de la liste.

## Exemple

```python
last([1, 2, 3]) # 3
last(['apple', 'banana', 'cherry']) # 'cherry'
last([]) # None
```
