# Последний элемент списка

## Задача

Напишите функцию под названием `last(lst)`, которая принимает список в качестве аргумента и возвращает последний элемент списка.

## Пример

```python
last([1, 2, 3]) # 3
last(['apple', 'banana', 'cherry']) # 'cherry'
last([]) # None
```
