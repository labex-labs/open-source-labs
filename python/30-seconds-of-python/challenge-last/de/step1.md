# Letztes Liste-Element

## Problem

Schreiben Sie eine Funktion namens `last(lst)`, die eine Liste als Argument nimmt und das letzte Element der Liste zurückgibt.

## Beispiel

```python
last([1, 2, 3]) # 3
last(['apple', 'banana', 'cherry']) # 'cherry'
last([]) # None
```
