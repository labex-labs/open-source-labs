# Último elemento de una lista

## Problema

Escribe una función llamada `last(lst)` que tome una lista como argumento y devuelva el último elemento de la lista.

## Ejemplo

```python
last([1, 2, 3]) # 3
last(['apple', 'banana', 'cherry']) # 'cherry'
last([]) # None
```
