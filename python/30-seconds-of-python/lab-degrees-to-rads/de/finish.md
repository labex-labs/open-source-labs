# Zusammenfassung

In dieser Herausforderung haben Sie gelernt, wie man einen Winkel von Grad in Radiant umrechnet, indem man die Formel `radians = (degrees * pi) / 180.0` verwendet. Sie haben auch eine Funktion geschrieben, die diese Formel implementiert und den Winkel in Radiant auf 4 Nachkommastellen gerundet zurückgibt.
