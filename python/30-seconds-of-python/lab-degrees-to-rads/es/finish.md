# Resumen

En este desafío, has aprendido cómo convertir un ángulo de grados a radianes utilizando la fórmula `radians = (degrees * pi) / 180.0`. También has escrito una función que implementa esta fórmula y devuelve el ángulo en radianes redondeado a 4 decimales.
