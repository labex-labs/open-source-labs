# 逆順で各リスト要素に対して関数を実行する

## 問題

`for_each_right(itr, fn)` という関数を書きなさい。この関数は、リスト `itr` と関数 `fn` を引数とします。この関数は、`itr` の各要素に対して `fn` を最後の要素から始めて 1 回ずつ実行する必要があります。

## 例

```python
for_each_right([1, 2, 3], print) # 3 2 1
```

## 制約

- この関数は、任意のリストと関数に対して機能する必要があります。
- この関数は元のリストを変更してはなりません。
