# 각 단어 대문자화

문자열 `s`를 인수로 받아 각 단어의 첫 글자가 대문자로 된 새로운 문자열을 반환하는 함수 `capitalize_every_word(s)`를 작성하십시오.

```python
def capitalize_every_word(s):
  return s.title()
```

```python
capitalize_every_word('hello world!') # 'Hello World!'
```
