# Capitalizar Cada Palavra

Escreva uma função `capitalize_every_word(s)` que recebe uma string `s` como argumento e retorna uma nova string com a primeira letra de cada palavra capitalizada.

```python
def capitalize_every_word(s):
  return s.title()
```

```python
capitalize_every_word('hello world!') # 'Hello World!'
```
