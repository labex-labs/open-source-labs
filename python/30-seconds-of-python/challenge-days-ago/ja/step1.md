# 何日間前のチャレンジ

## 問題

あなたの課題は、整数 `n` を引数として受け取り、今日から `n` 日前の日付を返す `days_ago(n)` という関数を書くことです。

この問題を解くには、`datetime` モジュールの `date` クラスを使って現在の日付を取得し、`timedelta` クラスを使って現在の日付から `n` 日を引きます。

## 例

```python
days_ago(5) # date(2022, 11, 23)
```
