# 리스트 요소 제거

리스트 `itr`과 정수 `n`을 인수로 받아 리스트의 시작 부분에서 `n`개의 요소를 제거한 새 리스트를 반환하는 함수 `take(itr, n=1)`을 작성하십시오. `n`이 리스트의 길이보다 크면 원래 리스트를 반환합니다.

```python
def take(itr, n = 1):
  return itr[:n]
```

```python
take([1, 2, 3], 5) # [1, 2, 3]
take([1, 2, 3], 0) # []
```
