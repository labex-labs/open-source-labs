# 判断数字是否为质数

## 问题

编写一个名为 `is_prime(n)` 的 Python 函数，该函数接受一个整数 `n` 作为输入，如果该数字是质数，则返回 `True`，否则返回 `False`。要解决此问题，你需要遵循以下规则：

- 如果数字为 `0`、`1`、负数或 `2` 的倍数，则返回 `False`。
- 使用 `all()` 和 `range()` 检查从 `3` 到给定数字的平方根的数字。
- 如果没有数字能整除给定数字，则返回 `True`，否则返回 `False`。

## 示例

```python
is_prime(11) # True
```
