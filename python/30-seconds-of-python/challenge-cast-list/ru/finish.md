# Резюме

В этом испытании вы узнали, как создать функцию, которая преобразует значение в список, если оно не является уже списком или другим итерируемым объектом. Вы использовали `isinstance()` для проверки, является ли значение итерируемым, и возвращали его с использованием `list()` или помещали его в список соответственно.
