# リストにキャストするチャレンジ

## 問題

`cast_list(val)` という関数を書きましょう。この関数は、値を引数として受け取り、それをリストとして返します。値が既にリストである場合は、そのまま返します。値がリストではないが反復可能である場合は、リストとして返します。値が反復可能でない場合は、単一要素のリストとして返します。

## 例

```python
cast_list('foo') # ['foo']
cast_list([1]) # [1]
cast_list(('foo', 'bar')) # ['foo', 'bar']
```
