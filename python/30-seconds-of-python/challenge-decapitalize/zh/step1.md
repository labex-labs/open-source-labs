# 将字符串首字母大写转换为小写

## 问题

编写一个函数 `decapitalize(s, upper_rest = False)`，该函数接受一个字符串 `s`，并返回一个新字符串，其中首字母大写转换为小写。该函数还应有一个可选参数 `upper_rest`，当设置为 `True` 时，将把字符串的其余部分转换为大写。

## 示例

```python
decapitalize('FooBar') # 'fooBar'
decapitalize('FooBar', True) # 'fOOBAR'
```
