# Zusammenfassung

In dieser Herausforderung haben Sie gelernt, wie Sie die Elemente einer Liste basierend auf einer angegebenen Funktion gruppieren und die Anzahl der Elemente in jeder Gruppe zurückgeben. Sie haben auch gelernt, wie Sie `collections.defaultdict` und `map()` verwenden, um dieses Problem zu lösen.
