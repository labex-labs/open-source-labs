# リストの `n` 番目の要素すべて

## 問題

`every_nth(lst, nth)` という関数を書きましょう。この関数は、リスト `lst` と整数 `nth` を引数として受け取り、元のリストの `n` 番目の要素すべてを含む新しいリストを返します。

## 例

```python
every_nth([1, 2, 3, 4, 5, 6], 2) # [ 2, 4, 6 ]
```

## 制約

- 入力となるリスト `lst` には少なくとも `nth` 個の要素が含まれています。
- `nth` の値は 0 より大きいです。
