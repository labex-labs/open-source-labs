# Резюме

Чтобы разбить многострочную строку на список отдельных строк в Python, можно использовать метод `split()`, указав символ конца строки (`\n`) в качестве разделителя. Это разделит строку при каждом символе конца строки и вернет список полученных строк.
