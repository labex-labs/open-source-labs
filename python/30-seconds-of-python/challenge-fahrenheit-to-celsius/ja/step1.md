# 華氏から摂氏への変換

## 問題

華氏での温度をパラメータとして受け取り、摂氏での温度を返す関数 `fahrenheit_to_celsius(degrees)` を書きます。この関数は、変換式 `C = (F - 32) * 5 / 9` に従う必要があり、ここで `C` は摂氏での温度、`F` は華氏での温度です。

## 例

```python
fahrenheit_to_celsius(77) # 25.0
```
