# Fahrenheit in Celsius Umrechnung

## Problem

Schreiben Sie eine Funktion `fahrenheit_to_celsius(degrees)`, die eine Temperatur in Fahrenheit als Parameter nimmt und die Temperatur in Celsius zurückgibt. Die Funktion sollte die Umrechnungsformel `C = (F - 32) * 5 / 9` folgen, wobei `C` die Temperatur in Celsius und `F` die Temperatur in Fahrenheit ist.

## Beispiel

```python
fahrenheit_to_celsius(77) # 25.0
```
