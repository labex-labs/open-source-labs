# 出現回数を数えるチャレンジ

## 問題

`count_occurrences(lst, val)` という関数を書きなさい。この関数は、リスト `lst` と値 `val` を引数として受け取り、`lst` の中で `val` が何回出現するかを返します。関数は、出現回数を数えるために組み込みの `list.count()` メソッドを使用する必要があります。

## 例

```python
count_occurrences([1, 1, 2, 1, 2, 3], 1) # 3
```
