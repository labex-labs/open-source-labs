# Resumen

En este desafío, aprendiste cómo comprobar si un número es divisible por otro número usando el operador módulo. Creaste una función que toma dos enteros como argumentos y devuelve `True` si el `dividendo` es divisible por el `divisor`, y `False` en caso contrario.
