# Introducción

En Python, podemos usar el operador módulo (`%`) para comprobar si un número es divisible por otro número. Si el residuo es igual a `0`, entonces el número es divisible. En este desafío, crearás una función que compruebe si un número es divisible por otro número.
