# キロメートルからマイルへのチャレンジ

## 問題

`km_to_miles` というPython関数を書きましょう。この関数はパラメータ `km`（浮動小数点数）を受け取り、それに相当するマイル単位の距離（同じく浮動小数点数）を返します。変換式は `mi = km * 0.621371` です。

## 例

```python
km_to_miles(8.1) # 5.0331051
```
