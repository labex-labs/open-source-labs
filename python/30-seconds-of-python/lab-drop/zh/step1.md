# 从左侧删除列表元素

编写一个函数 `drop(a, n=1)`，它接受一个列表 `a` 和一个可选整数 `n` 作为参数，并返回一个新列表，该列表从原始列表的左侧删除了 `n` 个元素。如果未提供 `n`，则该函数应仅删除列表的第一个元素。

```python
def drop(a, n = 1):
  return a[n:]
```

```python
drop([1, 2, 3]) # [2, 3]
drop([1, 2, 3], 2) # [3]
drop([1, 2, 3], 42) # []
```
