# 화씨를 섭씨로 변환

화씨 온도를 매개변수로 받아 섭씨 온도를 반환하는 함수 `fahrenheit_to_celsius(degrees)`를 작성하십시오. 이 함수는 변환 공식 `C = (F - 32) * 5 / 9`를 따라야 합니다. 여기서 `C`는 섭씨 온도이고 `F`는 화씨 온도입니다.

```python
def fahrenheit_to_celsius(degrees):
  return ((degrees - 32) * 5 / 9)
```

```python
fahrenheit_to_celsius(77) # 25.0
```
