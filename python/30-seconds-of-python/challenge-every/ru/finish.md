# Резюме

В этом испытании вы узнали, как создать функцию, которая проверяет, возвращает ли предоставленная функция `True` для каждого элемента в списке. Вы использовали функцию `all()` в сочетании с `map()` и предоставленной функцией, чтобы решить задачу.
