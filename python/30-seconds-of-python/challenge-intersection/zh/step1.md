# 列表交集挑战

## 问题

编写一个函数 `list_intersection(a, b)`，该函数接受两个列表 `a` 和 `b` 作为输入，并返回一个新列表，其中只包含同时存在于 `a` 和 `b` 中的元素。如果没有共同元素，该函数应返回一个空列表。

## 示例

```python
list_intersection([1, 2, 3], [4, 3, 2]) # [2, 3]
list_intersection([1, 2, 3], [4, 5, 6]) # []
list_intersection([1, 2, 3], [3, 3, 2]) # [2, 3]
```
