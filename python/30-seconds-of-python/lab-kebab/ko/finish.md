# 요약

이 챌린지에서는 Python 에서 문자열을 케밥 케이스로 변환하는 방법을 배웠습니다. `re.sub()`를 사용하여 `-` 또는 `_`를 공백으로 바꾸고, `re.sub()`를 사용하여 문자열의 모든 단어를 일치시키고, `str.lower()`를 사용하여 소문자로 변환하고, `str.join()`을 사용하여 `-`를 구분 기호로 모든 단어를 결합했습니다.
