# 数が偶数かどうかを確認する

## 問題

引数として数値を受け取り、その数値が偶数の場合は `True` を返し、奇数の場合は `False` を返す関数 `is_even(num)` を作成します。数が偶数か奇数かを確認するには、剰余演算子 (`%`) を使用できます。数が偶数の場合、2 で割ったときの余りは 0 になります。数が奇数の場合、2 で割ったときの余りは 1 になります。

## 例

```python
is_even(4) # True
is_even(7) # False
is_even(0) # True
```
