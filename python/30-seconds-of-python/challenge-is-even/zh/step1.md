# 检查一个数字是否为偶数

## 问题

编写一个函数 `is_even(num)`，它接受一个数字作为参数，如果该数字是偶数则返回 `True`，如果是奇数则返回 `False`。要检查一个数字是偶数还是奇数，可以使用取模（`%`）运算符。如果一个数字是偶数，那么它除以 2 时没有余数。如果一个数字是奇数，那么它除以 2 时余数为 1。

## 示例

```python
is_even(4) # True
is_even(7) # False
is_even(0) # True
```
