# Km 에서 Miles 로 변환

`km_to_miles`라는 Python 함수를 작성하세요. 이 함수는 `km` (float) 매개변수를 받아서 마일 단위의 해당 거리 (역시 float) 를 반환합니다. 변환 공식은 `mi = km * 0.621371`입니다.

```python
def km_to_miles(km):
  return km * 0.621371
```

```python
km_to_miles(8.1) # 5.0331051
```
