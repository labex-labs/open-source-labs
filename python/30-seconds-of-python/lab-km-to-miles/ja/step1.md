# キロメートルからマイルへの変換

`km_to_miles` という名前のPython関数を書きましょう。この関数はパラメータ `km`（浮動小数点数）を受け取り、それに相当するマイル数（同じく浮動小数点数）を返します。変換式は `mi = km * 0.621371` です。

```python
def km_to_miles(km):
  return km * 0.621371
```

```python
km_to_miles(8.1) # 5.0331051
```
