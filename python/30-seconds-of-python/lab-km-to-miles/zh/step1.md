# 公里转换为英里

编写一个名为 `km_to_miles` 的 Python 函数，它接受一个参数 `km`（一个浮点数），并返回等效的英里数（也是一个浮点数）。转换公式为 `mi = km * 0.621371`。

```python
def km_to_miles(km):
  return km * 0.621371
```

```python
km_to_miles(8.1) # 5.0331051
```
