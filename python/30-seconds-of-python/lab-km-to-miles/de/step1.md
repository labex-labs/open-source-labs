# Kilometer in Meilen

Schreiben Sie eine Python-Funktion namens `km_to_miles`, die einen Parameter `km` (eine Gleitkommazahl) annimmt und die entsprechende Entfernung in Meilen (auch eine Gleitkommazahl) zurückgibt. Die Umrechnungsformel lautet `mi = km * 0.621371`.

```python
def km_to_miles(km):
  return km * 0.621371
```

```python
km_to_miles(8.1) # 5.0331051
```
