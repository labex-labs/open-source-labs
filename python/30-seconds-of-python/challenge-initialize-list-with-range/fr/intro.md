# Introduction

En Python, vous pouvez créer une liste de nombres à l'aide de la fonction `range()`. Cependant, parfois vous souhaiterez peut-être créer une liste de nombres qui ne sont pas séquentiels ou qui commencent à un nombre différent de 0. Dans de tels cas, vous pouvez utiliser la fonction `initialize_list_with_range()`.
