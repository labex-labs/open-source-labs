# 根据函数查找列表中的最大值

## 问题

编写一个函数 `max_by(lst, fn)`，该函数接受一个列表 `lst` 和一个函数 `fn` 作为参数。该函数应使用提供的函数 `fn` 将 `lst` 中的每个元素映射到一个值，然后返回最大值。

## 示例

```python
max_by([{ 'n': 4 }, { 'n': 2 }, { 'n': 8 }, { 'n': 6 }], lambda v : v['n']) # 8
```

## 约束条件

- 列表 `lst` 至少包含一个元素。
- 函数 `fn` 将接受一个参数并返回一个值。
