# 10 進数をバイナリに変換する

## 問題

10 進数を入力として受け取り、そのバイナリ表現を文字列として返す関数`to_binary(n)`を書きなさい。あなたの関数は以下の手順を実行する必要があります。

1. `bin()`を使って 10 進数をそのバイナリに変換します。
2. バイナリ表現を文字列として返します。

## 例

```python
to_binary(100) # '0b1100100'
to_binary(42) # '0b101010'
to_binary(255) # '0b11111111'
```
