# 十进制数转二进制数

## 问题

编写一个函数 `to_binary(n)`，该函数接受一个十进制数作为输入，并返回其二进制表示形式的字符串。你的函数应执行以下步骤：

1. 使用 `bin()` 将十进制数转换为等效的二进制数。
2. 将二进制表示形式作为字符串返回。

## 示例

```python
to_binary(100) # '0b1100100'
to_binary(42) # '0b101010'
to_binary(255) # '0b11111111'
```
