# はじめに

Python では、`set()` と `intersection()` メソッドを使って 2 つのリストの共通部分を見つけることができます。しかし、両方のリストの各要素に適用される特定の関数に基づいて共通部分を見つけたい場合はどうでしょうか。このチャレンジでは、2 つのリストと関数を受け取り、両方のリストの各要素に提供された関数を適用した後、両方のリストに存在する要素のリストを返す関数を作成します。
