# Zusammenfassung

In dieser Herausforderung hast du gelernt, wie du den kleinsten Wert einer Liste basierend auf einer bestimmten Eigenschaft oder einem Attribut jedes Elements in der Liste mithilfe der `min_by()`-Funktion findest. Diese Funktion bildet jedes Element in der Liste zu einem Wert ab, indem eine bereitgestellte Funktion verwendet wird, und gibt dann den kleinsten Wert zurück.
