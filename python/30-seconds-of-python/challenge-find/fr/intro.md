# Introduction

En Python, on peut utiliser une compréhension de liste et `next()` pour trouver la valeur du premier élément d'une liste donnée qui satisfait une fonction de test fournie. Cela peut être utile dans de nombreux scénarios, comme trouver le premier nombre impair dans une liste ou la première chaîne de caractères qui commence par une certaine lettre.
