# Список без последнего элемента

Напишите функцию на Python с именем `initial(lst)`, которая принимает список в качестве аргумента и возвращает все элементы списка, кроме последнего.

```python
def initial(lst):
  return lst[:-1]
```

```python
initial([1, 2, 3]) # [1, 2]
```
