# Inicial da Lista (List Initial)

Escreva uma função Python chamada `initial(lst)` que recebe uma lista como argumento e retorna todos os elementos da lista, exceto o último.

```python
def initial(lst):
  return lst[:-1]
```

```python
initial([1, 2, 3]) # [1, 2]
```
