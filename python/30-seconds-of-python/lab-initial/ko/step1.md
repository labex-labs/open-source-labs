# 리스트 초기화 (List Initial)

`initial(lst)`라는 Python 함수를 작성하세요. 이 함수는 리스트를 인수로 받아 마지막 요소를 제외한 리스트의 모든 요소를 반환합니다.

```python
def initial(lst):
  return lst[:-1]
```

```python
initial([1, 2, 3]) # [1, 2]
```
