# コンパクトなリスト

## 問題

`compact(lst)` という関数を書きましょう。この関数は、引数としてリストを受け取り、すべての偽の値が削除された新しいリストを返します。偽の値には、`False`、`None`、`0`、および `""` が含まれます。

この問題を解くには、`filter()` 関数を使って、リストから偽の値をフィルタリングすることができます。

## 例

```python
compact([0, 1, False, 2, '', 3, 'a', 's', 34]) # [ 1, 2, 3, 'a', 's', 34 ]
```
