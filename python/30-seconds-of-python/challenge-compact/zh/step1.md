# 精简列表

## 问题

编写一个函数 `compact(lst)`，该函数接受一个列表作为参数，并返回一个移除了所有虚假值的新列表。虚假值包括 `False`、`None`、`0` 和 `""`。

要解决这个问题，你可以使用 `filter()` 函数从列表中过滤掉虚假值。

## 示例

```python
compact([0, 1, False, 2, '', 3, 'a', 's', 34]) # [ 1, 2, 3, 'a', 's', 34 ]
```
