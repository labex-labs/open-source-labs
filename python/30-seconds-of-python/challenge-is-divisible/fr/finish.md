# Sommaire

Dans ce défi, vous avez appris à vérifier si un nombre est divisible par un autre nombre en utilisant l'opérateur modulo. Vous avez créé une fonction qui prend deux entiers en arguments et renvoie `True` si le `dividende` est divisible par le `diviseur`, et `False` sinon.
