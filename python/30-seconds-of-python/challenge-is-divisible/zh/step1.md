# 数字可整除性

## 问题

编写一个函数 `is_divisible(dividend, divisor)`，该函数接受两个整数作为参数，如果 `dividend` 能被 `divisor` 整除，则返回 `True`，否则返回 `False`。

## 示例

```python
is_divisible(6, 3) # True
is_divisible(7, 2) # False
is_divisible(10, 5) # True
```
