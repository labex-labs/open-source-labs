# Zusammenfassung

In dieser Herausforderung haben Sie gelernt, wie Sie in Python eine 2D-Liste initialisieren. Sie haben eine List Comprehension und `range()` verwendet, um `h` Zeilen zu generieren, wobei jede eine Liste mit der Länge `w` ist und mit einem Standardwert initialisiert ist. Sie haben auch gelernt, wie Sie den Standardwert auf `None` setzen, wenn kein Wert angegeben ist.
