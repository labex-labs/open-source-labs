# 十进制数转换为二进制数

编写一个函数 `to_binary(n)`，它接受一个十进制数作为输入，并返回其作为字符串的二进制表示形式。你的函数应执行以下步骤：

1. 使用 `bin()` 将十进制数转换为其等效的二进制数。
2. 将二进制表示形式作为字符串返回。

```python
def to_binary(n):
  return bin(n)
```

```python
to_binary(100) # 0b1100100
```
