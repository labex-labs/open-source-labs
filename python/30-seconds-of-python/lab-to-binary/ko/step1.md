# 숫자를 이진법으로 변환하기

십진수를 입력으로 받아 이진 표현을 문자열로 반환하는 함수 `to_binary(n)`을 작성하십시오. 함수는 다음 단계를 수행해야 합니다.

1. `bin()`을 사용하여 십진수를 이진법으로 변환합니다.
2. 이진 표현을 문자열로 반환합니다.

```python
def to_binary(n):
  return bin(n)
```

```python
to_binary(100) # 0b1100100
```
