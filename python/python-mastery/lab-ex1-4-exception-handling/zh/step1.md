# 定义一个函数

以你在上一个实验中编写的程序`pcost.py`为例，将其转换为一个名为`portfolio_cost(filename)`的函数，该函数以文件名作为输入，读取该文件中的投资组合数据，并以浮点数形式返回投资组合的总成本。编写好函数后，在程序末尾添加以下语句来调用该函数：

```python
print(portfolio_cost('/home/labex/project/portfolio.dat'))
```

运行你的程序，确保它产生与之前相同的输出。
