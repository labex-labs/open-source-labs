#!/bin/zsh

cat ~/.python_history | grep "add"
cat ~/.python_history | grep "len"
cat ~/.python_history | grep "most_common"
cat ~/.python_history | grep "defaultdict"
cat ~/.python_history | grep "split"
cat ~/.python_history | grep "get_traced_memory"
