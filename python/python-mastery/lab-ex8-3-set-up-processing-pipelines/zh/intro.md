# 简介

**目标**：

- 使用协程来设置处理管道

**创建的文件**：`cofollow.py`，`coticker.py`

**注意**：
对于本练习，`stocksim.py` 程序应仍在后台运行。

在练习8.2中，你编写了一些使用生成器来设置处理管道的代码。该程序的一个关键方面是生成器函数之间数据流的概念。使用协程可以设置非常类似的数据流。唯一的区别是，对于协程，你将数据发送到不同的处理元素，而不是使用for循环提取数据。
