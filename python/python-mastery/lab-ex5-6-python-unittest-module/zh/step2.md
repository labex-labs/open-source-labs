# 单元测试

以`teststock.py`中的代码为指导，扩展`TestStock`类，进行以下测试：

- 测试你是否可以使用关键字参数（如`Stock(name='GOOG',shares=100,price=490.1)`）创建一个`Stock`。
- 测试`cost`属性是否返回正确的值。
- 测试`sell()`方法是否正确更新股份数量。
- 测试`from_row()`类方法是否能从有效的数据创建一个新实例。
- 测试`__repr__()`方法是否创建一个合适的表示字符串。
- 测试比较运算符方法`__eq__()`
