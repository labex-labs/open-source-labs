# 简介

**目标**：

- 了解委托生成器

**修改的文件**：`cofollow.py`，`server.py`

依赖生成器的代码中存在一个潜在问题，即向用户和编写库隐藏细节的问题。通常需要许多底层机制来驱动所有事情，而直接将其暴露给用户往往相当笨拙。

从 Python 3.3 开始，可以使用新的 `yield from` 语句将生成器委托给另一个函数。这是清理依赖生成器的代码的一种有用方法。
