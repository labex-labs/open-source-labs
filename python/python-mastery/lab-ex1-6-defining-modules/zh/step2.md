# 主模块

在你的 `pcost.py` 程序中，最后一条语句调用了一个函数并打印出结果。修改该程序，使得只有在程序作为主程序运行时才执行这一步骤。现在，尝试用两种方式运行该程序：

首先，作为主程序运行：

```bash
python3 pcost.py
44671.15
```

接下来，作为库导入运行该程序。你应该不会看到任何输出。

```python
>>> import pcost
>>>
```
