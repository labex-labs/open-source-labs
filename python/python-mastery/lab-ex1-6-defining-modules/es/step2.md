# Módulo principal

En su programa `pcost.py`, la última declaración llamó a una función e imprimió el resultado. Modifique el programa para que este paso solo ocurra si el programa se ejecuta como el programa principal. Ahora, intente ejecutar el programa de dos maneras:

Primero, ejecute el programa como principal:

```bash
python3 pcost.py
44671.15
```

Luego, ejecute el programa como una importación de biblioteca. No debería ver ningún resultado.

```python
>>> import pcost
>>>
```
