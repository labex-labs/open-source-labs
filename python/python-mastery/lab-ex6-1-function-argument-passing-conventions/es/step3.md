# Creando una representación útil

Modifica la clase `Structure` de modo que produzca una representación agradable cuando se utiliza `repr()`. Por ejemplo:

```python
>>> s = Stock('GOOG', 100, 490.1)
>>> s
Stock('GOOG',100,490.1)
>>>
```
