# Création d'une représentation utile

Modifiez la classe `Structure` de sorte qu'elle produise une représentation agréable lorsqu'on utilise `repr()`. Par exemple :

```python
>>> s = Stock('GOOG', 100, 490.1)
>>> s
Stock('GOOG',100,490.1)
>>>
```
