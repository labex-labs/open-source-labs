# Ein nützliches Repräsentationsformat erstellen

Ändern Sie die `Structure`-Klasse so, dass sie eine schöne Darstellung liefert, wenn `repr()` verwendet wird. Beispielsweise:

```python
>>> s = Stock('GOOG', 100, 490.1)
>>> s
Stock('GOOG',100,490.1)
>>>
```
