# Objekte lesen

Ändern Sie die Funktion `read_portfolio()`, um die neue `Stock.from_row()`-Methode zu verwenden, die Sie geschrieben haben.

Gedankenstütze: Welche Version des Codes gefällt Ihnen besser? Die Version von `read_portfolio()`, die die Typumwandlungen ausführte, oder die, die die Umwandlungen in der `Stock.from_row()`-Methode ausführt? Denken Sie an die Datenabstraktion.

Können Sie `read_portfolio()` so ändern, dass Objekte mithilfe einer anderen Klasse als `Stock` erstellt werden? Beispielsweise kann der Benutzer die Klasse auswählen, die er möchte?
