# Lecture d'objets

Modifiez la fonction `read_portfolio()` pour utiliser la nouvelle méthode `Stock.from_row()` que vous avez écrite.

Point de réflexion : Quelle version de code préférez-vous? La version de `read_portfolio()` qui effectuait les conversions de type ou celle qui effectue les conversions dans la méthode `Stock.from_row()`? Pensez à l'abstraction des données.

Êtes-vous capable de modifier `read_portfolio()` pour créer des objets à l'aide d'une classe autre que `Stock`? Par exemple, est-il possible que l'utilisateur sélectionne la classe qu'il souhaite?
