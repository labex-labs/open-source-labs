# 读取对象

修改 `read_portfolio()` 函数，使其使用你编写的新的 `Stock.from_row()` 方法。

思考要点：你更喜欢哪个版本的代码？是在 `read_portfolio()` 中执行类型转换的版本，还是在 `Stock.from_row()` 方法中执行转换的版本？考虑一下数据抽象。

你能修改 `read_portfolio()` 以使用 `Stock` 以外的类来创建对象吗？例如，用户能选择他们想要的类吗？
