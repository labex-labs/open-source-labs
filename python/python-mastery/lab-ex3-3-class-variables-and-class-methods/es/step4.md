# Leyendo objetos

Cambia la función `read_portfolio()` para que use el nuevo método `Stock.from_row()` que escribiste.

Punto de reflexión: ¿Qué versión de código te gusta más? La versión de `read_portfolio()` que realizaba las conversiones de tipo o la que realiza las conversiones en el método `Stock.from_row()`? Piensa en la abstracción de datos.

¿Puedes modificar `read_portfolio()` para crear objetos usando una clase diferente a `Stock`? Por ejemplo, ¿puede el usuario seleccionar la clase que desea?
