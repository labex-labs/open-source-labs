# オブジェクトの読み取り

書いた新しい`Stock.from_row()`メソッドを使用するように`read_portfolio()`関数を変更します。

考え点：どちらのバージョンのコードが好きですか？型変換を行った`read_portfolio()`のバージョンと、`Stock.from_row()`メソッドで変換を行うバージョンです。データ抽象化について考えてみてください。

`read_portfolio()`を変更して、`Stock`以外のクラスを使用してオブジェクトを作成できますか？たとえば、ユーザーが希望するクラスを選択できますか？
