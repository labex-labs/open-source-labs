# 简介

**目标**：

- 了解托管生成器

**创建的文件**：`multitask.py`，`server.py`

生成器或协程函数如果没有其他代码驱动，就永远无法执行。例如，用于迭代的生成器除非使用 `for` 循环实际进行迭代，否则不会做任何事情。同样，协程集合除非以某种方式调用其 `send()` 方法，否则不会运行。

在生成器的高级应用中，可以通过各种不同寻常的方式来驱动生成器。在本实验中，我们来看几个例子。
