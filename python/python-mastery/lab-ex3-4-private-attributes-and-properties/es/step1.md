# Atributos privados

Como regla general, los atributos que son internos a una clase deben tener un guión bajo al principio. En el ejercicio anterior, la clase `Stock` tenía una variable de clase `types` que se utilizaba para convertir filas de datos. Cambia el código para que esta variable tenga un guión bajo al principio.
