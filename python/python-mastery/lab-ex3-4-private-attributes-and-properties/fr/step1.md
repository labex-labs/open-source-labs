# Attributs privés

En règle générale, les attributs internes à une classe devraient avoir un tiret de soulignement au début. Dans l'exercice précédent, la classe `Stock` avait une variable de classe `types` qui était utilisée pour convertir les lignes de données. Modifiez le code de sorte que cette variable ait un tiret de soulignement au début.
