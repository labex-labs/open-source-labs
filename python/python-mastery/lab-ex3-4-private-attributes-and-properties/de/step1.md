# Private Attribute

Als allgemeine Regel sollten Attribute, die intern zu einer Klasse gehören, einen führenden Unterstrich haben. Im vorherigen Exercise hatte die `Stock`-Klasse eine `types`-Klassenvariable, die für die Umwandlung von Datenzeilen verwendet wurde. Ändern Sie den Code so, dass diese Variable einen führenden Unterstrich hat.
