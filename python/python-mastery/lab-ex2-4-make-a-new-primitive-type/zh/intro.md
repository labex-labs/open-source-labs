# 简介

**目标**：

- 创建一种新的原生类型

在大多数程序中，你会使用诸如 `int`、`float` 和 `str` 等原生类型来表示数据。然而，你并不局限于这些类型。标准库中有一些模块，比如 `decimal` 和 `fractions` 模块，它们实现了新的原生类型。只要你理解了使 Python 对象能够工作的底层协议，你也可以创建自己的类型。在这个实验中，我们将创建一种新的原生类型。虽然有很多细节需要注意，但这会让你大致了解所需的内容。
