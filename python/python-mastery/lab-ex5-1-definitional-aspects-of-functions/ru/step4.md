# Задача по дизайну: заголовки CSV

Код предполагает, что первая строка CSV-данных всегда содержит заголовки столбцов. Однако это не всегда так. Например, файл `portfolio_noheader.csv` содержит данные, но не содержит заголовков столбцов.

Как вы бы переписали код, чтобы учесть отсутствие заголовков столбцов, которые будут предоставляться вручную вызывающим объектом вместо этого?
