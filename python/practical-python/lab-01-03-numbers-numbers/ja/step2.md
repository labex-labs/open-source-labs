# ブール型 (bool)

ブール型には 2 つの値があります。`True` と `False`。

```python
a = True
b = False
```

数値としては、それぞれ値 `1` と `0` の整数として評価されます。

```python
c = 4 + True # 5
d = False
if d == 0:
    print('d is False')
```

_ただし、そのようなコードは書かないでください。それは奇妙です。_
