# 数字转换

类型名称可用于转换值：

```python
a = int(x)    # 将 x 转换为整数
b = float(x)  # 将 x 转换为浮点数
```

试试看。

```python
>>> a = 3.14159
>>> int(a)
3
>>> b = '3.14159' # 它也适用于包含数字的字符串
>>> float(b)
3.14159
>>>
```

提醒：这些练习假设你在 `~/project` 目录中工作。查找文件 `mortgage.py`。
