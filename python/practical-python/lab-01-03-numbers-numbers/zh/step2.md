# 布尔值（bool）

布尔值有两个值：`True`（真）、`False`（假）。

```python
a = True
b = False
```

在数值上，它们被评估为值为 `1`、`0` 的整数。

```python
c = 4 + True # 5
d = False
if d == 0:
    print('d 为 False')
```

_但是，不要这样写代码。这会很奇怪。_
