# Booleanos (bool)

Los booleanos tienen dos valores: `True`, `False`.

```python
a = True
b = False
```

Numéricamente, se evalúan como enteros con valor `1`, `0`.

```python
c = 4 + True # 5
d = False
if d == 0:
    print('d es False')
```

_Pero, no escribas código así. Sería extraño._
