# Booleanos (bool)

Booleanos possuem dois valores: `True`, `False`.

```python
a = True
b = False
```

Numericamente, eles são avaliados como inteiros com valor `1`, `0`.

```python
c = 4 + True # 5
d = False
if d == 0:
    print('d is False')
```

_Mas, não escreva código assim. Seria estranho._
