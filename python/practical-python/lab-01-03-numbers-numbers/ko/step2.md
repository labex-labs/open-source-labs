# 부울 (bool)

부울은 두 가지 값을 가집니다: `True`, `False`.

```python
a = True
b = False
```

수치적으로, 부울은 값 `1`, `0`을 가진 정수로 평가됩니다.

```python
c = 4 + True # 5
d = False
if d == 0:
    print('d is False')
```

_하지만, 그런 코드는 작성하지 마세요. 이상할 것입니다._
