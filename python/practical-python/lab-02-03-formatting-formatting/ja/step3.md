# 辞書の書式設定

値の辞書に文字列の書式設定を適用するには、`format_map()`メソッドを使用できます。

```python
>>> s = {
    'name': 'IBM',
   'shares': 100,
    'price': 91.1
}
>>> '{name:>10s} {shares:10d} {price:10.2f}'.format_map(s)
'       IBM        100      91.10'
>>>
```

これは`f-文字列`と同じコードを使用しますが、提供された辞書から値を取得します。
