# 연습 문제 3.18: 기존 함수 수정

`parse_csv()`의 수정된 버전과 함께 작동하도록 `report.py` 파일의 `read_portfolio()` 및 `read_prices()` 함수를 수정하십시오. 이는 사소한 수정만 포함해야 합니다. 그 후, `report.py` 및 `pcost.py` 프로그램은 항상 그랬던 것처럼 동일하게 작동해야 합니다.
