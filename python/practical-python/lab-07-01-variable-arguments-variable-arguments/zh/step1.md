# 位置可变参数（\*args）

一个接受任意数量参数的函数被称为使用了可变参数。例如：

```python
def f(x, *args):
 ...
```

函数调用：

```python
f(1,2,3,4,5)
```

额外的参数会作为一个元组传递。

```python
def f(x, *args):
    # x -> 1
    # args -> (2,3,4,5)
```
