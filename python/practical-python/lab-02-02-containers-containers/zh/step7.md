# 复合键

在Python中，几乎任何类型的值都可以用作字典的键。字典的键必须是不可变类型。例如，元组：

```python
holidays = {
  (1, 1) : '新年',
  (3, 14) : '圆周率日',
  (9, 13) : "程序员节",
}
```

然后进行访问：

```python
>>> holidays[3, 14]
'圆周率日'
>>>
```

_列表、集合或另一个字典都不能用作字典的键，因为列表和字典是可变的。_
