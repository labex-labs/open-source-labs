#!/bin/zsh
grep -q "write" ~/.python_history && grep -q "as" ~/.python_history && grep -q "open" ~/.python_history && grep -q "import os" ~/.python_history && grep -q "getcwd" ~/.python_history && grep -q "with" ~/.python_history && echo "True"
