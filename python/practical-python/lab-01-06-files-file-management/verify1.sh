#!/bin/zsh
grep -q "as" ~/.python_history && grep -q "open" ~/.python_history && grep -q "read" ~/.python_history && grep -q "close" ~/.python_history && grep -q "with" ~/.python_history && echo "True"