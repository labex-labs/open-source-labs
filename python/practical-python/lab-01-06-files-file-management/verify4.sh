#!/bin/zsh
grep -q "print" ~/.python_history && grep -q "as" ~/.python_history && grep -q "open" ~/.python_history && grep -q "next" ~/.python_history && grep -q "split" ~/.python_history && grep -q "with" ~/.python_history && echo "True"