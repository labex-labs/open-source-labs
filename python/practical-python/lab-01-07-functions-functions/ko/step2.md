# 라이브러리 함수 (Library Functions)

Python 에는 방대한 표준 라이브러리가 포함되어 있습니다. 라이브러리 모듈은 `import`를 사용하여 접근합니다. 예를 들어:

```python
import math
x = math.sqrt(10)

import urllib.request
u = urllib.request.urlopen('http://www.python.org/')
data = u.read()
```

라이브러리와 모듈에 대해서는 나중에 더 자세히 다루겠습니다.
