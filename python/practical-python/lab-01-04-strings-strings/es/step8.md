# Conversiones de cadenas

Utilice `str()` para convertir cualquier valor en una cadena. El resultado es una cadena que contiene el mismo texto que se habría producido con la instrucción `print()`.

```python
>>> x = 42
>>> str(x)
'42'
>>>
```
