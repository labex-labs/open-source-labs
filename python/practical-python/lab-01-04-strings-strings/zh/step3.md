# 字符串表示形式

字符串中的每个字符在内部都作为一个所谓的 Unicode “码点” 存储，它是一个整数。你可以使用以下转义序列指定确切的码点值：

```python
a = '\xf1'          # a = 'ñ'
b = '\u2200'        # b = '∀'
c = '\U0001D122'    # c = '𝄢'
d = '\N{FOR ALL}'   # d = '∀'
```

[Unicode 字符数据库](https://unicode.org/charts) 是所有可用字符代码的参考。
