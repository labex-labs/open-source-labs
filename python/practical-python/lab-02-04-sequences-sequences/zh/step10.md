# 针对元组的 for 循环

你可以使用多个迭代变量进行迭代。

```python
points = [
  (1, 4),(10, 40),(23, 14),(5, 6),(7, 8)
]
for x, y in points:
    # 循环时 x = 1，y = 4
    #            x = 10，y = 40
    #            x = 23，y = 14
    #           ...
```

当使用多个变量时，每个元组会被“解包”成一组迭代变量。变量的数量必须与每个元组中的元素数量相匹配。
