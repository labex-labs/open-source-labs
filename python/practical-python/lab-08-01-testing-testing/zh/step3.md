# 内联测试

断言也可用于简单测试。

```python
def add(x, y):
    return x + y

assert add(2,2) == 4
```

通过这种方式，你将测试与代码包含在同一个模块中。

_优点：如果代码明显有问题，尝试导入模块时将会崩溃。_

但这不推荐用于全面测试。它更像是一种基本的“冒烟测试”。该函数在任何示例上都能正常工作吗？如果不能，那么肯定有地方出问题了。
