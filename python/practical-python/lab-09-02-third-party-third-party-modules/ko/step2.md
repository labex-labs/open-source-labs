# 표준 라이브러리 모듈

Python 표준 라이브러리의 모듈은 일반적으로 `/usr/local/lib/python3.6`과 같은 위치에서 제공됩니다. 간단한 테스트를 통해 확실하게 확인할 수 있습니다:

```python
>>> import re
>>> re
<module 're' from '/usr/local/lib/python3.6/re.py'>
>>>
```

REPL 에서 모듈을 단순히 살펴보는 것은 알아두면 좋은 디버깅 팁입니다. 파일의 위치를 보여줍니다.
