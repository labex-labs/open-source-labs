# 모듈 검색 경로

`sys.path`는 `import` 문에 의해 검사되는 모든 디렉토리의 목록을 포함하는 디렉토리입니다. 확인해 보세요:

```python
>>> import sys
>>> sys.path
... 결과를 확인하세요 ...
>>>
```

무언가를 import 하려고 하는데 해당 항목이 이러한 디렉토리 중 하나에 위치하지 않으면 `ImportError` 예외가 발생합니다.
