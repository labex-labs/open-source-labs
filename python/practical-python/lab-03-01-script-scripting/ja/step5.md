# 関数とは？

関数は、名前付きの文のシーケンスです。

```python
def funcname(args):
  statement
  statement
...
  return result
```

内部では、Python の「任意の」文を使用できます。

```python
def foo():
    import math
    print(math.sqrt(2))
    help(math)
```

Python には特別な文はありません（覚えやすいです）。
