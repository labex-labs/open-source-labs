# Was ist eine Funktion?

Eine Funktion ist eine benannte Folge von Anweisungen.

```python
def funcname(args):
  Anweisung
  Anweisung
...
  return Ergebnis
```

_Alle_ Python-Anweisungen können darin verwendet werden.

```python
def foo():
    import math
    print(math.sqrt(2))
    help(math)
```

Es gibt keine _besonderen_ Anweisungen in Python (was es leicht zu merken macht).
