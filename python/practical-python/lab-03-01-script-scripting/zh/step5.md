# 什么是函数？

函数是一系列有名称的语句。

```python
def funcname(args):
  statement
  statement
...
  return result
```

任何Python语句都可以用在函数内部。

```python
def foo():
    import math
    print(math.sqrt(2))
    help(math)
```

Python中没有特殊语句（这使得它很容易记忆）。
