# Módulo principal de Python

Python no tiene una función o método _main_. En cambio, existe un _módulo principal_. El _módulo principal_ es el archivo fuente que se ejecuta primero.

```bash
$ python3 prog.py
...
```

Cualquier archivo que le des al intérprete al inicio se convierte en _main_. No importa el nombre.
