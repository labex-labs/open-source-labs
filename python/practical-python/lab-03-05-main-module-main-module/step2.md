# Python Main Module

Python has no _main_ function or method. Instead, there is a _main_
module. The _main module_ is the source file that runs first.

```bash
$ python3 prog.py
...
```

Whatever file you give to the interpreter at startup becomes _main_. It doesn't matter the name.
