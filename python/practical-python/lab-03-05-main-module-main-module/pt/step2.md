# Módulo Principal em Python

Python não possui uma função ou método _main_. Em vez disso, existe um _módulo principal_ (main module). O _módulo principal_ é o arquivo fonte que é executado primeiro.

```bash
$ python3 prog.py
...
```

Qualquer arquivo que você fornecer ao interpretador na inicialização se torna _main_. O nome não importa.
