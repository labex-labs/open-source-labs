# Python 메인 모듈 (Main Module)

Python 에는 _main_ 함수 또는 메서드가 없습니다. 대신, _main_ 모듈이 있습니다. *main 모듈*은 가장 먼저 실행되는 소스 파일입니다.

```bash
$ python3 prog.py
...
```

시작 시 인터프리터에 제공하는 파일이 무엇이든 *main*이 됩니다. 이름은 중요하지 않습니다.
