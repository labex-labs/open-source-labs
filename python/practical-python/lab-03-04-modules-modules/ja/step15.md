# 演習 3.14：さらにライブラリをインポートする

セクション 1 では、ポートフォリオを読み取り、そのコストを計算する `pcost.py` というプログラムを書きました。

```python
>>> import pcost
>>> pcost.portfolio_cost('/home/labex/project/portfolio.csv')
44671.15
>>>
```

`pcost.py` ファイルを修正して、`report.read_portfolio()` 関数を使用するようにします。
