# 해설

이 연습 문제를 완료하면 세 개의 프로그램이 있어야 합니다. 범용 `parse_csv()` 함수를 포함하는 `fileparse.py`. 멋진 보고서를 생성하지만 `read_portfolio()` 및 `read_prices()` 함수도 포함하는 `report.py`. 마지막으로, 포트폴리오 비용을 계산하지만 `report.py` 프로그램용으로 작성된 `read_portfolio()` 함수를 사용하는 `pcost.py`.
