# 네임스페이스 (Namespace)

모듈은 명명된 값들의 집합이며, 때로는 *네임스페이스*라고 불립니다. 이름은 소스 파일에 정의된 모든 전역 변수와 함수입니다. import 후, 모듈 이름은 접두사로 사용됩니다. 따라서 *네임스페이스*입니다.

```python
import foo

a = foo.grok(2)
b = foo.spam('Hello')
...
```

모듈 이름은 파일 이름과 직접적으로 연결됩니다 (foo -> foo.py).
