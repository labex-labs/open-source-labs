# 一般的な操作

辞書から値を取得するには、キー名を使用します。

```python
>>> print(s['name'], s['shares'])
GOOG 100
>>> s['price']
490.10
>>>
```

値を追加または変更するには、キー名を使用して代入します。

```python
>>> s['shares'] = 75
>>> s['date'] = '6/6/2007'
>>>
```

値を削除するには、`del` ステートメントを使用します。

```python
>>> del s['date']
>>>
```
