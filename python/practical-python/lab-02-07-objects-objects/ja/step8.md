# 名前、値、型

変数名には _型_ はありません。ただの名前です。ただし、値には基本的な型があります。

```python
>>> a = 42
>>> b = 'Hello World'
>>> type(a)
<type 'int'>
>>> type(b)
<type 'str'>
```

`type()` は、それが何であるかを教えてくれます。型名は通常、値を作成またはその型に変換する関数として使用されます。
