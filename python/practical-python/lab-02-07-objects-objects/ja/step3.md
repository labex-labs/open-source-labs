# 値の再代入

値を再代入するときは、以前の値が使用していたメモリを **決して上書きしません**。

```python
a = [1,2,3]
b = a
a = [4,5,6]

print(a)      # [4, 5, 6]
print(b)      # [1, 2, 3]    元の値を保持しています
```

忘れないでください：**変数は名前であり、メモリ位置ではありません**。
