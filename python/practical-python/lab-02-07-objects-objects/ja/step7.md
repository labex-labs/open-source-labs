# ディープコピー

時には、オブジェクトとその中に含まれるすべてのオブジェクトのコピーを作成する必要があります。この場合、`copy`モジュールを使用できます。

```python
>>> a = [2,3,[100,101],4]
>>> import copy
>>> b = copy.deepcopy(a)
>>> a[2].append(102)
>>> b[2]
[100,101]
>>> a[2] is b[2]
False
>>>
```
