# 代入

Pythonにおける多くの操作は、値の「代入」または「格納」に関連しています。

```python
a = value         # 変数への代入
s[n] = value      # リストへの代入
s.append(value)   # リストへの追加
d['key'] = value  # 辞書への追加
```

**注意**: 代入操作は、代入される値のコピーを **決して行いません**。すべての代入は、単に参照コピー（好きならポインタコピー）です。
