# 값 재할당

값을 재할당하는 것은 _절대로_ 이전 값이 사용하던 메모리를 덮어쓰지 않습니다.

```python
a = [1,2,3]
b = a
a = [4,5,6]

print(a)      # [4, 5, 6]
print(b)      # [1, 2, 3]    Holds the original value
```

기억하세요: **변수는 메모리 위치가 아닌 이름입니다.**
