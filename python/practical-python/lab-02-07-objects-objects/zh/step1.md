# 赋值

Python 中的许多操作都与 _赋值_ 或 _存储_ 值有关。

```python
a = value         # 赋值给变量
s[n] = value      # 赋值给列表
s.append(value)   # 追加到列表
d['key'] = value  # 添加到字典
```

_注意：赋值操作 **绝不会复制** 被赋值的值。_ 所有赋值操作都只是引用复制（如果你愿意，也可以说是指针复制）。
