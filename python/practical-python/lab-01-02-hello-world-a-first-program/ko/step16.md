# 사용자 입력 (User input)

입력된 사용자 입력을 한 줄 읽으려면 `input()` 함수를 사용합니다.

```python
name = input('Enter your name:')
print('Your name is', name)
```

`input`은 사용자에게 프롬프트를 출력하고 응답을 반환합니다. 이것은 작은 프로그램, 학습 연습 또는 간단한 디버깅에 유용합니다. 실제 프로그램에서는 널리 사용되지 않습니다.
