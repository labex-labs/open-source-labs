# 대소문자 구분 (Case Sensitivity)

Python 은 대소문자를 구분합니다. 대문자와 소문자는 서로 다른 문자로 간주됩니다. 다음은 모두 서로 다른 변수입니다.

```python
name = 'Jake'
Name = 'Elwood'
NAME = 'Guido'
```

언어 구문은 항상 소문자입니다.

```python
while x < 0:   # OK
WHILE x < 0:   # ERROR
```
