# 大文字小文字の区別

Python は大文字小文字を区別します。大文字と小文字は異なる文字として扱われます。これらはすべて異なる変数です。

```python
name = 'Jake'
Name = 'Elwood'
NAME = 'Guido'
```

言語の文は常に小文字です。

```python
while x < 0:   # 正しい
WHILE x < 0:   # エラー
```
