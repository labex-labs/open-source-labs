# Exercice 1.21 : Tests d'appartenance

Utilisez l'opérateur `in` ou `not in` pour vérifier si `'AIG'`, `'AA'` et `'CAT'` sont dans la liste de symboles.

```python
>>> # Est-ce que 'AIG' EST DANS `symlist`?
True
>>> # Est-ce que 'AA' EST DANS `symlist`?
False
>>> # Est-ce que 'CAT' N'EST PAS DANS `symlist`?
True
>>>
```
