# Exercice 1.20 : Boucler sur les éléments d'une liste

La boucle `for` fonctionne en parcourant les données d'une séquence telle qu'une liste. Vérifiez cela en tapant la boucle suivante et en observant ce qui se passe :

```python
>>> for s in symlist:
        print('s =', s)
# Regardez la sortie
```
