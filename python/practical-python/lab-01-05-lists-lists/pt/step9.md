# Exercício 1.21: Testes de pertinência (Membership tests)

Use o operador `in` ou `not in` para verificar se `'AIG'`, `'AA'` e `'CAT'` estão na lista de símbolos.

```python
>>> # 'AIG' está NA `symlist`?
True
>>> # 'AA' está NA `symlist`?
False
>>> # 'CAT' NÃO está NA `symlist`?
True
>>>
```
