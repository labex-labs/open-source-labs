# Exercício 1.20: Iterando sobre itens de lista

O loop `for` funciona iterando sobre dados em uma sequência, como uma lista. Verifique isso digitando o seguinte loop e observando o que acontece:

```python
>>> for s in symlist:
        print('s =', s)
# Veja a saída
```
