# 연습 문제 1.21: 멤버십 테스트 (Membership tests)

`in` 또는 `not in` 연산자를 사용하여 `'AIG'`, `'AA'`, 및 `'CAT'`가 심볼 리스트에 있는지 확인하십시오.

```python
>>> # 'AIG'가 `symlist` 에 IN 되어 있습니까?
True
>>> # 'AA'가 `symlist` 에 IN 되어 있습니까?
False
>>> # 'CAT'가 `symlist` 에 NOT IN 되어 있습니까?
True
>>>
```
