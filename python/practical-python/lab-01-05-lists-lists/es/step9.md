# Ejercicio 1.21: Pruebas de pertenencia

Utiliza el operador `in` o `not in` para comprobar si `'AIG'`, `'AA'` y `'CAT'` están en la lista de símbolos.

```python
>>> # ¿Está 'AIG' EN la `symlist`?
True
>>> # ¿Está 'AA' EN la `symlist`?
False
>>> # ¿No está 'CAT' EN la `symlist`?
True
>>>
```
