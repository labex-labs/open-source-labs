# Ejercicio 1.24: Volver a ponerlo todo junto

¿Quieres tomar una lista de cadenas y unirlas en una sola cadena? Utiliza el método `join()` de las cadenas de la siguiente manera (ten en cuenta: esto puede parecer extraño al principio).

```python
>>> a = ','.join(symlist)
>>> a
'YHOO,RHT,HPQ,GOOG,AIG,AAPL,AA'
>>> b = ':'.join(symlist)
>>> b
'YHOO:RHT:HPQ:GOOG:AIG:AAPL:AA'
>>> c = ''.join(symlist)
>>> c
'YHOORHTHPQGOOGAIGAAPLAA'
>>>
```
