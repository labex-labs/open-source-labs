# Funções de Callback

No exemplo acima, a função chave é um exemplo de uma função de callback. O método `sort()` "chama de volta" (calls back) uma função que você fornece. Funções de callback são frequentemente funções curtas de uma linha que são usadas apenas para essa única operação. Os programadores frequentemente pedem um atalho para especificar esse processamento extra.
