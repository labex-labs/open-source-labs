# インスタンス

インスタンスは、プログラムで操作する実際の「オブジェクト」です。

クラスを関数として呼び出すことで作成されます。

```python
>>> a = Player(2, 3)
>>> b = Player(10, 20)
>>>
```

`a` と `b` は `Player` のインスタンスです。

_強調: クラス宣言はただの定義です (それ自体では何もしません)。関数定義と同様です。_
