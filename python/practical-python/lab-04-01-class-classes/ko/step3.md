# 인스턴스 (Instances)

인스턴스는 프로그램에서 조작하는 실제 *객체*입니다.

클래스를 함수처럼 호출하여 생성됩니다.

```python
>>> a = Player(2, 3)
>>> b = Player(10, 20)
>>>
```

`a`와 `b`는 `Player`의 인스턴스입니다.

_강조: class 문은 단지 정의일 뿐이며 (그 자체로는 아무것도 하지 않습니다). 함수 정의와 유사합니다._
