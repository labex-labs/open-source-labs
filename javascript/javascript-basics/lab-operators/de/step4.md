# Strenge Gleichheit

Dies führt einen Test durch, um zu überprüfen, ob zwei Werte gleich sind und vom gleichen Datentyp. Es gibt ein `true`/`false` (Boolean) Ergebnis zurück.

```js
let myVariable = 3;
myVariable === 4;
```
