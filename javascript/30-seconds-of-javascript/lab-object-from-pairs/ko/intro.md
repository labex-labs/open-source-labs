# 소개

이 랩에서는 JavaScript 에서 키 - 값 쌍으로부터 객체를 생성하는 방법을 살펴봅니다. `Array.prototype.reduce()` 메서드를 사용하여 쌍들을 단일 객체로 결합할 것입니다. 또한, 유사한 기능을 제공하는 `Object.fromEntries()` 메서드에 대해서도 알아볼 것입니다. 이 랩을 마치면 JavaScript 에서 키 - 값 쌍으로 작업하는 방법에 대한 이해도가 높아질 것입니다.
