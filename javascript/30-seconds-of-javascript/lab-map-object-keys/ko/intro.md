# 소개

이 랩에서는 제공된 함수를 사용하여 객체의 키를 매핑하고 새로운 객체를 생성하는 방법을 살펴봅니다. `Object.keys()`를 사용하여 객체의 키를 반복하고, `Array.prototype.reduce()`를 사용하여 제공된 함수를 통해 동일한 값을 가지면서 매핑된 키를 가진 새로운 객체를 생성합니다. 이 랩을 마치면 JavaScript 를 사용하여 객체의 키를 변환하는 방법에 대한 확실한 이해를 얻게 될 것입니다.
