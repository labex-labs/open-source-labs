# N 까지의 숫자 합계를 계산하는 함수

1 부터 `n`까지의 숫자 합계를 계산하려면 공식 `(n * (n + 1)) / 2`를 사용할 수 있습니다. 다음은 JavaScript 로 작성된 예제 코드입니다.

```js
const sumN = (n) => (n * (n + 1)) / 2;
```

이 함수를 사용하려면 `n`의 값을 인수로 전달하십시오. 예를 들어, `sumN(100)`은 `5050`을 반환합니다. 코딩 연습을 시작하려면 터미널/SSH 를 열고 `node`를 입력하십시오.
