# Introducción

En este laboratorio, exploraremos el concepto de sumar todos los números entre 1 y un número dado `n`. Utilizaremos la fórmula `(n * (n + 1)) / 2` para calcular la suma de los números. A través de este laboratorio, obtendrás una mejor comprensión de cómo resolver problemas matemáticos utilizando la programación.
