# Nまでの数の合計を計算する関数

1 から `n` までの数の合計を計算するには、`(n * (n + 1)) / 2` の式を使用できます。以下は、JavaScript のサンプルコードです。

```js
const sumN = (n) => (n * (n + 1)) / 2;
```

この関数を使用するには、`n` の値を引数として渡します。たとえば、`sumN(100)` を実行すると `5050` が返されます。コーディングの練習を始めるには、ターミナル/SSH を開き、`node` と入力します。
