# 소개

이 랩에서는 JavaScript 에서 `differenceBy` 함수의 구현을 살펴보겠습니다. 이 함수는 제공된 함수를 두 배열의 각 요소에 적용하여 두 배열 간의 차이를 찾을 수 있게 해줍니다. `Set`, `Array.prototype.map()`, 그리고 `Array.prototype.filter()`를 사용하여 특정 기준에 따라 배열을 효과적으로 비교하고 필터링하는 방법을 배우겠습니다.
