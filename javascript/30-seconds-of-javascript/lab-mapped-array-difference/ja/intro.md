# はじめに

この実験では、JavaScript における `differenceBy` 関数の実装方法を検討します。この関数を使うと、2 つの配列の各要素に対して指定された関数を適用することで、両方の配列の違いを見つけることができます。特定の基準に基づいて配列を効果的に比較およびフィルタリングするために、`Set`、`Array.prototype.map()`、および `Array.prototype.filter()` をどのように使うかを学びます。
