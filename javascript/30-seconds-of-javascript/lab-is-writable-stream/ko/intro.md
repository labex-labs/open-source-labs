# 소개

이 랩에서는 주어진 인수가 JavaScript 에서 쓰기 가능한 스트림 (writable stream) 인지 확인하는 방법을 살펴봅니다. 값 (value) 이 `null`과 다른지, `function` 타입의 `pipe` 속성을 가지고 있는지, 그리고 `function` 타입의 `_write` 속성과 `object` 타입의 `_writableState` 속성을 가지고 있는지 확인하는 함수를 사용합니다. 이 랩을 통해 JavaScript 에서 스트림을 사용하는 방법에 대한 이해를 높일 수 있습니다.
