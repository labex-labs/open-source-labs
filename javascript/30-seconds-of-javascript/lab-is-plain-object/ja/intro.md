# はじめに

この実験では、JavaScriptを使って与えられた値が単純なオブジェクトかどうかをチェックする概念を探ります。提供された値がObjectコンストラクタによって作成されたオブジェクトであるかどうかを確認するために、`isPlainObject()`関数を使用します。この実験が終了するまでに、JavaScriptにおいてオブジェクトが単純かどうかを判断する方法をより深く理解するようになります。
