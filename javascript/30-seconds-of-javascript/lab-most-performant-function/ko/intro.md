# 소개

이 랩에서는 함수 배열에서 가장 빠르게 실행되는 함수의 인덱스를 반환하는 성능 좋은 함수를 작성하는 방법을 탐구하고 연습합니다. 이 랩은 함수 실행 시간을 정확하게 측정하고 비교하기 위해 `performance.now()`와 `Array.prototype.map()`을 사용하는 데 중점을 둡니다. 이 랩을 마치면 최대 성능을 위해 코드를 최적화하는 방법에 대한 이해도가 높아질 것입니다.
