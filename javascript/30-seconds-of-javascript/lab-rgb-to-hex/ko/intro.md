# 소개

이 랩에서는 JavaScript 에서 RGB 값을 16 진수 색상 코드로 변환하는 과정을 살펴봅니다. 비트 단위 왼쪽 시프트 연산자 (bitwise left-shift operator) 와 `Number.prototype.toString()`을 사용하여 주어진 RGB 매개변수를 `String.prototype.padStart()`를 사용하여 6 자리 16 진수 값으로 변환합니다. 이 랩은 변환 과정을 이해하고 JavaScript 에서 알고리즘을 구현하는 실질적인 경험을 제공하는 데 도움이 될 것입니다.
