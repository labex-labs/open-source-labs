# はじめに

この実験では、配列の最後の `n` 要素を抽出するために使用される `lastN` 関数を調べます。これを達成するために `Array.prototype.slice()` メソッドをどのように使用するか学び、JavaScript プログラムでどのように実装できるか見てみます。この実験が終わるとき、JavaScript で配列を操作し、それらから特定の要素を抽出する方法をより深く理解しているでしょう。
