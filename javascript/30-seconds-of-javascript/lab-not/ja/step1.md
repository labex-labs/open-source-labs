# JavaScript における論理否定演算子の使用

JavaScript でコーディングを練習するには、ターミナル/SSH を開き、`node` と入力します。

論理否定演算子 (`!`) は、与えられた値の逆を返します。以下は、JavaScript における論理否定関数の例の実装です：

```js
const not = (a) => !a;
```

与えられた値の逆を返すために `not` 関数を使用できます。以下はいくつかの例です：

```js
not(true); // false
not(false); // true
```
