# 소개

이 랩에서는 JavaScript 를 사용하여 객체 키를 심볼 (Symbol) 로 변환하는 방법을 배웁니다. `Object.keys()` 메서드를 사용하여 객체의 키를 가져온 다음, `Array.prototype.reduce()`와 `Symbol` 메서드를 함께 사용하여 심볼화된 키를 가진 새로운 객체를 생성합니다. 이 기술은 객체에 고유하고 불변 (immutable) 한 키가 필요한 시나리오에서 유용할 수 있습니다.
