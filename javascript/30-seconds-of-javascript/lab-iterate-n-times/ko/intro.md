# 소개

이 랩에서는 JavaScript 의 `times()` 함수를 살펴봅니다. 이 함수는 지정된 횟수만큼 또는 콜백이 `false`를 반환할 때까지 콜백을 반복합니다. 이 함수를 사용하여 함수를 반복적으로 실행하는 방법과 콜백 함수에 인수를 전달하는 방법을 배우게 됩니다. 이 랩을 마치면 `times()` 함수를 사용하여 코드를 더욱 효율적이고 간결하게 만드는 방법에 대한 확실한 이해를 갖게 될 것입니다.
