# Introdução

Neste laboratório, exploraremos como ordenar um array de objetos com base nas propriedades e ordens fornecidas. Usaremos o método `Array.prototype.sort()` e o método `Array.prototype.reduce()` para alcançar este objetivo. O laboratório fornecerá uma compreensão prática de como manipular arrays de objetos em JavaScript.
