# Einführung

In diesem Lab werden wir untersuchen, wie man ein Array von Objekten mithilfe der bereitgestellten Zuordnungsfunktionen in ein Objekt abbilden kann. Wir werden die `Array.prototype.reduce()`-Methode verwenden, um das Array abzubilden, und die `mapKey`- und `mapValue`-Funktionen, um die Schlüssel und Werte des resultierenden Objekts jeweils abzubilden. Am Ende dieses Labs werden Sie ein besseres Verständnis dafür haben, wie man in JavaScript Datenstrukturen transformiert.
