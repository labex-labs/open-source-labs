# 소개

이 랩에서는 주어진 날짜가 유효한지 여부를 확인하는 JavaScript 함수를 살펴봅니다. 이 함수는 spread operator 와 `Date` 생성자를 사용하여 주어진 인수로 새로운 `Date` 객체를 생성합니다. 그런 다음 `valueOf()` 메서드와 `Number.isNaN()`을 사용하여 객체가 유효한지 확인합니다. 이 랩은 JavaScript 함수와 날짜 객체 작업을 연습할 수 있는 좋은 기회입니다.
