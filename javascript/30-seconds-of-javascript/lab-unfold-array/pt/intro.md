# Introdução

Neste laboratório, exploraremos a função `unfold()` em JavaScript. Esta função nos permite criar um array chamando repetidamente uma função iteradora com um valor inicial (seed) até que a função retorne `false`. Através deste laboratório, aprenderemos como usar a função `unfold()` e como ela pode ser usada para simplificar certas tarefas de programação.
