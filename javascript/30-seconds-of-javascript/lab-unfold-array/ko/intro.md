# 소개

이 랩에서는 JavaScript 의 `unfold()` 함수를 탐구해 보겠습니다. 이 함수를 사용하면 초기 시드 값으로 반복적으로 이터레이터 함수를 호출하여 함수가 `false`를 반환할 때까지 배열을 생성할 수 있습니다. 이 랩을 통해 `unfold()` 함수를 사용하는 방법과 특정 프로그래밍 작업을 단순화하는 데 어떻게 사용될 수 있는지 배우게 됩니다.
