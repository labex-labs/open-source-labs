# 소개

이 랩에서는 JavaScript 에서 객체로부터 키 - 값 쌍 배열의 배열을 생성하는 방법을 살펴봅니다. 이 랩에서는 `Object.keys()`와 `Array.prototype.map()`을 사용하여 객체의 키를 반복하고 키 - 값 쌍의 배열을 생성하는 방법에 대한 단계별 지침을 제공합니다. 또한 유사한 기능을 제공하는 내장된 `Object.entries()` 메서드에 대해서도 알아봅니다.
