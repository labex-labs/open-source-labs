# はじめに

この実験では、2つ以上の数値または配列の合計を計算するために `Array.prototype.reduce()` メソッドの使用方法を検討します。値 `0` でアキュミュレータを初期化する方法と、配列を個々の引数として渡すためにスプレッド演算子を使用する方法を学びます。この実験が終了するまでに、JavaScript で `reduce()` メソッドをどのように使用するかをより深く理解するようになります。
