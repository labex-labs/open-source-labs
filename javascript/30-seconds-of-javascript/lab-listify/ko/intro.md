# 소개

이 랩에서는 JavaScript 의 `listify()` 함수를 사용하여 객체를 객체 배열로 매핑하는 방법을 배웁니다. 이 함수는 `Object.entries()`와 `Array.prototype.reduce()`를 사용하여 객체를 매핑하고, `mapFn`을 사용하여 객체의 키와 값을 매핑합니다. 이 랩을 마치면 JavaScript 를 사용하여 객체를 배열로 효율적으로 매핑할 수 있게 됩니다.
