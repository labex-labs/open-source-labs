# 숫자가 홀수인지 확인하기

코딩 연습을 위해 터미널/SSH 를 열고 `node`를 입력하세요. 모듈로 연산자 (`%`) 를 사용하여 숫자가 홀수인지 짝수인지 확인합니다. 결과가 1 이면 숫자는 홀수이고, 결과가 0 이면 숫자는 짝수입니다. 숫자가 홀수이면 `true`를 반환하고, 숫자가 짝수이면 `false`를 반환합니다.

```js
const isOdd = (num) => num % 2 === 1;
```

사용 예시:

```js
isOdd(3); // true
```
