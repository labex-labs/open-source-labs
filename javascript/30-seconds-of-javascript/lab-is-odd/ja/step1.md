# 数が奇数かどうかを確認する

コーディングを練習するには、ターミナル/SSH を開いて `node` と入力します。剰余 (`%`) 演算子を使用して、数が奇数か偶数かを確認します。結果が 1 の場合、その数は奇数であり、結果が 0 の場合、その数は偶数です。数が奇数の場合は `true` を返し、偶数の場合は `false` を返します。

```js
const isOdd = (num) => num % 2 === 1;
```

使用例:

```js
isOdd(3); // true
```
