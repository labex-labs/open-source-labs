# Introdução

Neste laboratório, exploraremos o uso da função `overArgs` em JavaScript. A função nos permite transformar argumentos antes de passá-los para outra função. Aprenderemos como usar `overArgs` para simplificar o código e aumentar a reutilização, criando uma nova função que invoca uma função existente com argumentos transformados.
