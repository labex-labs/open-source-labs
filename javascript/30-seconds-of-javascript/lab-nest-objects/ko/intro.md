# 소개

이 랩에서는 평면 배열 내에서 서로 연결된 객체를 재귀적으로 중첩하는 JavaScript 함수인 `nest`를 살펴봅니다. 이 함수는 재귀, `Array.prototype.filter()`, 그리고 `Array.prototype.map()`을 사용하여 항목의 `id` 및 `parent_id` 속성을 기반으로 항목을 필터링하고 중첩합니다. 이 랩을 마치면 JavaScript 에서 재귀를 사용하여 중첩된 객체를 생성하는 방법에 대한 이해도가 높아질 것입니다.
