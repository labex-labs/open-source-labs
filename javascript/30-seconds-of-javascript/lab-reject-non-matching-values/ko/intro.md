# 소개

이 랩에서는 배열의 값을 predicate 함수를 기반으로 필터링하여, predicate 함수가 `false`를 반환하는 값만 반환하는 방법을 살펴봅니다. 이를 위해 `Array.prototype.filter()`를 predicate 함수와 함께 사용합니다. 이 랩을 마치면 JavaScript 코드에서 이 기술을 구현하는 방법에 대한 이해를 높일 수 있습니다.
