# 소개

이 랩에서는 주어진 인수에 여러 함수를 호출하고 결과를 반환하는 기능을 탐구합니다. 특히, 여러 함수를 입력으로 받아 해당 함수 각각을 수신하는 인수에 적용하는 새로운 함수를 반환하는 함수를 만들 것입니다. 이는 JavaScript 에서 `Array.prototype.map()`과 `Function.prototype.apply()`의 사용법을 더 잘 이해하는 데 도움이 될 것입니다.
