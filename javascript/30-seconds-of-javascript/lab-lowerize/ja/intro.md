# はじめに

この実験では、JavaScript を使ってオブジェクトのすべてのキーを小文字に変換する方法を学びます。`Object.keys()` と `Array.prototype.reduce()` を使ってキーをオブジェクトにマッピングし、`String.prototype.toLowerCase()` を使って小文字に変換します。この手法は、キーの大文字と小文字の表記が一貫していないオブジェクトを扱う際に役立ちます。
