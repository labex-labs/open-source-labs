# 소개

이 랩에서는 비교 함수를 기반으로 배열의 값을 교체하거나 추가하는 JavaScript 프로그래밍 연습을 진행합니다. 이 연습을 통해 스프레드 연산자 (spread operator), `Array.prototype.findIndex()`, `Array.prototype.push()`, 그리고 `Array.prototype.splice()`를 사용하여 JavaScript 에서 배열을 조작하는 방법을 배우게 됩니다. 이 랩은 JavaScript 에서 배열과 함수를 다루는 기술을 향상시키는 데 도움이 될 것입니다.
