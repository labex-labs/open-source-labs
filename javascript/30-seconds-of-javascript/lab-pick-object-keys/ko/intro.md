# 소개

이 랩에서는 JavaScript 에서 `pick` 함수를 사용하여 객체에서 특정 키 - 값 쌍을 추출하는 방법을 살펴봅니다. 객체와 키 배열을 `pick` 함수에 전달하는 방법과 `Array.prototype.reduce()`를 사용하여 객체에서 지정된 키 - 값 쌍만 필터링하고 반환하는 방법을 배우게 됩니다. 이 랩은 JavaScript 에서 객체로 작업하고 객체에서 필요한 정보만 추출하는 방법을 이해하는 데 도움이 될 것입니다.
