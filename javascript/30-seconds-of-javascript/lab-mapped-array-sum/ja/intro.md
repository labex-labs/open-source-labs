# はじめに

この実験では、JavaScript におけるマッピングされた配列の合計の概念を探ります。この実験の目的は、提供された関数を使って各要素を値にマッピングすることにより、配列の合計をどのように計算するかを深く理解することです。この実験が終了するまでに、学習者は `Array.prototype.map()` と `Array.prototype.reduce()` を使って、JavaScript プロジェクトにおいてマッピングされた配列の合計を実装できるようになります。
