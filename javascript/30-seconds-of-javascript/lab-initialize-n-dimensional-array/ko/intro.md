# 소개

이 랩에서는 JavaScript 에서 재귀를 사용하여 주어진 값으로 n 차원 배열을 생성하는 방법을 살펴봅니다. `Array.from()` 및 `Array.prototype.map()` 메서드를 사용하여 각 행이 `initializeNDArray()`를 사용하여 초기화된 새로운 배열인 행을 생성합니다. 이 랩을 마치면 JavaScript 에서 다차원 배열을 생성하는 방법과 이 작업을 수행하기 위해 재귀를 사용하는 방법에 대한 확실한 이해를 갖게 될 것입니다.
