# 소개

이 랩에서는 JavaScript 를 사용하여 일련의 프로미스 (promise) 를 순차적으로 실행하는 방법을 살펴봅니다. `Array.prototype.reduce()` 메서드를 사용하여 프로미스 체인 (promise chain) 을 생성할 것입니다. 여기서 각 프로미스는 해결될 때 다음 프로미스를 반환합니다. 이 랩을 마치면 프로미스를 순차적으로 실행하는 방법에 대한 이해도가 높아져 프로그래밍 기술을 한 단계 더 발전시킬 수 있습니다.
