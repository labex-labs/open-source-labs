# 소개

이 랩에서는 JavaScript 를 사용하여 지정된 인덱스에 값을 배열에 삽입하는 개념을 탐구합니다. `Array.prototype.splice()` 메서드를 사용하여 주어진 인덱스 뒤에 삭제 개수 (delete count) 가 `0`인 값을 삽입하는 방법을 배우게 됩니다. 이 랩은 JavaScript 에서 배열을 조작하는 실질적인 경험을 제공하고, 새로운 값을 삽입하면서 원본 배열을 어떻게 변경하는지 이해하는 데 도움이 될 것입니다.
