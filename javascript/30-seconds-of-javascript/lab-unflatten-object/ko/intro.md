# 소개

이 랩에서는 JavaScript 에서 키에 경로가 있는 객체를 unflatten 하는 방법을 살펴봅니다. 중첩된 `Array.prototype.reduce()` 및 `String.prototype.split()` 메서드를 사용하여 점 구분 기호로 각 키를 분할하고 키에 대한 객체를 추가합니다. 이 랩을 마치면 평면 객체를 중첩 객체로 효율적으로 변환하는 방법에 대한 확실한 이해를 갖게 될 것입니다.
