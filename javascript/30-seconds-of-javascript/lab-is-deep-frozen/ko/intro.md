# 소개

이 랩에서는 JavaScript 에서 객체가 깊이 동결 (deeply frozen) 되었는지 확인하는 방법을 배웁니다. 이 랩은 `Object.isFrozen()`을 사용하여 객체가 동결되었는지 확인하고, `Object.keys()`와 `Array.prototype.every()`를 사용하여 모든 키가 깊이 동결되었는지 확인하는 재귀 함수를 안내합니다. 이 랩을 통해 JavaScript 에서 객체의 깊이 동결 상태를 결정하는 방법에 대한 이해를 높일 수 있습니다.
