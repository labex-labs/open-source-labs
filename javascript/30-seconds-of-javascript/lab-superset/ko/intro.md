# 소개

이 랩에서는 `Set` 생성자와 `Array.prototype.every()` 메서드를 사용하여 한 iterable 이 다른 iterable 의 상위 집합인지 확인하는 방법을 살펴봅니다. 이 랩에서는 첫 번째 iterable 이 중복을 제외하고 두 번째 iterable 의 모든 요소를 포함하는지 확인하는 함수를 만드는 과정을 안내합니다. 이 랩을 마치면 JavaScript 에서 set 을 사용하는 방법에 대한 이해도가 높아질 것입니다.
