# 値が文字列であるかどうかをチェックする

値が文字列であるかどうかをチェックするには、`typeof` キーワードの後にチェックしたい値を続けます。この方法は文字列プリミティブにのみ機能します。

与えられた値が文字列であるかどうかをチェックするコードの例を以下に示します。

```js
const isString = (val) => typeof val === "string";
```

`isString` 関数を次のように使用できます。

```js
isString("10"); // true
```
