# 값이 문자열인지 확인하기

값이 문자열인지 확인하려면 `typeof` 키워드 뒤에 확인하려는 값을 사용합니다. 이 방법은 문자열 원시 타입에 대해서만 작동합니다.

다음은 주어진 값이 문자열인지 확인하는 예제 코드입니다.

```js
const isString = (val) => typeof val === "string";
```

`isString` 함수는 다음과 같이 사용할 수 있습니다.

```js
isString("10"); // true
```
