# はじめに

この実験では、1つの反復可能オブジェクトの値のサブセットが別の反復可能オブジェクトに含まれているかどうかを確認する方法を探ります。 `Set` コンストラクタと `Array.prototype.every()` を使って、最初の反復可能オブジェクトのすべての値が2番目の反復可能オブジェクトに存在するかどうかを確認する関数を作成します。この実験が終了するまでに、JavaScriptにおけるセットの使い方をより深く理解するようになります。
