# はじめに

この実験では、ソート済み配列の最後の挿入インデックスを見つける概念を探ります。配列が降順にソートされているかどうかを確認し、`Array.prototype.reverse()` と `Array.prototype.findIndex()` をどのように使用して要素を挿入する適切な最後のインデックスを見つけるかを学びます。この実験が終わるとき、配列のソート順を維持する方法をより深く理解しているでしょう。
