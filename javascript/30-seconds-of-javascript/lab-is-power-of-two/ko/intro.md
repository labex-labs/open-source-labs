# 소개

이 랩에서는 주어진 숫자가 `2`의 거듭제곱인지 판별하는 JavaScript 함수를 살펴봅니다. 비트 단위 AND 연산자 (bitwise binary AND operator) 를 사용하고 숫자가 falsy 가 아닌지 확인하여 숫자가 `2`의 거듭제곱인지 정확하게 판별할 수 있습니다. 이 랩은 JavaScript 에서 비트 연산자 (bitwise operators) 와 논리 연산자 (logical operators) 를 사용하는 연습을 할 수 있는 기회를 제공합니다.
