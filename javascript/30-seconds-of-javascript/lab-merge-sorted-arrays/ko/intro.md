# 소개

이 랩에서는 JavaScript 를 사용하여 정렬된 두 개의 배열을 하나의 정렬된 배열로 병합하는 방법을 살펴봅니다. 스프레드 연산자 (spread operator), `Array.from()`, 그리고 `shift()` 메서드를 사용하여 배열을 효율적으로 병합할 것입니다. 이 랩을 마치면 JavaScript 에서 배열을 조작하는 방법에 대한 더 깊은 이해를 얻게 될 것입니다.
