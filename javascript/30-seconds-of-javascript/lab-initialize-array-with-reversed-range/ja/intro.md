# はじめに

この実験では、JavaScriptを使って逆順の範囲で配列を初期化する方法を探ります。指定された範囲の数値を含むが逆順の配列を作成するために、`Array.from()` と `Array.prototype.map()` メソッドをどのように使うか学びます。また、`start` と `step` パラメータのデフォルト値を設定して、それらを使って望ましい出力を生成する方法も見てみます。
