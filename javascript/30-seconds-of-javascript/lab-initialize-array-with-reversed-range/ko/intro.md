# 소개

이 랩에서는 JavaScript 를 사용하여 역순 범위로 배열을 초기화하는 방법을 살펴봅니다. `Array.from()` 및 `Array.prototype.map()` 메서드를 사용하여 지정된 범위의 숫자를 포함하지만 역순으로 정렬된 배열을 만드는 방법을 배웁니다. 또한 `start` 및 `step` 매개변수에 대한 기본값을 설정하고 이를 사용하여 원하는 출력을 생성하는 방법도 살펴봅니다.
