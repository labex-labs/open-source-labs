# 数が偶数かどうかを確認する

コーディングを練習するには、ターミナル/SSH を開き、`node` と入力します。次のコードを使って数が偶数か奇数かを確認します：

```js
const isEven = (num) => num % 2 === 0;
```

上記のコードは、剰余 (`%`) 演算子を使って数が奇数か偶数かを確認します。数が偶数の場合、関数は `true` を返します。奇数の場合、関数は `false` を返します。

以下は、`isEven` 関数を使う方法の例です：

```js
isEven(3); // returns false
```
