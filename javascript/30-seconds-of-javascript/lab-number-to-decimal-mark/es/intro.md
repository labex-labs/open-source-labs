# Introducción

En este laboratorio, exploraremos el objeto `Number` en JavaScript y aprenderemos cómo convertir un número en una cadena con formato de separador decimal. Utilizaremos el método `toLocaleString()` para lograr esto. Al final del laboratorio, tendrás una comprensión sólida de cómo formatear números de manera legible y amigable para el usuario.
