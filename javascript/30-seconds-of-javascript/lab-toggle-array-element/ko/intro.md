# 소개

이 랩에서는 JavaScript 를 사용하여 배열 내 요소를 토글하는 방법을 살펴봅니다. `toggleElement()` 함수를 사용하면 배열에 이미 포함된 요소를 제거하거나, 그렇지 않은 경우 배열에 요소를 추가할 수 있습니다. 이 랩은 `includes()` 및 `filter()` 메서드를 스프레드 연산자 (spread operator) 와 함께 사용하여 배열 요소를 효율적으로 토글하는 방법을 이해하는 데 도움이 될 것입니다.
