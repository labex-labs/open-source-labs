# 소개

이 랩에서는 주어진 배열에서 `n`개의 최대 요소를 반환하는 데 사용되는 `maxN` 함수를 살펴보겠습니다. `Array.prototype.sort()`, spread operator (`...`), 그리고 `Array.prototype.slice()`를 사용하여 배열을 내림차순으로 정렬하고 슬라이스하여 지정된 수의 요소를 반환하는 방법을 배우게 됩니다. 이 랩은 JavaScript 에서 배열을 조작하는 방법에 대한 이해를 높이는 데 도움이 될 것입니다.
