# 소개

이 랩에서는 특정 조건이 충족될 때까지 함수에 의해 생성된 값으로 배열을 초기화하고 채우는 개념을 탐구합니다. `conditionFn`과 `mapFn`의 두 인수를 사용하는 `initializeArrayUntil` 함수를 사용할 것입니다. 이 랩을 통해 이 함수를 사용하여 사용자 지정 값과 조건으로 배열을 생성하는 방법에 대한 더 깊은 이해를 얻게 될 것입니다.
