# はじめに

この実験では、JavaScript の `nthElement` 関数について学びます。この関数は配列の n 番目の要素を返します。`Array.prototype.slice()` を使って配列から要素を抽出する方法と、範囲外のインデックスを上手に処理する方法を学びます。この実験が終わるとき、`nthElement` 関数とそれが JavaScript プロジェクトでどのように役立つかをしっかり理解しているでしょう。
