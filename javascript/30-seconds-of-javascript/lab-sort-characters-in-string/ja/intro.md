# はじめに

この実験では、JavaScript を使って文字列をアルファベット順にソートする方法を探ります。これを達成するために、スプレッド演算子、`Array.prototype.sort()`、および `String.prototype.localeCompare()` を使用します。実験が終了するとき、これらのメソッドを使って JavaScript で文字列を操作する方法を十分に理解しているでしょう。
