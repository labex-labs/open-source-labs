# 소개

이 랩에서는 배열에서 일치하는 값을 추출하고 제거된 요소를 반환하는 방법을 살펴봅니다. `Array.prototype.filter()`, `Array.prototype.includes()`, 그리고 `Array.prototype.push()` 메서드를 사용하여 pull 배열에 지정된 값을 필터링하고 원래 배열을 변경하는 방법을 배우게 됩니다. 이 랩을 마치면 JavaScript 에서 배열을 조작하는 방법에 대한 더 나은 이해를 얻게 될 것입니다.
