# はじめに

この実験では、与えられた文字列が簡略化拡張 ISO 形式 (ISO 8601) の有効な日付文字列であるかどうかを判断する方法を探ります。`Date` コンストラクタとそれに関連するメソッドを使用して、文字列から `Date` オブジェクトを作成し、その有効性をチェックします。この実験の最後までに、JavaScript で日付を操作する方法と、ISO 形式を使用して日付を検証する方法についてより深い理解が得られるでしょう。
