# 소개

이 랩에서는 JavaScript 의 `mapString` 함수를 살펴봅니다. 이 함수는 주어진 문자열의 각 문자에 제공된 함수를 적용하여 새로운 문자열을 생성할 수 있게 해줍니다. `String.prototype.split()`, `Array.prototype.map()`, 그리고 `Array.prototype.join()`을 사용하여 `mapString`을 구현하는 방법을 배우고, 다양한 시나리오에서 어떻게 유용하게 사용될 수 있는지 알아보겠습니다.
