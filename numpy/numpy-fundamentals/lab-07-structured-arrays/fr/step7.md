# Accès aux champs par attribut

Nous pouvons accéder aux champs d'un tableau enregistrement par attribut au lieu d'index. Cela fournit une manière plus pratique de travailler avec des données structurées.

```python
# Accès aux champs par attribut
names = recordarr.name
```
