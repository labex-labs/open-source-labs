# Indexation avec plusieurs champs

Nous pouvons indexer un tableau structuré avec plusieurs champs en passant une liste de noms de champs. Cela renverra un nouveau tableau structuré ne contenant que les champs spécifiés.

```python
# Indexation avec plusieurs champs
subset = x[['name', 'age']]
```
