# Modificación de campos

También podemos modificar los campos individuales de un array estructurado mediante la indexación con el nombre del campo y la asignación de nuevos valores.

```python
# Modifica el campo 'age'
x['age'] = [26, 31]
```
