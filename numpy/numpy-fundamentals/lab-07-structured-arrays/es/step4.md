# Indexación con múltiples campos

Podemos indexar un array estructurado con múltiples campos pasando una lista de nombres de campo. Esto devolverá un nuevo array estructurado que contendrá solo los campos especificados.

```python
# Indexación con múltiples campos
subset = x[['name', 'age']]
```
