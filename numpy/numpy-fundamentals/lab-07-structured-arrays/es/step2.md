# Acceso a los campos

Podemos acceder a los campos individuales de un array estructurado mediante la indexación con el nombre del campo. Esto devolverá un nuevo array que contendrá solo los valores de ese campo.

```python
# Accede al campo 'name'
names = x['name']
```
