# Acceso a campos por atributo

Podemos acceder a los campos de un array de registros por atributo en lugar de por índice. Esto proporciona una forma más conveniente de trabajar con datos estructurados.

```python
# Acceso a campos por atributo
names = recordarr.name
```
