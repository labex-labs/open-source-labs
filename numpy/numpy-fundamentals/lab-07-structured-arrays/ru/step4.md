# Индексация по нескольким полям

Мы можем индексировать структурированный массив по нескольким полям, передав список имен полей. Это вернет новый структурированный массив, содержащий только указанные поля.

```python
# Индексирование по нескольким полям
subset = x[['name', 'age']]
```
