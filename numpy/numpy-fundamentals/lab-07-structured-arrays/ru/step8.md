# Преобразование структурированного массива в записной массив

Мы можем преобразовать структурированный массив в записной массив с использованием метода `view` и указанием типа `np.recarray`.

```python
# Преобразовать структурированный массив в записной массив
recordarr = x.view(np.recarray)
```
