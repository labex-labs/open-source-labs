# Доступ к полям

Мы можем получить доступ к отдельным полям структурированного массива, используя индексацию по имени поля. Это вернет новый массив, содержащий только значения этого поля.

```python
# Доступ к полю 'name'
names = x['name']
```
