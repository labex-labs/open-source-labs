# Indexando com Múltiplos Campos

Podemos indexar um array estruturado com múltiplos campos passando uma lista de nomes de campos. Isso retornará um novo array estruturado contendo apenas os campos especificados.

```python
# Index with multiple fields
subset = x[['name', 'age']]
```
