# Acessando Campos

Podemos acessar campos individuais de um array estruturado indexando com o nome do campo. Isso retornará um novo array contendo apenas os valores desse campo.

```python
# Access the 'name' field
names = x['name']
```
