# Acessando Campos por Atributo

Podemos acessar campos de um record array por atributo em vez de índice. Isso fornece uma maneira mais conveniente de trabalhar com dados estruturados.

```python
# Access fields by attribute
names = recordarr.name
```
