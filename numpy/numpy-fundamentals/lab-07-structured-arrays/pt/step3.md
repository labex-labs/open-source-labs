# Modificando Campos

Também podemos modificar campos individuais de um array estruturado indexando com o nome do campo e atribuindo novos valores.

```python
# Modify the 'age' field
x['age'] = [26, 31]
```
