# コピーの作成

コピーは、配列のデータバッファとメタデータの両方を複製することで作成できます。コピーを作成するには、`ndarray`オブジェクトの`copy()`メソッドを使用できます。

```python
import numpy as np

# 配列を作成
x = np.array([1, 2, 3, 4, 5])

# コピーを作成
y = x.copy()

# コピーを変更
y[0] = 10

# 元の配列を表示
print(x)  # 出力：[1, 2, 3, 4, 5]
```

上記の例では、コピー`y`は元の配列`x`と独立しています。
