# 동일한 형태의 배열을 사용한 브로드캐스팅

가장 간단한 경우, 두 배열은 요소별 연산을 위해 정확히 동일한 형태를 가져야 합니다. 예를 들어:

```python
import numpy as np

a = np.array([1.0, 2.0, 3.0])
b = np.array([2.0, 2.0, 2.0])
result = a * b
```

이 경우, `a`와 `b`는 동일한 형태를 가지므로 곱셈은 요소별로 수행되며 결과는 `[2.0, 4.0, 6.0]`입니다.
