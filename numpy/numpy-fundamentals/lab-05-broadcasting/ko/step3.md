# 스칼라 값과의 브로드캐스팅

브로드캐스팅은 또한 배열과 스칼라 값 간의 요소별 연산을 허용합니다. 스칼라 값은 자동으로 배열의 형태에 맞게 "확장 (stretched)"됩니다. 예를 들어:

```python
import numpy as np

a = np.array([1.0, 2.0, 3.0])
b = 2.0
result = a * b
```

이 경우, `b`는 스칼라 값이지만, `a`와 동일한 형태를 가진 배열이 되도록 확장됩니다. 그런 다음 곱셈은 요소별로 수행되어 `[2.0, 4.0, 6.0]`의 결과를 얻습니다.
