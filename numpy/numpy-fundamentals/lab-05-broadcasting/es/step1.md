# Comprendiendo la difusión (broadcasting)

La difusión (broadcasting) permite que NumPy realice operaciones elemento a elemento en matrices con diferentes formas. La matriz más pequeña se "difunde" automáticamente para que coincida con la forma de la matriz más grande. Esto se hace bajo ciertas restricciones, que exploraremos en los siguientes pasos.
