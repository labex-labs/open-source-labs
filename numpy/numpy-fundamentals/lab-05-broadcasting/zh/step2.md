# 对形状相同的数组进行广播

在最简单的情况下，两个数组必须具有完全相同的形状才能进行逐元素操作。例如：

```python
import numpy as np

a = np.array([1.0, 2.0, 3.0])
b = np.array([2.0, 2.0, 2.0])
result = a * b
```

在这种情况下，`a` 和 `b` 具有相同的形状，因此乘法是逐元素进行的，结果是 `[2.0, 4.0, 6.0]`。
