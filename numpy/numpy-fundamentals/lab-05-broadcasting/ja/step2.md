# 同じ形状の配列でのブロードキャスト

最も単純な場合、要素ごとの演算には2つの配列がまったく同じ形状である必要があります。たとえば：

```python
import numpy as np

a = np.array([1.0, 2.0, 3.0])
b = np.array([2.0, 2.0, 2.0])
result = a * b
```

この場合、`a` と `b` は同じ形状であるため、要素ごとに乗算が行われ、結果は `[2.0, 4.0, 6.0]` になります。
