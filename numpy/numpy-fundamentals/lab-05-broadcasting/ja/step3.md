# スカラー値でのブロードキャスト

ブロードキャストは、配列とスカラー値の間でも要素ごとの演算を可能にします。スカラー値は自動的に「拡張」されて配列の形状に合わせられます。たとえば：

```python
import numpy as np

a = np.array([1.0, 2.0, 3.0])
b = 2.0
result = a * b
```

この場合、`b` はスカラー値ですが、`a` と同じ形状の配列に拡張されます。その後、要素ごとに乗算が行われ、結果は `[2.0, 4.0, 6.0]` になります。
