# 一般的なブロードキャストのルール

NumPyは、2つの配列の形状を要素ごとに比較して、ブロードキャストに対して互換性があるかどうかを判断します。以下のルールが適用されます。

1. 2つの次元がサイズが等しい場合、互換性があります。
2. どちらか一方の次元がサイズ1の場合、互換性があります。

これらの条件が満たされない場合、`ValueError` が発生し、配列の形状が互換性がないことが示されます。
