# Compreendendo o Broadcasting

O broadcasting (difusão) permite que o NumPy execute operações elemento a elemento em arrays com diferentes formatos. O array menor é automaticamente "broadcast" (difundido) para corresponder ao formato do array maior. Isso é feito sob certas restrições, que exploraremos nas etapas a seguir.
