# 广播

广播是通用函数的一项强大功能，它允许对形状不同的数组执行操作。广播规则决定了在操作过程中如何处理形状不同的数组。让我们看一个例子。

```python
import numpy as np

# 创建两个数组
arr1 = np.array([1, 2, 3])
arr2 = np.array([[1], [2], [3]])

# 将数组相乘
result = arr1 * arr2

# 打印结果
print(result)
```

输出：

```
array([[1, 2, 3],
       [2, 4, 6],
       [3, 6, 9]])
```
