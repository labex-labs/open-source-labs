#!/bin/zsh

cd ~/project
git diff | grep 'arange'
git diff | grep 'linspace'
git diff | grep 'eye'
git diff | grep 'diag'
git diff | grep 'vander'
git diff | grep 'zeros'
git diff | grep 'ones'
git diff | grep 'default_rng'
