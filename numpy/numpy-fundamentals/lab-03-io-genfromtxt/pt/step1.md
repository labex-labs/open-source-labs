# Importando as Bibliotecas Necessárias

Primeiramente, vamos importar as bibliotecas necessárias. Usaremos a biblioteca `numpy` para criar arrays e o módulo `io` da biblioteca `io` para criar objetos semelhantes a arquivos (file-like objects).

```python
import numpy as np
from io import StringIO
```
