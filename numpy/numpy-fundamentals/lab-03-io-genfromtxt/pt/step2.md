# Definindo a Entrada

A função `numpy.genfromtxt` requer a fonte dos dados como o único argumento obrigatório. Pode ser uma string, uma lista de strings, um gerador ou um objeto semelhante a um arquivo aberto (open file-like object) com um método `read`.

```python
data = "1, 2, 3\n4, 5, 6"
```
