# Определение входных данных

Функция `numpy.genfromtxt` требует источника данных в качестве единственного обязательного аргумента. Это может быть строка, список строк, генератор или открытый объект, подобный файлу, с методом `read`.

```python
data = "1, 2, 3\n4, 5, 6"
```
