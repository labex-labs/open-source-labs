# Выбор столбцов

Аргумент `usecols` используется для выбора тех столбцов, которые необходимо импортировать. Он принимает целое число или последовательность целых чисел, соответствующих индексам столбцов для импорта.

```python
np.genfromtxt(StringIO(data), usecols=(0, -1))
```
