# Импорт необходимых библиотек

Сначала импортируем необходимые библиотеки. Мы будем использовать библиотеку `numpy` для создания массивов и модуль `io` из библиотеки `io` для создания объектов, подобных файлам.

```python
import numpy as np
from io import StringIO
```
