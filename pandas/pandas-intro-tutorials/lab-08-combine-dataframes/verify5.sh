#!/bin/zsh

cd ~/project
git diff | grep 'read_csv'
git diff | grep 'merge'
git diff | grep 'how'
git diff | grep 'left'
git diff | grep 'left_on'
git diff | grep 'parameter'
git diff | grep 'right_on'
git diff | grep 'id'
