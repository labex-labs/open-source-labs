# DataFrameまたはSeriesにIntegerArrayを格納する

`IntegerArray`を作成したら、それを`DataFrame`または`Series`に格納することができます。作成した`IntegerArray`から`Series`を作成してみましょう。

```python
# IntegerArrayからSeriesを作成
series = pd.Series(arr)
```
