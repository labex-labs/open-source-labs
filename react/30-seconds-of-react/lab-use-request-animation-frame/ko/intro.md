# 소개

이 랩에서는 React 에서 `useRequestAnimationFrame` 훅을 사용하여 함수를 애니메이션하는 방법을 살펴봅니다. 이 훅은 각 리페인트 전에 실행되도록 보장합니다. 이 훅은 웹 애플리케이션에서 부드럽고 효율적인 애니메이션을 생성하는 데 유용합니다. 훅을 생성하고 실시간으로 업데이트되는 간단한 카운터 컴포넌트에 구현하는 과정을 안내합니다.
