# はじめに

この実験では、React において制御されていない `<textarea>` 要素を作成する方法を学びます。この実験の目的は、`defaultValue` と `onChange` のプロップを使用して、テキストエリアの値を親コンポーネント内のコールバック関数に渡す方法を示すことです。この実験が終了するまでに、React において制御されていない入力フィールドを作成する方法をより深く理解するようになります。
