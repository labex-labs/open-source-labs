# 소개

이 랩에서는 React 에서 `useState` 및 `useEffect` 훅을 사용하여 파일 드래그 앤 드롭 (drag and drop) 컴포넌트를 만드는 방법을 배웁니다. 이 컴포넌트를 사용하면 사용자가 지정된 영역으로 파일을 쉽게 드래그 앤 드롭할 수 있으며, 드롭된 파일을 인수로 콜백 함수를 트리거합니다. 이 랩을 마치면 React 에서 드래그 앤 드롭 기능을 처리하는 방법에 대한 이해도가 높아질 것입니다.
