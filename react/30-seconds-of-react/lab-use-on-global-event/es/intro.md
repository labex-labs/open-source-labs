# Introducción

En este laboratorio, exploraremos el hook `useOnGlobalEvent` en React. Este hook nos permite escuchar eventos que ocurren en el objeto global, como el evento `mousemove` en el objeto `window`. Al utilizar este hook, podemos ejecutar fácilmente una función de devolución de llamada cada vez que ocurre un evento global específico.
