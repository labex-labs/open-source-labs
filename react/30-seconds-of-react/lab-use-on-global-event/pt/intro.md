# Introdução

Neste laboratório, exploraremos o hook `useOnGlobalEvent` em React. Este hook nos permite escutar eventos que ocorrem no objeto global, como o evento `mousemove` no objeto `window`. Ao usar este hook, podemos facilmente executar uma função de callback sempre que um evento global específico ocorrer.
