# 소개

이 랩에서는 React 에서 `MutationObserver`를 사용하여 DOM 트리에 발생한 변경 사항을 감지하는 방법, 즉 `useMutationObserver` 훅을 사용하는 방법을 배웁니다. 이 훅을 사용하면 변경 사항이 감지될 때 실행될 콜백 함수를 지정할 수 있으며, 옵션을 제공하여 옵저버 (observer) 의 동작을 사용자 정의할 수도 있습니다. 이 랩을 통해 React 애플리케이션에서 `useMutationObserver` 훅을 구현하는 방법을 이해할 수 있습니다.
