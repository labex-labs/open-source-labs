# 简介

在本实验中，我们将学习如何在 React 中使用 `useNavigatorOnLine` 钩子来检查客户端是在线还是离线。我们将创建一个函数，使用 `Navigator.onLine` Web API 获取客户端的在线状态，使用 `useState()` 钩子创建一个合适的状态变量，并使用 `useEffect()` 钩子为适当的事件添加监听器，以更新状态并在卸载时清理这些监听器。最后，我们将返回在线状态状态变量，以根据当前在线状态显示一条消息。
