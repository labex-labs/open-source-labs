# 소개

이 랩에서는 React 에서 `useError` 훅을 사용하여 에러 디스패처를 만드는 방법을 배웁니다. 이 훅을 사용하면 에러를 저장하는 상태 변수를 생성하고, 해당 변수가 truthy 할 때마다 에러를 throw 할 수 있습니다. 또한 `useCallback` 훅을 사용하여 상태를 업데이트하고 캐시된 함수를 반환합니다. 이 랩을 마치면 React 애플리케이션에서 에러를 효과적으로 처리할 수 있게 됩니다.
