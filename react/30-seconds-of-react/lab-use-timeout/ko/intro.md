# 소개

이 랩에서는 React 의 `useTimeout` 커스텀 훅을 사용하여 `setTimeout()` 함수를 선언적으로 구현하는 방법을 살펴봅니다. 이 훅을 사용하면 타임아웃을 설정하고 쉽게 정리할 수 있으며, 최신 콜백 함수도 기억할 수 있습니다. 또한 `useTimeout` 훅을 사용하여 매 초마다 컴포넌트의 상태를 업데이트하는 1 초 타이머를 만드는 예시도 살펴보겠습니다.
