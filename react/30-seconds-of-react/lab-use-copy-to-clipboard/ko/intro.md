# 소개

이 랩에서는 React 에서 주어진 텍스트를 클립보드에 복사하는 `useCopyToClipboard` 훅을 사용하는 방법을 배웁니다. 이 훅은 웹 애플리케이션에서 "클립보드에 복사" 기능을 구현하는 데 유용합니다. 또한 이 구현에서 `useState`, `useCallback`, 그리고 `useEffect` 훅을 사용하는 방법도 살펴볼 것입니다.
