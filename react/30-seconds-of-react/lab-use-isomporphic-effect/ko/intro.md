# 소개

이 랩에서는 React 의 `useIsomorphicEffect` 훅 사용법을 살펴봅니다. 이 훅은 서버에서 `useEffect()`를, 클라이언트에서 `useLayoutEffect()`를 실행하여 환경에 관계없이 동일한 효과가 실행되도록 합니다. 이 랩을 통해 `useIsomorphicEffect` 훅을 사용하여 React 애플리케이션에서 일관되고 신뢰할 수 있는 효과를 만드는 방법을 배우게 됩니다.
