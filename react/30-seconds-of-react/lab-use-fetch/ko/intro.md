# 소개

이 랩에서는 React 의 `useFetch` 훅에 대해 배우겠습니다. 이 훅을 사용하면 `fetch()` 메서드를 선언적으로 구현하여 API 에서 데이터를 가져오고 컴포넌트 상태를 업데이트하는 것이 더 쉬워집니다. URL 과 옵션을 인수로 받아 비동기적으로 `fetch()`를 호출하여 응답, 오류 및 중단 상태 변수를 업데이트하는 사용자 정의 훅을 만들 것입니다.
