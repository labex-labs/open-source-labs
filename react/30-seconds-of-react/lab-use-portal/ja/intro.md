# はじめに

この実験では、React における `usePortal` フックの使用方法を検討します。このフックの目的は、親コンポーネントの外で子コンポーネントをレンダリングできるポータルを作成することです。`useState()`、`useCallback()`、および `useEffect()` フックを使用してポータルを作成および管理する方法、ならびに `ReactDOM.createPortal()` と `ReactDOM.unmountComponentAtNode()` を使用してポータルをレンダリングおよび削除する方法を学びます。
