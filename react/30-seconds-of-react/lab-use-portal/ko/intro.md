# 소개

이 랩에서는 React 의 `usePortal` 훅 사용법을 살펴봅니다. 이 훅의 목적은 부모 컴포넌트 외부에서 자식 요소를 렌더링할 수 있도록 하는 포털을 생성하는 것입니다. `useState()`, `useCallback()`, 그리고 `useEffect()` 훅을 사용하여 포털을 생성하고 관리하는 방법, 그리고 `ReactDOM.createPortal()`과 `ReactDOM.unmountComponentAtNode()`를 사용하여 포털을 렌더링하고 제거하는 방법을 배우게 됩니다.
