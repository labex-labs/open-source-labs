# 简介

在本实验中，我们将探索在 React 中使用 `usePortal` 钩子。此钩子的目的是创建一个门户，允许在父组件外部渲染子组件。我们将学习如何使用 `useState()`、`useCallback()` 和 `useEffect()` 钩子来创建和管理门户，以及如何使用 `ReactDOM.createPortal()` 和 `ReactDOM.unmountComponentAtNode()` 来渲染和移除门户。
