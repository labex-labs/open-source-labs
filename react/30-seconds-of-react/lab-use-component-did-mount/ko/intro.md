# 소개

이 랩에서는 React 에서 `useComponentDidMount` 훅을 사용하는 방법을 배우겠습니다. 이 훅은 클래스 컴포넌트의 `componentDidMount()` 메서드와 유사하게 컴포넌트가 마운트된 직후 콜백을 실행할 수 있게 해줍니다. 이 랩을 마치면 이 훅을 사용하는 방법과 React 프로젝트에서 어떻게 유용하게 사용할 수 있는지 더 잘 이해하게 될 것입니다.
