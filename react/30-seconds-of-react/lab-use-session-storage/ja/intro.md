# はじめに

この実験では、React で `useSessionStorage` フックを使用して状態値を `sessionStorage` に永続化する方法を学びます。値を遅延初期化する方法、`Storage.getItem()` と `Storage.setItem()` を使用して値を取得および保存する方法、および定義された関数を使用して状態変数を更新する方法を学びます。この実験が終了するとき、React アプリケーション内で `sessionStorage` を使用してデータを保存および取得する方法をより深く理解しているでしょう。
