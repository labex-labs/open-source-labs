```shell
git pull
```

```shell
git checkout b00b937
# Assuming the remote `master` branch is ahead of the local one
git checkout master
git pull # The local `master` branch is now up to date with the remote branch
```
