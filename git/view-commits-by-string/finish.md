# Summary

Using the `git log -S` command, you can easily find all the commits that modified a specific string in your codebase. This can be useful when debugging issues or tracking down the source of a bug. Remember to use the arrow keys to navigate through the list of commits and press <kbd>Q</kbd> to exit the log.
