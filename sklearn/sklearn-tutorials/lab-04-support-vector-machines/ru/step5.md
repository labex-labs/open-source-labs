# Регрессия с использованием SVM

- Для задач регрессии методы SVM можно использовать с классом `SVR`:

```python
X = [[0, 0], [1, 1]]
y = [0.5, 2.5]
regr = svm.SVR()
regr.fit(X, y)
regr.predict([[1, 1]])
```
