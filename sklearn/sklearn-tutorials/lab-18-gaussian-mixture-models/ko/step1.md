# 필요한 라이브러리 가져오기

가우시안 혼합 모델 (Gaussian mixture models) 을 위한 `sklearn.mixture`와 데이터 전처리 및 시각화에 필요한 다른 라이브러리를 가져오겠습니다.

```python
from sklearn.mixture import GaussianMixture
import numpy as np
import matplotlib.pyplot as plt
```
