# カラーマップを作成する

MatplotlibのListedColormap関数を使って、可視化の目的で2つのカラーマップを作成します。

```python
cmap_light = ListedColormap(["orange", "cyan", "cornflowerblue"])
cmap_bold = ListedColormap(["darkorange", "c", "darkblue"])
```
