# 必要なライブラリをインポートする

まずは、`numpy`、`matplotlib`、および`sklearn`を含む必要なライブラリをインポートします。

```python
import time
import matplotlib.pyplot as plt
import numpy as np

from sklearn.cluster import AgglomerativeClustering
from sklearn.neighbors import kneighbors_graph
```
