# Zusammenfassung

In diesem Lab haben wir den Local Outlier Factor (LOF)-Algorithmus verwendet, um eine Novelty Detection durchzuführen. Wir haben Daten für das Training, das Testing und die Ausreißer generiert, das LOF-Modell trainiert, das Modell ausgewertet und die Ergebnisse visualisiert.