# 导入库

我们将首先导入必要的库。我们将使用`numpy`、`matplotlib.pyplot`、`make_blobs`以及`sklearn.preprocessing`模块中的`KBinsDiscretizer`。

```python
import numpy as np
import matplotlib.pyplot as plt
from sklearn.preprocessing import KBinsDiscretizer
from sklearn.datasets import make_blobs
```
