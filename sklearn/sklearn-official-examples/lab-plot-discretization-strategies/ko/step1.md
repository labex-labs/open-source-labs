# 라이브러리 가져오기

필요한 라이브러리를 가져오는 것으로 시작합니다. `numpy`, `matplotlib.pyplot`, `make_blobs`, 그리고 `sklearn.preprocessing` 모듈의 `KBinsDiscretizer`를 사용할 것입니다.

```python
import numpy as np
import matplotlib.pyplot as plt
from sklearn.preprocessing import KBinsDiscretizer
from sklearn.datasets import make_blobs
```
