# まとめ

この実験では、Python の Scikit-learn ライブラリを使って接続制約付きの階層的クラスタリングを行う方法を示しました。まず、スイスロールデータセットを生成し、非構造化階層的クラスタリングを行いました。次に、10 個の近傍点を持つ k 近傍法を定義し、構造化階層的クラスタリングを行いました。最後に、`matplotlib`ライブラリを使って非構造化および構造化階層的クラスタをプロットしました。
