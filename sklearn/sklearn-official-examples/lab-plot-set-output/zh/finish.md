# 总结

在本实验中，我们学习了如何使用Scikit-Learn中的`set_output` API来配置转换器，使其输出pandas DataFrame。我们展示了如何配置一个估计器以输出DataFrame，配置一个管道以输出DataFrame，以及使用`set_config`全局配置`set_output`。我们还学习了如何使用`config_context`配置`set_output`。
