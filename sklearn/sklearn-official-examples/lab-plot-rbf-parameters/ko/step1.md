# 데이터셋 로드 및 준비

- scikit-learn 에서 iris 데이터셋을 로드합니다.
- 데이터를 특징 행렬 `X`와 목표 벡터 `y`로 분리합니다.
- `StandardScaler`를 사용하여 특징 행렬 `X`를 표준화합니다.
- 의사결정 함수 시각화를 위해 데이터셋의 단순화된 버전을 만듭니다. `X`에서 처음 두 개의 특징만 유지하고, 데이터셋에서 두 개의 클래스만 유지하여 이진 분류 문제로 만듭니다.
