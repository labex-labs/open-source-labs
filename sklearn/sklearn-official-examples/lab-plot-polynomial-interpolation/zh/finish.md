# 总结

在本实验中，我们学习了如何使用岭回归用特定次数的多项式来逼近一个函数。给定一维点 `x_i` 的 `n_samples`，我们展示了两种不同的方法来实现这一点。我们使用 `make_pipeline` 函数来添加非线性特征，并演示了这些变换器如何非常适合用线性模型来建模非线性效应。我们绘制了函数、训练点以及使用多项式特征和B样条的插值。我们还分别绘制了两个变换器的所有列，并展示了样条的节点。最后，我们演示了周期样条的用法。
