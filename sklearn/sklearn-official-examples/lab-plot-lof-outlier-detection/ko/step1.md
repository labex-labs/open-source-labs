# 라이브러리 가져오기

데이터 조작 및 시각화를 위해 각각 `numpy`와 `matplotlib`를 가져올 것입니다. 또한 이상치 탐지를 위해 `sklearn.neighbors`에서 `LocalOutlierFactor`를 가져올 것입니다.

```python
import numpy as np
import matplotlib.pyplot as plt
from sklearn.neighbors import LocalOutlierFactor
```
