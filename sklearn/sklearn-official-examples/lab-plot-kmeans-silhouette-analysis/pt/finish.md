# Resumo

Neste laboratório, utilizamos o Método Silhouette para determinar o número ideal de clusters para o algoritmo KMeans. Gerámos dados de amostra usando a função `make_blobs` e plotamos as pontuações silhouette para uma gama de valores para `n_clusters`. Interpretamos os resultados e escolhemos o valor ideal para `n_clusters`.
