# 라이브러리 가져오기

이 단계에서는 필요한 라이브러리인 `numpy`, `matplotlib`, `gridspec` 및 `sklearn.mixture`에서 `BayesianGaussianMixture`를 가져옵니다.

```python
import numpy as np
import matplotlib.pyplot as plt
import matplotlib.gridspec as gridspec
from sklearn.mixture import BayesianGaussianMixture
```
