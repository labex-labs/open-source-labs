# ライブラリのインポート

このステップでは、必要なライブラリをインポートします。それは、`numpy`、`matplotlib`、`gridspec`、および`sklearn.mixture`からの`BayesianGaussianMixture`です。

```python
import numpy as np
import matplotlib.pyplot as plt
import matplotlib.gridspec as gridspec
from sklearn.mixture import BayesianGaussianMixture
```
