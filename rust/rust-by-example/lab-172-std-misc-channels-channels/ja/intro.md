# はじめに

この実験では、Rust の非同期チャネルについて学びます。これは、提供された Rust のコードスニペットに示すように、`Sender`と`Receiver`のエンドポイントを使ってスレッド間で双方向通信を可能にします。

> **注：** 実験でファイル名が指定されていない場合、好きなファイル名を使うことができます。たとえば、`main.rs`を使って、`rustc main.rs &&./main`でコンパイルして実行することができます。
