# はじめに

この実験では、Rust における`super`と`self`キーワードの使い方を調べます。これらのキーワードは、アイテムにアクセスする際の曖昧さを解消し、ハードコードされたパスを避けるために使用されます。

> **注：** 実験でファイル名が指定されていない場合、好きなファイル名を使用できます。たとえば、`main.rs`を使用して、`rustc main.rs &&./main` でコンパイルして実行することができます。
