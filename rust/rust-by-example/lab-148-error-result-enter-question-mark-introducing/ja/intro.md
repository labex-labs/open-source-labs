# はじめに

この実験では、Rustの`?`演算子について学びます。この演算子を使うことで、パニックする代わりに`Err`を返すことでエラー処理を簡略化できます。また、`?`が導入される前に同様の機能を実現していた`try!`マクロについても検討します。

> **注:** 実験でファイル名が指定されていない場合、好きなファイル名を使うことができます。たとえば、`main.rs`を使って、`rustc main.rs &&./main`でコンパイルして実行することができます。
