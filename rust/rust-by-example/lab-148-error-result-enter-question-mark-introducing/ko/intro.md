# 소개

이 랩에서는 Rust 의 `?` 연산자에 대해 배우고, 이 연산자를 통해 패닉 (panic) 대신 `Err`를 반환하여 오류 처리를 간소화하는 방법을 익힐 것입니다. 또한, `?`가 도입되기 전에 유사한 기능을 수행했던 `try!` 매크로도 살펴볼 것입니다.

> **참고:** 랩에서 파일 이름을 지정하지 않은 경우, 원하는 파일 이름을 사용할 수 있습니다. 예를 들어, `main.rs`를 사용하고, `rustc main.rs && ./main`으로 컴파일하고 실행할 수 있습니다.
