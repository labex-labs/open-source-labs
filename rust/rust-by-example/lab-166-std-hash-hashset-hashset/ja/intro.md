# はじめに

この実験では、Rust における `HashSet` データ構造の機能を調べます。このデータ構造は一意の要素を保証し、集合に対して和集合、差集合、積集合、対称差などの操作を行うことができます。

> **注:** 実験でファイル名が指定されていない場合、好きなファイル名を使用できます。たとえば、`main.rs` を使用して、`rustc main.rs &&./main` でコンパイルして実行することができます。
