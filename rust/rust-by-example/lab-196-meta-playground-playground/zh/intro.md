# 简介

在本实验中，你将了解 Rust 在线编译器（Rust Playground），这是一个允许你试验 Rust 代码的 Web 界面。你还将发现如何将其与 `mdbook` 配合使用，以使代码示例可运行和可编辑。此外，你将探索如何将其与文档配合使用，并通过在 Rust 代码中使用 `html_playground_url` 属性来启用 “运行” 按钮。

> **注意**：如果实验未指定文件名，你可以使用任何你想要的文件名。例如，你可以使用 `main.rs`，并通过 `rustc main.rs &&./main` 进行编译和运行。
