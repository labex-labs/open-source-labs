# 소개

이 실습에서는 Rust 에서 `while let`의 사용법을 보여줍니다. `while let`은 변수를 증가시키거나 Option 타입을 반복할 때 `match` 문열을 사용하는 것보다 더 간결하고 효율적인 대안입니다.

> **참고:** 실습에서 파일 이름을 지정하지 않으면 원하는 파일 이름을 사용할 수 있습니다. 예를 들어 `main.rs`를 사용하고 `rustc main.rs && ./main`으로 컴파일 및 실행할 수 있습니다.
