# はじめに

この実験では、Rust における`while let`の使い方を示します。変数をインクリメントする場合や`Option`型を反復処理する際に、`match`シーケンスを使うよりも、より簡潔で効率的な代替手段としての使い方です。

> **注：** 実験でファイル名が指定されていない場合、好きなファイル名を使うことができます。たとえば、`main.rs`を使って、`rustc main.rs &&./main`でコンパイルして実行することができます。
