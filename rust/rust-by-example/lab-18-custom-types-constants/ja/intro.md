# はじめに

この実験では、Rust における定数の概念について学びます。定数は `const` または `static` キーワードを使って宣言でき、明示的な型注釈を付けることができ、グローバルスコープを含む任意のスコープでアクセスできます。

> **注:** 実験でファイル名が指定されていない場合、好きなファイル名を使うことができます。たとえば、`main.rs` を使って、`rustc main.rs &&./main` でコンパイルして実行することができます。
