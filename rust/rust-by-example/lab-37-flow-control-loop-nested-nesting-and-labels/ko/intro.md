# 소개

이 실습에서는 Rust 에서 중첩 및 레이블이 지정된 루프에 대해 배우고, 레이블을 사용하여 중첩된 루프 내에서 외부 루프를 종료하거나 건너뛰기 위해 `break` 및 `continue` 문을 사용하는 방법을 살펴봅니다.

> **참고:** 실습에서 파일 이름을 지정하지 않으면 원하는 파일 이름을 사용할 수 있습니다. 예를 들어 `main.rs`를 사용하고 `rustc main.rs && ./main`으로 컴파일 및 실행할 수 있습니다.
