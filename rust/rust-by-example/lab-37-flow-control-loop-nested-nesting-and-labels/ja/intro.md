# はじめに

この実験では、Rust でのループのネストとラベル付けについて学びます。ここでは、ループにラベルを付けることで、`break` および `continue` 文を使用して、ネストされたループ内の外側のループを終了またはスキップすることができます。

> **注意：** 実験でファイル名が指定されていない場合は、任意のファイル名を使用できます。たとえば、`main.rs` を使用し、`rustc main.rs &&./main` でコンパイルして実行することができます。
