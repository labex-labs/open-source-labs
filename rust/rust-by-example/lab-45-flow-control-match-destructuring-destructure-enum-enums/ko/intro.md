# 소개

이 실습에서는 Rust 에서 열거형 (enum) 의 사용법을 보여주는 코드를 제공합니다. `match` 문을 사용하여 열거형을 분해하고, 열거형 변형에 따라 연관된 값에 접근하는 방법을 포함합니다.

> **참고:** 실습에서 파일 이름을 지정하지 않으면 원하는 파일 이름을 사용할 수 있습니다. 예를 들어 `main.rs`를 사용하고 `rustc main.rs && ./main` 명령어로 컴파일 및 실행할 수 있습니다.
