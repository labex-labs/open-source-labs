# はじめに

この実験では、Rust における列挙型の使用方法を示すコードを用意しています。これには、match 文を使って列挙型を分解し、列挙型のバリアントに基づいて関連付けられた値にアクセスする方法も含まれます。

> **注記：** 実験でファイル名が指定されていない場合、好きなファイル名を使うことができます。たとえば、`main.rs` を使って、`rustc main.rs &&./main` でコンパイルして実行することができます。
