# 简介

在本实验中，将演示 Rust 中 `let`-`else` 的用法。其中，可反驳模式能够匹配并绑定周围作用域中的变量，若模式不匹配，则可通过使用 `break`、`return` 或 `panic!` 等语句来使程序发散。在处理模式匹配和错误处理场景时，这种结构能让代码简洁且易读，无需重复代码块或使用外部 `let` 语句。

> **注意**：如果实验未指定文件名，你可以使用任何你想要的文件名。例如，你可以使用 `main.rs`，并通过 `rustc main.rs &&./main` 进行编译和运行。
