# はじめに

この実験では、Rust プロジェクトで広く使用されている Rust パッケージマネージャである Cargo を使用する場合、Rust の `crate_type` および `crate_name` 属性が効果を持たないことが説明されます。

> **注記：** 実験でファイル名が指定されていない場合、好きなファイル名を使用できます。たとえば、`main.rs` を使用して、`rustc main.rs &&./main` でコンパイルして実行できます。
