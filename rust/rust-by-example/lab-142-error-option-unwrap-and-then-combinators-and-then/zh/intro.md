# 简介

在本实验中，当处理 `Option<T>` 时，`and_then()` 组合器作为 `map()` 的可链式替代方法被引入，因为它避免了嵌套 `Option<Option<T>>` 并简化了代码的可读性。

> **注意**：如果实验未指定文件名，你可以使用任何你想要的文件名。例如，你可以使用 `main.rs`，通过 `rustc main.rs &&./main` 进行编译和运行。
