# 소개

이 랩에서는 `Option<T>`를 사용할 때 `map()`의 체인 가능한 대안으로 `and_then()` 컴비네이터를 소개합니다. 이는 `Option<Option<T>>`의 중첩을 피하고 코드 가독성을 향상시킵니다.

> **참고:** 랩에서 파일 이름을 지정하지 않은 경우, 원하는 파일 이름을 사용할 수 있습니다. 예를 들어, `main.rs`를 사용하고 `rustc main.rs && ./main`으로 컴파일하고 실행할 수 있습니다.
