# 简介

在这个实验中，我们将学习在单个变量解构时的部分移动，其中可以同时使用 `按值移动` 和 `按引用` 模式绑定。这会导致变量的部分移动，允许某些部分被移动，而其他部分仍可被引用。如果父变量被部分移动，那么之后它就不能作为一个整体使用了，但仅被引用而未被移动的部分仍然可以使用。

> **注意**：如果实验未指定文件名，你可以使用任何你想要的文件名。例如，你可以使用 `main.rs`，并通过 `rustc main.rs &&./main` 来编译和运行它。
