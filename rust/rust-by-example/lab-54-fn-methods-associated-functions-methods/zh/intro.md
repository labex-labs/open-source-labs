# 简介

在本实验中，我们定义了一个带有关联函数 `origin()` 和 `new()` 的 `Point` 结构体，以及一个带有方法 `area()`、`perimeter()` 和 `translate()` 的 `Rectangle` 结构体。我们还引入了一个带有方法 `destroy()` 的 `Pair` 结构体。我们通过创建结构体实例并在其上调用相应的函数和方法来演示如何使用这些函数和方法。

> **注意**：如果实验未指定文件名，你可以使用任何你想要的文件名。例如，你可以使用 `main.rs`，并通过 `rustc main.rs &&./main` 进行编译和运行。
