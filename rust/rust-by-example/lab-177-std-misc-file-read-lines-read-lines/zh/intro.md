# 简介

在这个实验中，我们得到了一个在 Rust 中从文件读取行的简单实现和一个更高效的实现。简单的方法使用 `read_to_string` 将文件读入单个字符串，然后将其拆分为行，而更高效的方法使用 `BufReader` 逐行读取文件，而不将整个内容加载到内存中。

> **注意**：如果实验未指定文件名，你可以使用任何你想要的文件名。例如，你可以使用 `main.rs`，通过 `rustc main.rs &&./main` 进行编译和运行。
