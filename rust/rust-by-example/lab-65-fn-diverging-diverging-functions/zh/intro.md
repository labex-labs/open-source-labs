# 简介

在本实验中，我们将了解 Rust 中使用 `!` 标记的发散函数。发散函数永远不会返回，其返回类型为空类型。这与只有一个可能值的 `()` 类型不同。当需要转换为任何其他类型时，例如在 `match` 分支中，发散函数可能会很有用。它们也是永远循环或终止进程的函数的返回类型。

> **注意**：如果实验未指定文件名，你可以使用任何你想要的文件名。例如，你可以使用 `main.rs`，并通过 `rustc main.rs &&./main` 进行编译和运行。
