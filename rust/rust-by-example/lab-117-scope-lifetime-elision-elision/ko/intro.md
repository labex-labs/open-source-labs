# 소개

이 랩에서는 Rust 의 생략 (elision) 개념을 탐구합니다. 여기서 borrow checker 는 가독성을 향상시키고 타이핑을 줄이기 위해 일반적인 lifetime 패턴의 생략을 허용합니다.

> **참고:** 랩에서 파일 이름을 지정하지 않은 경우, 원하는 파일 이름을 사용할 수 있습니다. 예를 들어, `main.rs`를 사용하고 `rustc main.rs && ./main`으로 컴파일하고 실행할 수 있습니다.
