# 소개

이 실습에서는 `impl Trait`과 유효한 트레이트 (`Fn`, `FnMut`, `FnOnce`) 를 지정하여 클로저를 입력 매개변수로 사용하고 출력 매개변수로 반환할 수 있다는 것을 배우게 됩니다. `move` 키워드는 모든 캡처가 값으로 발생하여 잘못된 참조를 방지하는 데 사용됩니다.

> **참고:** 실습에서 파일 이름을 지정하지 않으면 원하는 파일 이름을 사용할 수 있습니다. 예를 들어 `main.rs`를 사용하고 `rustc main.rs && ./main`으로 컴파일 및 실행할 수 있습니다.
