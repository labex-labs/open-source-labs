# 简介

在本实验中，我们了解到闭包可以用作输入参数，也可以通过使用 `impl Trait` 并指定有效的 trait（`Fn`、`FnMut`、`FnOnce`）作为输出参数返回。`move` 关键字用于指示所有捕获都是按值进行的，从而避免无效引用。

> **注意**：如果实验未指定文件名，你可以使用任何你想要的文件名。例如，你可以使用 `main.rs`，并通过 `rustc main.rs &&./main` 进行编译和运行。
