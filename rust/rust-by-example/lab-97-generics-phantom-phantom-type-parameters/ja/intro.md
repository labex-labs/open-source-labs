# はじめに

この実験では、コンパイル時に静的にチェックされ、ランタイムの動作や値を持たないファントム型パラメータの概念を探ります。Rustでは、`std::marker::PhantomData` とファントム型パラメータの概念を組み合わせて、異なるデータ型を含むタプルや構造体を作成することで、その使い方を示します。

> **注:** 実験でファイル名が指定されていない場合は、好きなファイル名を使うことができます。たとえば、`main.rs` を使って、`rustc main.rs &&./main` でコンパイルして実行することができます。
