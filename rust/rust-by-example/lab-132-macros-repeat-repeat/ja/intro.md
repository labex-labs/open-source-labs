# はじめに

この実験では、引数リストにおける `+` と `*` の記号を使用して繰り返しを示すマクロの使い方について学びます。具体的には、提供された例では、コンマで区切られた1つ以上の式を一致させるために `$(...),+` をどのように使用するかを示しています。マクロ `find_min!` は、可変個の引数の中で最小値を計算します。

> **注:** 実験でファイル名が指定されていない場合、好きなファイル名を使用できます。たとえば、`main.rs` を使用して、`rustc main.rs &&./main` でコンパイルして実行することができます。
