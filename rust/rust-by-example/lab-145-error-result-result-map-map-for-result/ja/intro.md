# はじめに

この実験では、Rust の `Result` 型に対して `map` や `and_then` などのコンビネータをどのように使ってエラーをもっとスマートに処理するかを検討します。提供されたサンプルに示すようになります。

> **注：** 実験でファイル名が指定されていない場合、好きなファイル名を使うことができます。たとえば、`main.rs` を使って、`rustc main.rs &&./main` でコンパイルして実行することができます。
