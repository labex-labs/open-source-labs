# 소개

이 랩에서는 Rust 의 `Result` 타입에 대한 `map` 및 `and_then`과 같은 콤비네이터 (combinator) 를 사용하여 오류를 보다 우아하게 처리하는 방법을 살펴봅니다. 제공된 예제에서 이를 확인할 수 있습니다.

> **참고:** 랩에서 파일 이름을 지정하지 않은 경우, 원하는 파일 이름을 사용할 수 있습니다. 예를 들어, `main.rs`를 사용하고 `rustc main.rs && ./main`으로 컴파일하고 실행할 수 있습니다.
