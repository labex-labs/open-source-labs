# 简介

在本实验中，我们将学习 Rust 中的子进程，使用 `process::Output` 结构体来表示已完成的子进程的输出，并使用 `process::Command` 结构体来构建进程。示例代码展示了如何执行 `rustc --version` 命令，并通过检查进程是否成功来相应地处理输出。

> **注意**：如果实验未指定文件名，你可以使用任何你想要的文件名。例如，你可以使用 `main.rs`，并通过 `rustc main.rs &&./main` 进行编译和运行。
