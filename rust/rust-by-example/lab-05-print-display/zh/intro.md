# 简介

在本实验中，你将学习如何在 Rust 中实现 `fmt::Display` 特性，以自定义结构体的输出外观。你还将探讨 `fmt::Display` 和 `fmt::Debug` 之间的区别，以及 `fmt::Display` 对通用容器类型的局限性。最后，你将进行一项活动，为新的 `Complex` 结构体实现 `fmt::Display` 特性，并以特定格式打印它。

> **注意**：如果实验未指定文件名，你可以使用任何你想要的文件名。例如，你可以使用 `main.rs`，并通过 `rustc main.rs &&./main` 进行编译和运行。
