# 소개

이 실험에서는 `conditional_function()`이라는 함수가 포함된 코드 조각이 있습니다. 하지만 `rustc`에 `--cfg` 플래그를 사용하여 사용자 정의 조건인 `some_condition`이 전달될 경우에만 컴파일 및 실행됩니다.

> **참고:** 실험에서 파일 이름을 지정하지 않으면 원하는 파일 이름을 사용할 수 있습니다. 예를 들어 `main.rs`를 사용하고 `rustc main.rs && ./main`으로 컴파일 및 실행할 수 있습니다.
