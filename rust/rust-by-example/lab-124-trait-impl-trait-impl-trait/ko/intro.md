# 소개

이 랩에서는 Rust 의 `impl Trait` 사용법을 살펴봅니다. `impl Trait`는 함수 선언을 단순화하고 더 유연하고 간결한 코드를 작성할 수 있도록 인자 타입 또는 반환 타입으로 사용할 수 있습니다.

> **참고:** 랩에서 파일 이름을 지정하지 않은 경우, 원하는 파일 이름을 사용할 수 있습니다. 예를 들어, `main.rs`를 사용하고 `rustc main.rs && ./main`으로 컴파일하고 실행할 수 있습니다.
