# 简介

在本实验中，记录 Rust 项目的主要方式是通过用文档注释来注释源代码，这些注释是用 CommonMark Markdown 规范编写的，并且支持其中的代码块。Rust 会确保正确性，并且这些代码块会被编译并用作文档测试。当使用 `cargo test` 命令时，这些测试会自动运行。文档测试背后的动机是作为示例来展示功能，并允许将文档中的示例用作完整的代码片段。

> **注意**：如果实验未指定文件名，你可以使用任何你想要的文件名。例如，你可以使用 `main.rs`，并用 `rustc main.rs &&./main` 进行编译和运行。
