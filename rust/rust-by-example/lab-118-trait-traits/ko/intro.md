# 소개

이 랩에서는 Rust 의 트레이트 (trait) 개념을 탐구합니다. 트레이트는 `Self`라고 불리는 알 수 없는 타입에 대해 정의된 메서드의 모음입니다. 트레이트는 모든 데이터 타입에 대해 구현될 수 있으며, 트레이트 내에 정의된 메서드를 사용할 수 있도록 합니다.

> **참고:** 랩에서 파일 이름을 지정하지 않은 경우, 원하는 파일 이름을 사용할 수 있습니다. 예를 들어, `main.rs`를 사용하고 `rustc main.rs && ./main`으로 컴파일하고 실행할 수 있습니다.
