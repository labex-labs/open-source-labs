# はじめに

この実験では、Rust におけるトレイトの概念を探ります。トレイトは、`Self` と呼ばれる未知の型に対して定義されたメソッドのコレクションです。トレイトは任意のデータ型に対して実装でき、トレイト内で定義されたメソッドを使用できるようになります。

> **注：** 実験でファイル名が指定されていない場合、好きなファイル名を使用できます。たとえば、`main.rs` を使用して、`rustc main.rs &&./main` でコンパイルして実行できます。
