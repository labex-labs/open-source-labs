# Introducción

En este laboratorio, aprendemos sobre la función `Iterator::find` en Rust, que itera sobre un iterador y busca el primer valor que satisface una condición dada. Si ninguno de los valores satisface la condición, devuelve `None`.

> **Nota:** Si el laboratorio no especifica un nombre de archivo, puede usar cualquier nombre de archivo que desee. Por ejemplo, puede usar `main.rs`, compilar y ejecutarlo con `rustc main.rs &&./main`.
