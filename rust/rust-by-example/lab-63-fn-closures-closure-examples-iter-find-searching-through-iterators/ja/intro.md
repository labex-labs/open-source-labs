# はじめに

この実験では、Rust の `Iterator::find` 関数について学びます。この関数はイテレータを反復処理し、指定された条件を満たす最初の値を検索します。値がすべて条件を満たさない場合は、`None` を返します。

> **注：** 実験でファイル名が指定されていない場合、好きなファイル名を使用できます。たとえば、`main.rs` を使用して、`rustc main.rs &&./main` でコンパイルして実行できます。
