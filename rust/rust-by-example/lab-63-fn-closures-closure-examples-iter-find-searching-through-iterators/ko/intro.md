# 소개

이 실습에서는 Rust 의 `Iterator::find` 함수에 대해 배웁니다. 이 함수는 반복자를 반복하며 주어진 조건을 만족하는 첫 번째 값을 찾습니다. 어떤 값도 조건을 만족하지 않으면 `None`을 반환합니다.

> **참고:** 실습에서 파일 이름을 지정하지 않으면 원하는 파일 이름을 사용할 수 있습니다. 예를 들어 `main.rs`를 사용하고 `rustc main.rs && ./main`으로 컴파일 및 실행할 수 있습니다.
