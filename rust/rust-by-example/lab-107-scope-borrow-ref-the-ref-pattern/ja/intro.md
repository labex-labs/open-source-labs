# はじめに

この実験では、Rust プログラミング言語において、パターンマッチングや `let` バインドを通じた構造化による分解において、構造体/タプルのフィールドへの参照を取得するために使用される `ref` キーワードを調べます。

> **注：** 実験でファイル名が指定されていない場合、好きなファイル名を使用できます。たとえば、`main.rs` を使用して、`rustc main.rs &&./main` でコンパイルして実行できます。
