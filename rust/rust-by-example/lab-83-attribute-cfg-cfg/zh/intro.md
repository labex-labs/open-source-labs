# 简介

在本实验中，你将学习 Rust 中的 `cfg` 属性和 `cfg!` 宏，它们分别用于在配置和求值中进行条件检查。`cfg` 属性启用条件编译，而 `cfg!` 宏在运行时计算为真或假。与可以移除代码的 `#[cfg]` 不同，使用 `cfg!` 的代码块无论求值结果如何都必须有效。

> **注意**：如果实验未指定文件名，你可以使用任何你想要的文件名。例如，你可以使用 `main.rs`，并通过 `rustc main.rs &&./main` 进行编译和运行。
