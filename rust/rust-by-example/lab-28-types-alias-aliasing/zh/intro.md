# 简介

在本实验中，我们将学习 Rust 中的别名（aliasing），它允许我们使用 `type` 语句为现有类型赋予一个新名称。别名必须遵循特定的命名约定，可用于为原始类型或用户定义类型创建新名称。别名的主要目的是减少代码重复并提高可读性。

> **注意**：如果实验未指定文件名，你可以使用任何你想要的文件名。例如，你可以使用 `main.rs`，并通过 `rustc main.rs &&./main` 进行编译和运行。
