# はじめに

この実験では、Rust プログラミング言語のコンテキストでエイリアシングの概念を検討します。エイリアシングとは、不変または可変の借用として、同じデータに対して複数の参照が作成される状況を指します。

> **注：** 実験でファイル名が指定されていない場合、好きなファイル名を使用できます。たとえば、`main.rs` を使用して、`rustc main.rs &&./main` でコンパイルして実行できます。
