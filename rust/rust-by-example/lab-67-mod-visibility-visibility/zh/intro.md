# 简介

在本实验中，我们将学习 Rust 模块中项的可见性，包括默认的私有可见性、使用 `pub` 修饰符来覆盖可见性，以及不同级别的可见性，如 `pub(in path)`、`pub(self)`、`pub(super)` 和 `pub(crate)`。我们还将探讨嵌套模块以及模块内访问私有项的限制。

> **注意**：如果实验未指定文件名，你可以使用任何你想要的文件名。例如，你可以使用 `main.rs`，通过 `rustc main.rs &&./main` 进行编译和运行。
