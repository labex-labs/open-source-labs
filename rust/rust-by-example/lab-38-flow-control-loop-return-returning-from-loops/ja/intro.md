# はじめに

この実験では、Rust のループからの戻り値の概念について学びます。これには、操作を成功するまで再試行し、`loop` 式内の `break` ステートメントの後に結果の値を置くことで、その値をコードの残りの部分に渡すことが含まれます。

> **注：** 実験でファイル名が指定されていない場合、好きなファイル名を使用できます。たとえば、`main.rs` を使用して、`rustc main.rs &&./main` でコンパイルして実行できます。
