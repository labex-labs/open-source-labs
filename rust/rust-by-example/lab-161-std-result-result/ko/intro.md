# 소개

이 랩에서는 Rust 의 `Result` 열거형을 살펴봅니다. `Result` 열거형은 연산의 성공 또는 실패를 나타내기 위해 `Ok`와 값을 반환하거나, 실패 이유를 설명하는 `Err`를 반환하는 방식으로 사용됩니다.

> **참고:** 랩에서 파일 이름을 지정하지 않은 경우, 원하는 파일 이름을 사용할 수 있습니다. 예를 들어, `main.rs`를 사용하고, `rustc main.rs && ./main`으로 컴파일하고 실행할 수 있습니다.
