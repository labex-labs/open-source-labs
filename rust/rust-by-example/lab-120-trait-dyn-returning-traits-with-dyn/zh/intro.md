# 简介

在本实验中，我们将学习如何通过使用 `Box<dyn Animal>` 类型来解决 Rust 中直接返回 trait 的限制，该类型允许函数返回对实现 `Animal` trait 的堆分配对象的引用。

> **注意**：如果实验未指定文件名，你可以使用任何你想要的文件名。例如，你可以使用 `main.rs`，并通过 `rustc main.rs &&./main` 进行编译和运行。
