# 소개

이 랩에서는 Rust 에서 트레이트를 직접 반환하는 것의 제한 사항을 해결하기 위해 `Box<dyn Animal>` 타입을 사용하는 방법을 배웁니다. 이 타입은 함수가 `Animal` 트레이트를 구현하는 힙 할당 객체에 대한 참조를 반환할 수 있도록 합니다.

> **참고:** 랩에서 파일 이름을 지정하지 않은 경우, 원하는 파일 이름을 사용할 수 있습니다. 예를 들어, `main.rs`를 사용하고 `rustc main.rs && ./main`으로 컴파일하고 실행할 수 있습니다.
