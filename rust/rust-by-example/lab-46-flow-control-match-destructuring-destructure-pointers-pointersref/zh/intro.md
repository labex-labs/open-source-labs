# 简介

在本实验中，我们将探讨 Rust 中解构（destructuring）和解引用（dereferencing）的概念，强调它们与 C/C++ 等语言在用法上的差异。解构涉及使用`&`、`ref`和`ref mut`来分配和匹配引用，而解引用则使用`*`来访问引用所指向的值。

> **注意**：如果实验未指定文件名，你可以使用任何你想要的文件名。例如，你可以使用`main.rs`，并通过`rustc main.rs &&./main`进行编译和运行。
