# 简介

在本实验中，引入了 `?` 运算符，作为一种在链式结果时使代码更简洁的方法。它用于返回 `Result` 的表达式末尾，并通过自动处理 `Err` 和 `Ok` 分支来简化代码。提供的示例演示了如何在 Rust 中使用 `?` 运算符来处理各种数学运算及其潜在错误。

> **注意**：如果实验未指定文件名，你可以使用任何你想要的文件名。例如，你可以使用 `main.rs`，并通过 `rustc main.rs &&./main` 进行编译和运行。
