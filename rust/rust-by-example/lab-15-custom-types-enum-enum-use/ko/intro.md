# 소개

이 랩에서는 Rust 에서 수동 스코핑 (scoping) 의 필요성을 없애고 네임스페이스 (namespace) 를 명시적으로 지정하지 않고도 이름을 사용할 수 있도록 하기 위해 `use` 선언을 사용합니다.

> **참고:** 랩에서 파일 이름을 지정하지 않은 경우, 원하는 파일 이름을 사용할 수 있습니다. 예를 들어, `main.rs`를 사용하고 `rustc main.rs && ./main`으로 컴파일하고 실행할 수 있습니다.
