# 简介

在本实验中，我们将学习 Rust 中的边界（bounds），它用于限制泛型类型的生命周期或特性。`:` 字符用于表示类型中的所有引用必须比某个生命周期更长，而 `+` 用于表示类型必须实现某个特性，并且其中的所有引用必须比某个生命周期更长。一个示例代码片段展示了 Rust 中边界的语法和用法。

> **注意**：如果实验未指定文件名，你可以使用任何你想要的文件名。例如，你可以使用 `main.rs`，并通过 `rustc main.rs &&./main` 进行编译和运行。
