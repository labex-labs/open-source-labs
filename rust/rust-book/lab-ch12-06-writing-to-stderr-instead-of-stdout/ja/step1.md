# 標準エラーにエラーメッセージを書き込む代わりに標準出力に書き込む

現在、`println!` マクロを使用してすべての出力を端末に書き込んでいます。ほとんどの端末では、2 種類の出力があります。一般的な情報用の _標準出力_ (`stdout`) と、エラーメッセージ用の _標準エラー_ (`stderr`) です。この区別により、ユーザーはプログラムの正常な出力をファイルに出力するように選択できますが、エラーメッセージは画面に表示されます。

`println!` マクロは標準出力にのみ印刷できるため、標準エラーに印刷するには別のものを使用する必要があります。
