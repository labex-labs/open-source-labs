# はじめに

**標準エラーにエラーメッセージを書き込む代わりに標準出力に書き込む**へようこそ。この実験は、[Rust Book](https://doc.rust-lang.org/book/)の一部です。LabExでRustのスキルを練習することができます。

この実験では、エラーメッセージが標準出力 (`stdout`) ではなく標準エラー (`stderr`) に書き込まれるようにコードを変更する必要があります。
