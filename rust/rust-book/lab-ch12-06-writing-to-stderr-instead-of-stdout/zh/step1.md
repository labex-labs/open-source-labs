# 将错误信息写入标准错误而非标准输出

目前，我们使用 `println!` 宏将所有输出写入终端。在大多数终端中，有两种输出：用于常规信息的 _标准输出_（`stdout`）和用于错误信息的 _标准错误_（`stderr`）。这种区分使用户能够选择将程序的成功输出定向到文件，但仍将错误信息打印到屏幕上。

`println!` 宏仅能够打印到标准输出，因此我们必须使用其他方法来打印到标准错误。
