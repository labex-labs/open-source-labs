# はじめに

**参照と借用**へようこそ。この実験は、[Rust Book](https://doc.rust-lang.org/book/)の一部です。LabEx で Rust のスキルを練習することができます。

この実験では、Rust における参照を使って値を所有する代わりに借用する方法を学びます。これにより、呼び出し元の関数に所有権を戻す必要なく、データを渡して操作することができます。
