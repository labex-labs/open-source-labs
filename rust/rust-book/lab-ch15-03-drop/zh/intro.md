# 简介

欢迎来到**使用 Drop 特性在清理时运行代码**。本实验是 [Rust 书籍](https://doc.rust-lang.org/book/) 的一部分。你可以在 LabEx 中练习你的 Rust 技能。

在本实验中，我们将探索 Rust 中的 `Drop` 特性，它允许在值超出作用域时自定义清理操作，通常用于智能指针，并且可以通过提供一个由 Rust 自动调用的 `drop` 方法来实现，从而避免了显式清理代码的需要。
