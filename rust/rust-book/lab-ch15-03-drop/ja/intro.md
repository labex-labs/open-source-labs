# はじめに

「Drop トレイトを使ったクリーンアップ時のコード実行」へようこそ。この実験は、[Rust Book](https://doc.rust-lang.org/book/)の一部です。LabEx で Rust のスキルを練習することができます。

この実験では、Rust の`Drop`トレイトを調べます。これは、値がスコープ外になるときのクリーンアップアクションのカスタマイズを可能にします。主にスマートポインタで使用され、Rust によって自動的に呼び出される`drop`メソッドを提供することで実装できます。これにより、明示的なクリーンアップコードが不要になります。
