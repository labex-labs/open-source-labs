# 简介

欢迎来到「使用 `Box<T>` 指向堆上的数据」。本实验是 [《Rust 程序设计语言》](https://doc.rust-lang.org/book/) 的一部分。你可以在 LabEx 中练习 Rust 技能。

在本实验中，我们将学习在以下几种情况下，如何使用 `Box<T>` 智能指针在堆上而非栈上存储数据：类型大小在编译时未知、转移大量数据的所有权以避免复制，或者拥有实现特定 trait 的值。
