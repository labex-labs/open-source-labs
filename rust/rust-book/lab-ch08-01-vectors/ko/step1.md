# 벡터를 사용하여 값 목록 저장하기

가장 먼저 살펴볼 컬렉션 타입은 `Vec<T>`이며, _벡터 (vector)_ 라고도 합니다. 벡터를 사용하면 메모리에서 모든 값을 서로 인접하게 배치하는 단일 데이터 구조에 둘 이상의 값을 저장할 수 있습니다. 벡터는 동일한 타입의 값만 저장할 수 있습니다. 파일의 텍스트 줄이나 장바구니의 품목 가격과 같이 항목 목록이 있는 경우 유용합니다.
