# はじめに

**ベクトルを使った値のリストの保存**へようこそ。この実験は、[Rust Book](https://doc.rust-lang.org/book/)の一部です。あなたはLabExでRustのスキルを練習することができます。

「この実験では、`Vec<T>`コレクション型（ベクトルとも呼ばれます）を調べます。これは、同じ型の値のリストを単一のデータ構造に保存することができます。」
