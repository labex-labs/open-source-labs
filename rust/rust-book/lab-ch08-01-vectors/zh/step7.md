# 向量离开作用域时会释放其元素

和任何其他 `struct` 一样，当向量超出作用域时会被释放，如清单 8-10 所示。

```rust
{
    let v = vec![1, 2, 3, 4];

    // 对v进行一些操作
} // <- v 超出作用域并在此处被释放
```

清单 8-10：展示向量及其元素被释放的位置

当向量被释放时，它的所有内容也会被释放，这意味着它所包含的整数将被清理。借用检查器确保对向量内容的任何引用仅在向量本身有效时使用。

让我们继续学习下一种集合类型：`String`！
