# 简介

欢迎来到「可反驳性：模式是否可能匹配失败」。本实验是 [《Rust 程序设计语言》](https://doc.rust-lang.org/book/) 的一部分。你可以在 LabEx 中练习 Rust 技能。

在本实验中，我们将学习模式中的可反驳性概念，以及它如何影响 Rust 中的匹配过程，包括可反驳模式和不可反驳模式之间的区别，以及如何在不同的结构（如 `let` 语句和 `if let` 表达式）中正确使用它们。
