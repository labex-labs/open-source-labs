# はじめに

**参照サイクルによるメモリリーク**へようこそ。この実験は、[Rust Book](https://doc.rust-lang.org/book/)の一部です。LabEx で Rust のスキルを練習することができます。

この実験では、Rust のメモリセーフティ保証が、偶然にメモリリークを作成するのを困難にはしますが不可能にはしない方法を探ります。特に、`Rc<T>`と`RefCell<T>`を使用する場合、参照サイクルが発生して値が破棄されず、メモリがリークする可能性があります。
