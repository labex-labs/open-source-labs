# 文字列リテラルをスライスとして

先ほど、文字列リテラルがバイナリ内に格納されていることについて説明しました。スライスについて学んだ今、文字列リテラルを適切に理解することができます。

```rust
let s = "Hello, world!";
```

ここでの`s`の型は`&str`です。これは、バイナリの特定の箇所を指すスライスです。これが、文字列リテラルが不変である理由でもあります。`&str`は不変参照です。
