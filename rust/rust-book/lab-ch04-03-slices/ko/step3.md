# String Literals as Slices (슬라이스로서의 문자열 리터럴)

이진 파일 내부에 문자열 리터럴이 저장된다는 것을 기억하세요. 이제 슬라이스에 대해 알았으므로 문자열 리터럴을 제대로 이해할 수 있습니다.

```rust
let s = "Hello, world!";
```

여기서 `s`의 유형은 `&str`입니다. 즉, 이진 파일의 특정 지점을 가리키는 슬라이스입니다. 이것이 또한 문자열 리터럴이 불변인 이유입니다. `&str`은 불변 참조입니다.
