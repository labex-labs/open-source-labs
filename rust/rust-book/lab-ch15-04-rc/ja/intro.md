# はじめに

**Rc<T>、参照カウントスマートポインタ**へようこそ。この実験は、[Rust Book](https://doc.rust-lang.org/book/)の一部です。LabExでRustのスキルを練習できます。

この実験では、RustにおけるRc<T>（参照カウント）の使用方法を調べます。これにより、値への参照数を追跡し、所有者がいなくなったときにのみクリーンアップされるように、値の複数の所有者を可能にします。
