# はじめに

**メソッド構文**へようこそ。この実験は、[Rust Book](https://doc.rust-lang.org/book/)の一部です。LabExでRustのスキルを練習できます。

この実験では、メソッドは`fn`キーワードと名前で宣言され、パラメータと戻り値を持つことができ、構造体のコンテキスト内で定義されます。最初のパラメータは常に`self`で、呼び出されている構造体のインスタンスを表します。
