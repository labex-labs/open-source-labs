# メソッド構文

**メソッド**は関数に似ています。`fn`キーワードと名前で宣言し、パラメータと戻り値を持ち、他の場所からメソッドが呼び出されたときに実行されるコードを含んでいます。関数とは異なり、メソッドは構造体（または列挙体またはトレイトオブジェクト、それぞれ第 6 章と第 17 章で扱います）のコンテキスト内で定義され、最初のパラメータは常に`self`で、メソッドが呼び出されている構造体のインスタンスを表します。
