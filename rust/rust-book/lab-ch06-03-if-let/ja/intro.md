# はじめに

**if let による簡潔な制御フロー**へようこそ。この実験は、[Rust Book](https://doc.rust-lang.org/book/)の一部です。LabEx で Rust のスキルを練習できます。

この実験では、`if let`構文の簡潔な制御フローを調べます。これにより、`if`と`let`を組み合わせて、より短く読みやすい方法で、1 つのパターンに一致する値を処理し、それ以外を無視することができます。
