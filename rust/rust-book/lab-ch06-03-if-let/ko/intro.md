# 소개

**if let 을 사용한 간결한 제어 흐름**에 오신 것을 환영합니다. 이 랩은 [Rust Book](https://doc.rust-lang.org/book/)의 일부입니다. LabEx 에서 Rust 기술을 연습할 수 있습니다.

이 랩에서는 `if let` 구문의 간결한 제어 흐름을 살펴봅니다. 이를 통해 `if`와 `let`을 결합하여 나머지 패턴을 무시하면서 하나의 패턴과 일치하는 값을 처리하는 더 짧고 읽기 쉬운 방식으로 구현할 수 있습니다.
