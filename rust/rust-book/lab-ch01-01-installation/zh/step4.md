# 更新与卸载

通过 `rustup` 安装 Rust 后，更新到新发布的版本很容易。在你的 shell 中，运行以下更新脚本：

```bash
rustup update
```

要卸载 Rust 和 `rustup`，在你的 shell 中运行以下卸载脚本：

```bash
rustup self uninstall
```

当系统提示你确认是否要卸载 Rust 时，输入 `N`。
